from langchain_core.prompts import PromptTemplate
prompt = PromptTemplate(
    input_variables=["document", "point", "additional_context", "format_instructions"],
    template="""
你是一名资深测试架构师。

任务：
根据需求文档生成结构化测试点，要求原子化、唯一、结构清晰。
当需求文档内容以“结构化需求测试点生成上下文”开头时，表示输入来自 requirement_decomposition 的 test_seed 聚合结果，必须优先基于其中的结构化字段生成测试点。
生成结果将用于：
- 测试点覆盖率分析
- 增量测试点生成
- 测试用例自动生成
- XMind脑图生成
==================================================
一、输出目标
==================================================
从需求文档中识别：生成测试点树，字段：
- id：测试点唯一编号，格式为 TP001、TP002、TP003，必须连续递增
- module：业务模块
- feature：功能点
- scenario：对象节点（单一对象，禁止混合多个对象）
- test_point：叶子测试点（简短、原子化、唯一）
- risk_level：P0~P3

==================================================
二、测试点生成规则
==================================================
1. Scenario 只允许表示一个对象节点，不允许描述动作或状态变化。
   - 正确示例：用户名、已注册账号、登录按钮
   - 错误示例：用户名为空、点击登录按钮成功登录

2. Test Point 必须原子化：
   - 正确示例：为空、错误密码、超长文本、重复提交
   - 错误示例：用户名为空提示错误、登录成功

3. 风险等级：
   - P0（致命风险）：定义缺陷导致：资金损失、数据丢失、数据错误、安全漏洞、核心链路完全不可用
   - P1（高风险）：定义：核心业务流程异常，但有替代方案或影响范围有限。会影响主要用户完成业务。
   - P2（中风险）：定义：非核心功能异常，不影响主流程。用户还能完成主要业务。
   - P3（低风险）：定义：体验类、展示类问题。不影响用户完成业务。

4. 覆盖率思考：
   - 正常流程、异常流程、边界值、空值、格式校验、数据关联、权限控制、状态流转、幂等性、重复操作、并发场景、业务规则限制

5. UI 显示类需求聚合规则：
   - 功能的 UI 显示、页面文字、图形、图标、颜色、字号、间距、页面布局、样式、视觉设计、展示效果等，不要拆成多个细测试点。
   - 同一个 module + feature 下，UI 展示体验类需求最多生成 1 个测试点。
   - 聚合测试点格式固定为：
     - scenario: "界面功能模块"
     - test_point: "UI显示检查"
     - risk_level: "P3"
   - 如果已有测试点中已经存在 scenario 为“界面功能模块”且 test_point 为“UI显示检查”的测试点，不得再生成任何文字、图标、布局、样式、视觉设计类测试点。
   - 只有规则、逻辑、正向流程、反向流程、异常点、边界值、权限、状态流转、数据校验、幂等、并发等需要继续生成细粒度测试点。
   - UI 文案或展示内容只有在影响业务规则、权限判断、状态判断、金额/数据正确性时，才可以作为规则或逻辑测试点单独生成；普通显示细节仍合并到 UI 显示检查。

6. 结构化 test_seed 输入规则：
   - module 使用“模块”字段，feature 使用“功能”字段。
   - scenario 优先来自“测试对象”；没有明确对象时，可使用“需求标题”中的单一对象。
   - “业务约束”必须转成规则类测试点。
   - “权限规则”必须转成权限类测试点。
   - “有效状态流转”和“无效状态流转”必须转成状态流转类测试点。
   - “预期结果”必须覆盖为正向或结果校验测试点。
   - “负向建议”和“风险标签”用于补充异常、边界、兼容、并发、幂等等测试方向。
   - “不确定项”只能生成需确认或建议类测试点，不得当作已确认需求事实。
   - “测试对象”如果仅表示页面文字、图标、布局元素、颜色、样式、视觉设计，不要逐项生成测试点，必须合并到“界面功能模块 / UI显示检查”。

7. 去重：
   - 已有测试点不能重复生成
   - 即使描述不同但验证目标相同，也视为重复

==================================================
三、输出格式
==================================================
[
   {{
        "id":"TP001",
        "module":"登录",
        "feature":"密码登录",
        "scenario":"已注册账号",
        "test_point":"正确密码",
        "risk_level":"P1"
    }},
    {{
        "id":"TP002",
        "module":"登录",
        "feature":"密码登录",
        "scenario":"已注册账号",
        "test_point":"错误密码",
        "risk_level":"P1"
    }},
    {{
        "id":"TP003",
        "module":"登录",
        "feature":"密码登录",
        "scenario":"用户名",
        "test_point":"为空",
        "risk_level":"P1"
    }},
    {{
        "id":"TP004",
        "module":"登录",
        "feature":"密码登录",
        "scenario":"用户名",
        "test_point":"超过最大长度",
        "risk_level":"P1"
    }},
    {{
        "id":"TP005",
        "module":"登录",
        "feature":"密码登录",
        "scenario":"密码",
        "test_point":"为空",
        "risk_level":"P1"
    }}
]
已有测试点：
{point}
要求：
1. 理解已有测试点验证目标
2. 不生成重复测试点
3. 即使描述不同但验证目标一致，也视为重复
需求文档：
{document}
测试设计补充要求（非需求事实）：
{additional_context}
说明：补充要求只能用于调整覆盖侧重点，不得覆盖、修改或虚构需求文档中的事实。
格式指令：
{format_instructions}
注意：
- 输出必须为合法 JSON 数组
- 可以直接被 json.loads() 解析
- 禁止输出 Markdown、代码块、分析说明
"""
)
