# 平台对宿主机暴露的唯一端口。
PLATFORM_PORT=8080

# PostgreSQL 配置。生产或共享环境必须替换示例密码。
POSTGRES_DB=test_platform
POSTGRES_USER=platform
POSTGRES_PASSWORD=replace-with-a-strong-password

# FastAPI 运行配置。
APP_ENV=development
LOG_LEVEL=INFO
TOOL_HEALTH_TIMEOUT_SECONDS=3

# 接口自动化工具（api-autotest）可选配置，均有合理默认值。
# 单任务最长执行时间（秒）与任务列表保留条数。
API_AUTOTEST_TASK_TIMEOUT_SECONDS=1800
API_AUTOTEST_TASKS_RETAIN=50
