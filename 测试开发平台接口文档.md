# 测试开发平台接口文档

> 生成日期: 2026-08-13  
> 服务版本: 1.0.0（依据线上运行时 OpenAPI 定义生成）  
> 适用对象: 开发、测试同学

---

## 1. 使用说明

### 1.1 Base URL

```
http://170.106.159.147:41873
```

所有接口统一前缀 `/api/v1`。

### 1.2 鉴权方式

平台有两类调用身份：

**① 用户会话（Web 控制台 / 人工调用）**

1. 调用 `POST /api/v1/auth/login` 登录，响应通过 `Set-Cookie` 下发：
   - `tp_session`：会话凭证，后续请求自动携带（Cookie）；
   - `tp_csrf`：CSRF 令牌。
2. **所有写操作（POST / PUT / PATCH / DELETE）** 必须携带请求头 `X-CSRF-Token`，其值等于 `tp_csrf` Cookie 的值（双提交校验），否则返回 `403 CSRF_INVALID`。登录与初始化接口除外。
3. 会话过期或被注销后，接口返回 `401 AUTH_REQUIRED`，需重新登录。

使用 curl 时的典型姿势：

```bash
# 登录并把 Cookie 存入 cookies.txt
curl -X POST 'http://170.106.159.147:41873/api/v1/auth/login' \
  -H 'Content-Type: application/json' \
  -c cookies.txt \
  -d '{"username": "你的用户名", "password": "你的密码"}'

# 从 cookies.txt 提取 tp_csrf 值
CSRF=$(awk '$6=="tp_csrf" {print $7}' cookies.txt)

# 后续写操作携带 Cookie 与 CSRF 头
curl -X POST 'http://170.106.159.147:41873/api/v1/...' \
  -H 'Content-Type: application/json' \
  -H "X-CSRF-Token: $CSRF" \
  -b cookies.txt \
  -d '{...}'
```

**② 工具身份（内部接口）**

- `/api/v1/internal/tools/*` 接口使用工具 Client Token 鉴权：请求头 `Authorization: Bearer <token>`。
- Token 在工具注册时一次性下发，平台只保存哈希，丢失需重新注册。
- 每个工具 Token 绑定最小能力集（config / credential / audit 等），越权返回 `403 TOOL_CLIENT_FORBIDDEN`。
- **注意：线上 Nginx 网关不对外暴露 `/api/v1/internal/*` 与 `/openapi.json`**（返回 404）。这些接口只能在服务器容器网络内调用，例如：

```bash
ssh ubuntu@170.106.159.147 \
  'cd ~/Testproject/test-platform && docker compose exec -T platform-api \
   python -c "import urllib.request; print(urllib.request.urlopen(\\"http://127.0.0.1:8000/api/v1/health/ready\\").read().decode())"'
```

### 1.3 统一错误响应格式

所有非 2xx 响应均为如下结构（含 FastAPI 参数校验错误）：

```json
{
  "code": "VERSION_CONFLICT",
  "message": "配置草稿已被其他操作更新",
  "request_id": "req_5f2c1a9b8e3d4a6f9c0b1d2e3f4a5b6c"
}
```

- `code`：稳定错误码，程序判断请以此为准；
- `message`：可直接展示的中文提示；
- `request_id`：排障标识，同时出现在响应头 `X-Request-ID`，反馈问题时请附带。

### 1.4 关键概念速览

| 概念 | 说明 |
| --- | --- |
| 环境 environment | 当前线上仅 `dev` 一个环境 |
| 配置定义 definition | 配置项的元数据（键、类型、敏感度、默认值），id 形如 `api-autotest.GATEWAY_API_URL` |
| 配置版本 release | 一次可发布/回滚的配置快照，草稿 → 校验 → 发布流程 |
| Secret | 敏感配置，加密存储、多版本；只有发布冻结的版本会下发给工具 |
| 凭证 credential | 工具自动维护的第三方会话（gateway_session / admin_login），由后台代理验证与续期 |
| 工具 tool | 接入平台的工具（如 `api-autotest`、`truthy-search`、`api-test-agent` 等） |

---

## 2. 接口总览

**初始化**

| 方法 | 路径 | 接口 |
| --- | --- | --- |
| POST | `/api/v1/setup` | 平台初始化 |

**认证与会话**

| 方法 | 路径 | 接口 |
| --- | --- | --- |
| POST | `/api/v1/auth/login` | 用户登录 |
| POST | `/api/v1/auth/logout` | 退出登录 |
| GET | `/api/v1/auth/me` | 当前用户信息 |
| POST | `/api/v1/auth/change-password` | 修改密码 |
| GET | `/api/v1/auth/sessions` | 会话列表 |
| DELETE | `/api/v1/auth/sessions/{session_id}` | 注销指定会话 |

**工具管理**

| 方法 | 路径 | 接口 |
| --- | --- | --- |
| GET | `/api/v1/tools` | 工具列表 |
| GET | `/api/v1/tools/{tool_id}/health` | 工具健康检查 |

**配置管理**

| 方法 | 路径 | 接口 |
| --- | --- | --- |
| GET | `/api/v1/config/definitions` | 配置定义列表 |
| GET | `/api/v1/config/releases` | 配置版本列表 |
| POST | `/api/v1/config/releases` | 创建配置版本 |
| PUT | `/api/v1/config/releases/{release_id}/items` | 更新草稿配置项 |
| POST | `/api/v1/config/releases/{release_id}/validate` | 校验配置版本 |
| POST | `/api/v1/config/releases/{release_id}/publish` | 发布配置版本 |
| POST | `/api/v1/config/releases/{release_id}/promote` | 提升配置版本 |
| POST | `/api/v1/config/releases/{release_id}/rollback` | 回滚配置版本 |

**Secret 管理**

| 方法 | 路径 | 接口 |
| --- | --- | --- |
| GET | `/api/v1/secrets` | Secret 列表 |
| PUT | `/api/v1/secrets/{secret_id}` | 写入 Secret |

**凭证管理**

| 方法 | 路径 | 接口 |
| --- | --- | --- |
| GET | `/api/v1/credentials` | 凭证列表 |
| POST | `/api/v1/credentials` | 创建凭证 |

**审计**

| 方法 | 路径 | 接口 |
| --- | --- | --- |
| GET | `/api/v1/audit/events` | 审计事件列表 |
| GET | `/api/v1/audit/events/{event_id}` | 审计事件详情 |
| POST | `/api/v1/audit/exports` | 导出审计事件 |

**用户与权限管理**

| 方法 | 路径 | 接口 |
| --- | --- | --- |
| GET | `/api/v1/admin/users` | 用户列表 |
| POST | `/api/v1/admin/users` | 创建用户 |
| GET | `/api/v1/admin/users/{user_id}` | 用户详情 |
| PATCH | `/api/v1/admin/users/{user_id}` | 更新用户 |
| POST | `/api/v1/admin/users/{user_id}/reset-password` | 重置用户密码 |
| DELETE | `/api/v1/admin/users/{user_id}/sessions` | 注销用户全部会话 |
| GET | `/api/v1/admin/roles` | 角色列表 |
| POST | `/api/v1/admin/roles` | 创建角色 |
| GET | `/api/v1/admin/roles/{role_id}` | 角色详情 |
| PATCH | `/api/v1/admin/roles/{role_id}` | 更新角色 |
| DELETE | `/api/v1/admin/roles/{role_id}` | 删除角色 |
| GET | `/api/v1/admin/permissions` | 权限定义列表 |

**健康检查**

| 方法 | 路径 | 接口 |
| --- | --- | --- |
| GET | `/api/v1/health/live` | 存活探针 |
| GET | `/api/v1/health/ready` | 就绪探针 |

**内部接口（工具调用，外网不可达）**

| 方法 | 路径 | 接口 |
| --- | --- | --- |
| GET | `/api/v1/internal/authorize` | 网关鉴权回调 |
| GET | `/api/v1/internal/tools/{tool_id}/runtime-config` | 工具运行时配置 |
| POST | `/api/v1/internal/tools/{tool_id}/config-ack` | 配置确认 |
| POST | `/api/v1/internal/tools/{tool_id}/credential-status` | 凭证状态上报 |
| PUT | `/api/v1/internal/tools/{tool_id}/credentials/{credential_id}/session` | 写回凭证会话 |
| POST | `/api/v1/internal/tools/{tool_id}/audit-events` | 上报审计事件 |

---

## 3. 接口详情

### 模块：初始化

### 平台初始化

`POST /api/v1/setup`

使用一次性 Bootstrap Token 创建首个平台管理员账号并自动登录。仅当平台尚无任何用户时可调用，调用成功后该接口返回 404。

**鉴权**: 无需会话；请求体需携带 `bootstrap_token`（服务器文件 `.runtime-secrets/dev/bootstrap-token`）

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `bootstrap_token` | string (≤512) | 是 | 长度≥16、长度≤512 |
| `username` | string (≤128) | 是 | 长度≥3、长度≤128 |
| `display_name` | string (≤128) | 是 | 长度≥1、长度≤128 |
| `password` | string (≤256) | 是 | 长度≥12、长度≤256 |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/setup' \
  -H 'Content-Type: application/json' \
  -d '{
  "bootstrap_token": "string",
  "username": "string",
  "display_name": "string",
  "password": "string"
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `user` | object | 是 | 可安全返回浏览器的用户摘要。 |
| `roles` | array<string> | 是 | - |
| `platform_permissions` | array<string> | 是 | - |
| `tool_permissions` | object | 是 | - |
| `session_expires_at` | string (ISO8601 时间) | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "user": {
    "id": "string",
    "username": "string",
    "display_name": "string",
    "status": "string",
    "must_change_password": true
  },
  "roles": [
    "string"
  ],
  "platform_permissions": [
    "string"
  ],
  "tool_permissions": {},
  "session_expires_at": "2026-08-13T08:00:00Z"
}
```

**可能的业务错误**
特征错误：`BOOTSTRAP_DENIED`、`USERNAME_EXISTS`、`NOT_FOUND（已初始化过）`。完整说明见文末错误码总表。


### 模块：认证与会话

### 用户登录

`POST /api/v1/auth/login`

校验用户名密码，成功后通过 Set-Cookie 下发会话 Cookie `tp_session` 与 CSRF Cookie `tp_csrf`。连续失败 5 次（15 分钟窗口）将被锁定 15 分钟。

**鉴权**: 无需会话

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `username` | string (≤128) | 是 | 长度≥1、长度≤128 |
| `password` | string (≤256) | 是 | 长度≥1、长度≤256 |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/auth/login' \
  -H 'Content-Type: application/json' \
  -d '{
  "username": "admin",
  "password": "********"
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `user` | object | 是 | 可安全返回浏览器的用户摘要。 |
| `roles` | array<string> | 是 | - |
| `platform_permissions` | array<string> | 是 | - |
| `tool_permissions` | object | 是 | - |
| `session_expires_at` | string (ISO8601 时间) | 是 | - |

真实响应示例（HTTP 200，已脱敏）：
```json
{
  "user": {
    "id": "usr_631a700e12734ae1a613a337e9124e8f",
    "username": "admin",
    "display_name": "Administrator",
    "status": "active",
    "must_change_password": false
  },
  "roles": [
    "role_platform_admin"
  ],
  "platform_permissions": [
    "platform.audit.export",
    "platform.audit.view",
    "platform.config.manage",
    "platform.role.manage",
    "platform.secret.manage",
    "platform.user.manage"
  ],
  "tool_permissions": {
    "trackevents": [
      "api-test-agent.case.review",
      "api-test-agent.contract.review",
      "api-test-agent.defect.create",
      "api-test-agent.execute",
      "task.cancel",
      "task.view.all",
      "tool.config.manage",
      "tool.execute",
      "tool.result.view",
      "tool.secret.manage",
      "tool.view"
    ],
    "...": "其余 5 个工具的授权结构相同，已省略"
  },
  "session_expires_at": "2026-08-14T08:20:58.566478Z"
}
```

**可能的业务错误**
特征错误：`INVALID_CREDENTIALS`、`ACCOUNT_LOCKED`。完整说明见文末错误码总表。

错误响应示例（HTTP 401）：
```json
{
  "code": "INVALID_CREDENTIALS",
  "message": "用户名或密码错误",
  "request_id": "req_aad6eb08e7e446b2975c62acfc4a3c0d"
}
```


### 退出登录

`POST /api/v1/auth/logout`

注销当前会话并清除 Cookie。

**鉴权**: 会话 + CSRF

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/auth/logout' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `message` | string | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "message": "string"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）。完整说明见文末错误码总表。


### 当前用户信息

`GET /api/v1/auth/me`

返回当前登录用户、角色与权限信息。

**鉴权**: 会话

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/auth/me' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `user` | object | 是 | 可安全返回浏览器的用户摘要。 |
| `roles` | array<string> | 是 | - |
| `platform_permissions` | array<string> | 是 | - |
| `tool_permissions` | object | 是 | - |
| `session_expires_at` | string (ISO8601 时间) | 是 | - |

真实响应示例（HTTP 200，已脱敏）：
```json
{
  "user": {
    "id": "usr_631a700e12734ae1a613a337e9124e8f",
    "username": "admin",
    "display_name": "Administrator",
    "status": "active",
    "must_change_password": false
  },
  "roles": [
    "role_platform_admin"
  ],
  "platform_permissions": [
    "platform.audit.export",
    "platform.audit.view",
    "platform.config.manage",
    "platform.role.manage",
    "platform.secret.manage",
    "platform.user.manage"
  ],
  "tool_permissions": {
    "trackevents": [
      "api-test-agent.case.review",
      "api-test-agent.contract.review",
      "api-test-agent.defect.create",
      "api-test-agent.execute",
      "task.cancel",
      "task.view.all",
      "tool.config.manage",
      "tool.execute",
      "tool.result.view",
      "tool.secret.manage",
      "tool.view"
    ],
    "...": "其余 5 个工具的授权结构相同，已省略"
  },
  "session_expires_at": "2026-08-14T08:20:58.566478Z"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。

错误响应示例（HTTP 401）：
```json
{
  "code": "AUTH_REQUIRED",
  "message": "请先登录",
  "request_id": "req_c016fa0452cd406baaeff5b10dd13c48"
}
```


### 修改密码

`POST /api/v1/auth/change-password`

修改当前用户密码，成功后其他会话全部失效。

**鉴权**: 会话 + CSRF

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `current_password` | string (≤256) | 是 | 长度≥1、长度≤256 |
| `new_password` | string (≤256) | 是 | 长度≥12、长度≤256 |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/auth/change-password' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt \
  -d '{
  "current_password": "string",
  "new_password": "string"
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `message` | string | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "message": "string"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）；特征错误：`CURRENT_PASSWORD_INVALID`。完整说明见文末错误码总表。

错误响应示例（HTTP 403）：
```json
{
  "code": "CSRF_INVALID",
  "message": "请求安全校验失败",
  "request_id": "req_0ac733d10d1343c8b251b0100ebfc3f8"
}
```

错误响应示例（HTTP 422）：
```json
{
  "code": "VALIDATION_ERROR",
  "message": "请求参数不正确",
  "request_id": "req_763b33143c034cf68cde2c3f21c8d825"
}
```


### 会话列表

`GET /api/v1/auth/sessions`

列出当前用户的有效会话。

**鉴权**: 会话

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/auth/sessions' \
  -b cookies.txt
```

**响应**

真实响应示例（HTTP 200，已脱敏）：
```json
[
  {
    "id": "ses_31d75fce8a9246888347d4da24233d4e",
    "created_at": "2026-08-13T08:20:58.457668Z",
    "last_seen_at": "2026-08-13T08:20:58.566478Z",
    "absolute_expires_at": "2026-08-14T08:20:58.566478Z",
    "ip_address": "43.173.72.75",
    "current": true
  },
  {
    "id": "ses_2cc0c9294e89480e9b921d25637b40be",
    "created_at": "2026-08-13T08:09:57.964429Z",
    "last_seen_at": "2026-08-13T08:09:58.073147Z",
    "absolute_expires_at": "2026-08-14T08:09:58.073147Z",
    "ip_address": "43.173.72.75",
    "current": false
  },
  {
    "...": "其余 11 条会话已省略"
  }
]
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 注销指定会话

`DELETE /api/v1/auth/sessions/{session_id}`

注销当前用户的某个会话（如其他设备）。

**鉴权**: 会话 + CSRF

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `session_id` | string | 会话 id |

**请求示例**

```bash
curl -X DELETE 'http://170.106.159.147:41873/api/v1/auth/sessions/{session_id}' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `message` | string | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "message": "string"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）。完整说明见文末错误码总表。


### 模块：工具管理

### 工具列表

`GET /api/v1/tools`

返回平台已登记的工具（含入口地址、分类、功能特性）。

**鉴权**: 会话

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/tools' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `items` | array<object> | 是 | - |

真实响应示例（HTTP 200，已脱敏）：
```json
{
  "items": [
    {
      "id": "trackevents",
      "name": "埋点测试",
      "description": "解析 TrackEvents 日志并校验事件字段",
      "entry_url": "/trackevents/",
      "short_code": "EVENT",
      "icon_key": "event",
      "category": "analysis",
      "features": [
        "事件统计",
        "字段校验",
        "结果报告"
      ],
      "sort_order": 10
    },
    {
      "id": "log-filter",
      "name": "日志筛选工具",
      "description": "上传、筛选并导出测试日志",
      "entry_url": "/log-filter/",
      "short_code": "LOG",
      "icon_key": "log",
      "category": "analysis",
      "features": [
        "日志上传",
        "条件筛选",
        "结果导出"
      ],
      "sort_order": 20
    },
    {
      "id": "truthy-search",
      "name": "检索评测",
      "description": "运行检索任务，对比基准结果并生成评测报告",
      "entry_url": "/truthy-search/",
      "short_code": "SEARCH",
      "icon_key": "search",
      "category": "evaluation",
      "features": [
        "检索执行",
        "字段对比",
        "评测报告"
      ],
      "sort_order": 30
    },
    {
      "id": "api-autotest",
      "name": "接口自动化",
      "description": "触发 Gateway 接口自动化执行，查看回归结果与 Allure 报告",
      "entry_url": "/api-autotest/",
      "short_code": "API",
      "icon_key": "api",
      "category": "automation",
      "features": [
        "执行触发",
        "结果统计",
        "报告查看"
      ],
      "sort_order": 40
    },
    {
      "id": "functional-test-agent",
      "name": "功能测试智能体",
      "description": "需求拆解、测试点 Review 与功能测试用例生成",
      "entry_url": "/functional-test-agent/",
      "short_code": "FT AI",
      "icon_key": "functional-ai",
      "category": "ai-testing",
      "features": [
        "需求拆解",
        "测试点 Review",
        "功能用例"
      ],
      "sort_order": 50
    },
    {
      "id": "api-test-agent",
      "name": "API 测试智能体",
      "description": "API 文档解析与文件化测试用例生成",
      "entry_url": "/api-test-agent/",
      "short_code": "API AI",
      "icon_key": "api-ai",
      "category": "ai-testing",
      "features": [
        "API 文档解析",
        "用例生成",
        "静态脚本检查"
      ],
      "sort_order": 60
    }
  ]
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 工具健康检查

`GET /api/v1/tools/{tool_id}/health`

实时探测指定工具的健康检查地址并返回结果。

**鉴权**: 会话 + 该工具的 `tool.view` 权限

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `tool_id` | string | 工具 id |

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/tools/{tool_id}/health' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `tool_id` | string | 是 | - |
| `status` | `healthy` / `unhealthy` | 是 | - |
| `checked_at` | string (ISO8601 时间) | 是 | - |

真实响应示例（HTTP 200，已脱敏）：
```json
{
  "tool_id": "trackevents",
  "status": "healthy",
  "checked_at": "2026-08-13T08:21:02.549797Z"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）；特征错误：`NOT_FOUND`、`PERMISSION_DENIED`。完整说明见文末错误码总表。

错误响应示例（HTTP 404）：
```json
{
  "code": "NOT_FOUND",
  "message": "工具不存在",
  "request_id": "req_a2737b97a8e34e11b8ce4085733048cd"
}
```


### 模块：配置管理

### 配置定义列表

`GET /api/v1/config/definitions`

返回配置项定义（键名、类型、敏感度、默认值等）。可按工具过滤。

**鉴权**: 会话

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `owner_type` | string | 否 | 配置归属类型，固定 `tool` |
| `owner_id` | string | 否 | 工具 id，如 `api-autotest`、`truthy-search` |

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/config/definitions' \
  -b cookies.txt
```

**响应**

真实响应示例（HTTP 200，已脱敏，线上当前共 98 条，此处截取前几条）：
```json
[
  {
    "id": "api-autotest.GATEWAY_API_URL",
    "key": "GATEWAY_API_URL",
    "display_name": "Gateway 会话接口",
    "description": "Gateway 会话接口",
    "owner_type": "tool",
    "owner_id": "api-autotest",
    "group_key": "connection",
    "value_type": "url",
    "sensitivity": "normal",
    "required": true,
    "default_value": null,
    "validation_schema": {},
    "apply_mode": "next_task",
    "editable": true,
    "sort_order": 120
  },
  {
    "id": "api-autotest.AUTH_TOKEN",
    "key": "AUTH_TOKEN",
    "display_name": "接口自动化 Access Token",
    "description": "接口自动化 Access Token",
    "owner_type": "tool",
    "owner_id": "api-autotest",
    "group_key": "credentials",
    "value_type": "secret",
    "sensitivity": "secret",
    "required": true,
    "default_value": null,
    "validation_schema": {},
    "apply_mode": "next_task",
    "editable": true,
    "sort_order": 130
  }
]
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 配置版本列表

`GET /api/v1/config/releases`

返回某工具在某环境下的配置发布版本（Release）。

**鉴权**: 会话

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `environment_id` | string | 是 | 环境标识，当前线上为 `dev` |
| `owner_type` | string | 是 | 配置归属类型，固定 `tool` |
| `owner_id` | string | 是 | 工具 id，如 `api-autotest`、`truthy-search` |

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/config/releases?environment_id=dev&owner_type=tool&owner_id=api-autotest' \
  -b cookies.txt
```

**响应**

真实响应示例（HTTP 200，已脱敏，线上当前共 1 条，此处截取前几条）：
```json
[
  {
    "id": "rel_f39c67b89b31439eb00116bfba75565f",
    "environment_id": "dev",
    "owner_type": "tool",
    "owner_id": "api-autotest",
    "version": 1,
    "revision": 2,
    "status": "active",
    "created_by": "usr_631a700e12734ae1a613a337e9124e8f",
    "published_by": "usr_631a700e12734ae1a613a337e9124e8f",
    "created_at": "2026-08-13T07:26:24.428373Z",
    "published_at": "2026-08-13T07:26:24.469672Z",
    "items": [
      {
        "definition_id": "api-autotest.ADMIN_LOGIN_API_URL",
        "value": "http****"
      },
      {
        "definition_id": "api-autotest.GATEWAY_API_URL",
        "value": "http****"
      },
      {
        "definition_id": "api-autotest.ADMIN_PASSWORD",
        "value": null
      },
      {
        "definition_id": "api-autotest.ADMIN_SESSION_TOKEN",
        "value": null
      },
      {
        "definition_id": "api-autotest.ADMIN_USERNAME",
        "value": null
      },
      {
        "definition_id": "api-autotest.AUTH_TOKEN",
        "value": null
      },
      {
        "definition_id": "api-autotest.DEVICE_ID",
        "value": null
      },
      {
        "definition_id": "api-autotest.EXPIRES_TIME",
        "value": null
      },
      {
        "definition_id": "api-autotest.REFRESH_EXPIRES_TIME",
        "value": null
      },
      {
        "definition_id": "api-autotest.REFRESH_TOKEN",
        "value": null
      },
      {
        "definition_id": "api-autotest.USER_ID",
        "value": null
      }
    ]
  }
]
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 创建配置版本

`POST /api/v1/config/releases`

基于当前激活版本创建一个新的草稿版本（revision=1 起步）。

**鉴权**: 会话 + CSRF

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `environment_id` | string | 是 | 正则约束 |
| `owner_type` | string | 是 | 正则约束 |
| `owner_id` | string (≤64) | 是 | 长度≥1、长度≤64 |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/config/releases' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt \
  -d '{
  "environment_id": "string",
  "owner_type": "string",
  "owner_id": "string"
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `environment_id` | string | 是 | - |
| `owner_type` | string | 是 | - |
| `owner_id` | string | 是 | - |
| `version` | integer | 是 | - |
| `revision` | integer | 是 | - |
| `status` | string | 是 | - |
| `created_by` | string | 是 | - |
| `published_by` | string | 是 | - |
| `created_at` | string (ISO8601 时间) | 是 | - |
| `published_at` | string (ISO8601 时间) | 是 | - |
| `items` | array<object> | 是 | - |

成功返回 HTTP 201，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "environment_id": "string",
  "owner_type": "string",
  "owner_id": "string",
  "version": 1,
  "revision": 1,
  "status": "string",
  "created_by": "string",
  "published_by": "string",
  "created_at": "2026-08-13T08:00:00Z",
  "published_at": "2026-08-13T08:00:00Z",
  "items": [
    {
      "definition_id": "string",
      "value": null
    }
  ]
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）。完整说明见文末错误码总表。


### 更新草稿配置项

`PUT /api/v1/config/releases/{release_id}/items`

更新草稿版本中的配置项（仅 normal 且 editable 的项；使用 revision 乐观锁，版本冲突返回 409）。

**鉴权**: 会话 + CSRF

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `release_id` | string | 配置版本 id（rel_ 前缀） |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `revision` | integer | 是 | ≥1.0 |
| `items` | array<object> | 是 | - |

**请求示例**

```bash
curl -X PUT 'http://170.106.159.147:41873/api/v1/config/releases/{release_id}/items' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt \
  -d '{
  "revision": 1,
  "items": [
    {
      "definition_id": "string",
      "value": null
    }
  ]
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `environment_id` | string | 是 | - |
| `owner_type` | string | 是 | - |
| `owner_id` | string | 是 | - |
| `version` | integer | 是 | - |
| `revision` | integer | 是 | - |
| `status` | string | 是 | - |
| `created_by` | string | 是 | - |
| `published_by` | string | 是 | - |
| `created_at` | string (ISO8601 时间) | 是 | - |
| `published_at` | string (ISO8601 时间) | 是 | - |
| `items` | array<object> | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "environment_id": "string",
  "owner_type": "string",
  "owner_id": "string",
  "version": 1,
  "revision": 1,
  "status": "string",
  "created_by": "string",
  "published_by": "string",
  "created_at": "2026-08-13T08:00:00Z",
  "published_at": "2026-08-13T08:00:00Z",
  "items": [
    {
      "definition_id": "string",
      "value": null
    }
  ]
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）；特征错误：`VERSION_CONFLICT`、`NOT_FOUND`、`CONFIG_VALIDATION_FAILED`。完整说明见文末错误码总表。


### 校验配置版本

`POST /api/v1/config/releases/{release_id}/validate`

校验草稿完整性（required 项必须齐全、Secret 已登记等），通过后方可发布。

**鉴权**: 会话 + CSRF

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `release_id` | string | 配置版本 id（rel_ 前缀） |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/config/releases/{release_id}/validate' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `message` | string | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "message": "string"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）；特征错误：`CONFIG_VALIDATION_FAILED`、`NOT_FOUND`。完整说明见文末错误码总表。


### 发布配置版本

`POST /api/v1/config/releases/{release_id}/publish`

发布草稿使其成为激活版本；发布时自动冻结所引用 Secret 的版本快照。

**鉴权**: 会话 + CSRF

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `release_id` | string | 配置版本 id（rel_ 前缀） |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/config/releases/{release_id}/publish' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `environment_id` | string | 是 | - |
| `owner_type` | string | 是 | - |
| `owner_id` | string | 是 | - |
| `version` | integer | 是 | - |
| `revision` | integer | 是 | - |
| `status` | string | 是 | - |
| `created_by` | string | 是 | - |
| `published_by` | string | 是 | - |
| `created_at` | string (ISO8601 时间) | 是 | - |
| `published_at` | string (ISO8601 时间) | 是 | - |
| `items` | array<object> | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "environment_id": "string",
  "owner_type": "string",
  "owner_id": "string",
  "version": 1,
  "revision": 1,
  "status": "string",
  "created_by": "string",
  "published_by": "string",
  "created_at": "2026-08-13T08:00:00Z",
  "published_at": "2026-08-13T08:00:00Z",
  "items": [
    {
      "definition_id": "string",
      "value": null
    }
  ]
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）；特征错误：`VERSION_CONFLICT`、`CONFIG_VALIDATION_FAILED`、`NOT_FOUND`。完整说明见文末错误码总表。


### 提升配置版本

`POST /api/v1/config/releases/{release_id}/promote`

将配置提升到目标环境（跨环境复制）。

**鉴权**: 会话 + CSRF

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `release_id` | string | 配置版本 id（rel_ 前缀） |

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `target_environment` | string | 是 | - |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/config/releases/{release_id}/promote' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `environment_id` | string | 是 | - |
| `owner_type` | string | 是 | - |
| `owner_id` | string | 是 | - |
| `version` | integer | 是 | - |
| `revision` | integer | 是 | - |
| `status` | string | 是 | - |
| `created_by` | string | 是 | - |
| `published_by` | string | 是 | - |
| `created_at` | string (ISO8601 时间) | 是 | - |
| `published_at` | string (ISO8601 时间) | 是 | - |
| `items` | array<object> | 是 | - |

成功返回 HTTP 201，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "environment_id": "string",
  "owner_type": "string",
  "owner_id": "string",
  "version": 1,
  "revision": 1,
  "status": "string",
  "created_by": "string",
  "published_by": "string",
  "created_at": "2026-08-13T08:00:00Z",
  "published_at": "2026-08-13T08:00:00Z",
  "items": [
    {
      "definition_id": "string",
      "value": null
    }
  ]
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）。完整说明见文末错误码总表。


### 回滚配置版本

`POST /api/v1/config/releases/{release_id}/rollback`

回滚到历史激活版本。

**鉴权**: 会话 + CSRF

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `release_id` | string | 配置版本 id（rel_ 前缀） |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/config/releases/{release_id}/rollback' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `environment_id` | string | 是 | - |
| `owner_type` | string | 是 | - |
| `owner_id` | string | 是 | - |
| `version` | integer | 是 | - |
| `revision` | integer | 是 | - |
| `status` | string | 是 | - |
| `created_by` | string | 是 | - |
| `published_by` | string | 是 | - |
| `created_at` | string (ISO8601 时间) | 是 | - |
| `published_at` | string (ISO8601 时间) | 是 | - |
| `items` | array<object> | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "environment_id": "string",
  "owner_type": "string",
  "owner_id": "string",
  "version": 1,
  "revision": 1,
  "status": "string",
  "created_by": "string",
  "published_by": "string",
  "created_at": "2026-08-13T08:00:00Z",
  "published_at": "2026-08-13T08:00:00Z",
  "items": [
    {
      "definition_id": "string",
      "value": null
    }
  ]
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）。完整说明见文末错误码总表。


### 模块：Secret 管理

### Secret 列表

`GET /api/v1/secrets`

列出某工具在某环境的 Secret（仅元数据，不含明文值）。

**鉴权**: 会话

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `environment_id` | string | 是 | 环境标识，当前线上为 `dev` |
| `owner_type` | string | 是 | 配置归属类型，固定 `tool` |
| `owner_id` | string | 是 | 工具 id，如 `api-autotest`、`truthy-search` |

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/secrets?environment_id=dev&owner_type=tool&owner_id=api-autotest' \
  -b cookies.txt
```

**响应**

真实响应示例（HTTP 200，已脱敏，线上当前共 9 条，此处截取前几条）：
```json
[
  {
    "id": "sec_dev_api-autotest_ADMIN_PASSWORD",
    "environment_id": "dev",
    "owner_type": "tool",
    "owner_id": "api-autotest",
    "definition_id": "api-autotest.ADMIN_PASSWORD",
    "configured": true,
    "status": "healthy",
    "version": 1,
    "expires_at": null,
    "updated_at": "2026-08-13T07:26:24.299595Z"
  },
  {
    "id": "sec_dev_api-autotest_ADMIN_SESSION_TOKEN",
    "environment_id": "dev",
    "owner_type": "tool",
    "owner_id": "api-autotest",
    "definition_id": "api-autotest.ADMIN_SESSION_TOKEN",
    "configured": true,
    "status": "healthy",
    "version": 2,
    "expires_at": null,
    "updated_at": "2026-08-13T07:26:31.278504Z"
  }
]
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 写入 Secret

`PUT /api/v1/secrets/{secret_id}`

新增或更新 Secret 值（每次写入生成新版本并激活）。secret_id 规则：`sec_{环境}_{定义 id 的 . 替换为 _}`，如 `sec_dev_api-autotest_AUTH_TOKEN`。

**鉴权**: 会话 + CSRF

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `secret_id` | string | Secret id，格式 `sec_{环境}_{定义 id 的 . 替换为 _}` |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `environment_id` | string | 是 | - |
| `owner_type` | string | 是 | 正则约束 |
| `owner_id` | string | 是 | - |
| `definition_id` | string | 是 | - |
| `value` | string (≤65536) | 是 | 长度≥1、长度≤65536 |
| `expires_at` | string (ISO8601 时间) | 否 | - |

**请求示例**

```bash
curl -X PUT 'http://170.106.159.147:41873/api/v1/secrets/{secret_id}' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt \
  -d '{
  "environment_id": "string",
  "owner_type": "string",
  "owner_id": "string",
  "definition_id": "string",
  "value": "string",
  "expires_at": "2026-08-13T08:00:00Z"
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `environment_id` | string | 是 | - |
| `owner_type` | string | 是 | - |
| `owner_id` | string | 是 | - |
| `definition_id` | string | 是 | - |
| `configured` | boolean | 是 | - |
| `status` | string | 是 | - |
| `version` | integer | 是 | - |
| `expires_at` | string (ISO8601 时间) | 是 | - |
| `updated_at` | string (ISO8601 时间) | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "environment_id": "string",
  "owner_type": "string",
  "owner_id": "string",
  "definition_id": "string",
  "configured": true,
  "status": "string",
  "version": 1,
  "expires_at": "2026-08-13T08:00:00Z",
  "updated_at": "2026-08-13T08:00:00Z"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）；特征错误：`CONFIG_VALIDATION_FAILED`、`VERSION_CONFLICT`、`SECRET_UNAVAILABLE`、`PERMISSION_DENIED`。完整说明见文末错误码总表。


### 模块：凭证管理

### 凭证列表

`GET /api/v1/credentials`

列出工具自动维护的会话凭证（gateway_session / admin_login），含健康状态与到期时间。

**鉴权**: 会话

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `environment_id` | string | 否 | 环境标识，当前线上为 `dev` |

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/credentials' \
  -b cookies.txt
```

**响应**

真实响应示例（HTTP 200，已脱敏，线上当前共 4 条，此处截取前几条）：
```json
[
  {
    "id": "cred_52d6dcf25da54dffbf2742962110ce1a",
    "tool_id": "api-autotest",
    "environment_id": "dev",
    "provider_type": "admin_login",
    "status": "healthy",
    "current_version": 1,
    "expires_at": "2026-08-13T19:26:31.212000Z",
    "refresh_expires_at": null,
    "last_error_code": null,
    "last_checked_at": "2026-08-13T07:26:31.283955Z"
  },
  {
    "id": "cred_c1357ded60564d5db538ec023c5bbcce",
    "tool_id": "api-autotest",
    "environment_id": "dev",
    "provider_type": "gateway_session",
    "status": "healthy",
    "current_version": 1,
    "expires_at": "2026-08-20T07:26:31.308000Z",
    "refresh_expires_at": "2027-02-09T07:26:31.308000Z",
    "last_error_code": null,
    "last_checked_at": "2026-08-13T07:26:31.336678Z"
  }
]
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 创建凭证

`POST /api/v1/credentials`

登记一个需由 credential-agent 自动验证/续期的凭证任务；同 tool+环境+provider_type 唯一，重复创建返回 409。

**鉴权**: 会话 + CSRF

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `tool_id` | string (≤64) | 是 | 长度≥1、长度≤64 |
| `environment_id` | string | 是 | 正则约束 |
| `provider_type` | string | 是 | 正则约束 |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/credentials' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt \
  -d '{
  "tool_id": "string",
  "environment_id": "string",
  "provider_type": "string"
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `tool_id` | string | 是 | - |
| `environment_id` | string | 是 | - |
| `provider_type` | string | 是 | - |
| `status` | string | 是 | - |
| `current_version` | integer | 是 | - |
| `expires_at` | string (ISO8601 时间) | 是 | - |
| `refresh_expires_at` | string (ISO8601 时间) | 是 | - |
| `last_error_code` | string | 是 | - |
| `last_checked_at` | string (ISO8601 时间) | 是 | - |

成功返回 HTTP 201，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "tool_id": "string",
  "environment_id": "string",
  "provider_type": "string",
  "status": "string",
  "current_version": 1,
  "expires_at": "2026-08-13T08:00:00Z",
  "refresh_expires_at": "2026-08-13T08:00:00Z",
  "last_error_code": "string",
  "last_checked_at": "2026-08-13T08:00:00Z"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）；特征错误：`VERSION_CONFLICT（凭证已存在）`、`CONFIG_VALIDATION_FAILED`。完整说明见文末错误码总表。


### 模块：审计

### 审计事件列表

`GET /api/v1/audit/events`

分页查询审计事件，支持按动作、资源、时间等过滤。

**鉴权**: 会话 + `platform.audit.view` 权限

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `page` | integer | 否 | 页码，从 1 开始 |
| `page_size` | integer | 否 | 每页条数 |
| `start_at` | string | 否 | - |
| `end_at` | string | 否 | - |
| `action` | string | 否 | 按动作过滤 |
| `tool_id` | string | 否 | 工具 id |
| `outcome` | string | 否 | 按结果过滤（success/failure） |

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/audit/events?page=1&page_size=20' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `items` | array<object> | 是 | - |
| `total` | integer | 是 | - |
| `page` | integer | 是 | - |
| `page_size` | integer | 是 | - |

真实响应示例（HTTP 200，已脱敏）：
```json
{
  "items": [
    {
      "id": "aud_d53164be01054c3aa67259971ef7181c",
      "occurred_at": "2026-08-13T08:20:58.457668Z",
      "actor_type": "user",
      "actor_id": "usr_631a700e12734ae1a613a337e9124e8f",
      "actor_snapshot": {
        "username": "admin",
        "display_name": "Administrator"
      },
      "action": "auth.login",
      "resource_type": "session",
      "resource_id": "ses_31d75fce8a9246888347d4da24233d4e",
      "tool_id": null,
      "environment_id": null,
      "outcome": "success",
      "error_code": null,
      "request_id": "req_7f6880a70556432dbbf51593e3234f93",
      "ip_address": "172.19.0.11",
      "before_json": null,
      "after_json": null,
      "metadata_json": {},
      "detail": ""
    },
    {
      "id": "aud_39c9eecf491344a49c59f3758c77851c",
      "occurred_at": "2026-08-13T08:10:11.398015Z",
      "actor_type": "anonymous",
      "actor_id": null,
      "actor_snapshot": {},
      "action": "auth.login",
      "resource_type": "session",
      "resource_id": null,
      "tool_id": null,
      "environment_id": null,
      "outcome": "failed",
      "error_code": "INVALID_CREDENTIALS",
      "request_id": "req_74818d1899954aa8ac4b2293e94f967a",
      "ip_address": "172.19.0.11",
      "before_json": null,
      "after_json": null,
      "metadata_json": {
        "username": "admin"
      },
      "detail": ""
    }
  ],
  "total": 67,
  "page": 1,
  "page_size": 2
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 审计事件详情

`GET /api/v1/audit/events/{event_id}`

查看单个审计事件的完整前后快照。

**鉴权**: 会话 + `platform.audit.view` 权限

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `event_id` | string | 审计事件 id |

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/audit/events/{event_id}' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `occurred_at` | string (ISO8601 时间) | 是 | - |
| `actor_type` | string | 是 | - |
| `actor_id` | string | 是 | - |
| `actor_snapshot` | object | 是 | - |
| `action` | string | 是 | - |
| `resource_type` | string | 是 | - |
| `resource_id` | string | 是 | - |
| `tool_id` | string | 是 | - |
| `environment_id` | string | 是 | - |
| `outcome` | string | 是 | - |
| `error_code` | string | 是 | - |
| `request_id` | string | 是 | - |
| `ip_address` | string | 是 | - |
| `before_json` | object | 是 | - |
| `after_json` | object | 是 | - |
| `metadata_json` | object | 是 | - |
| `detail` | string | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "occurred_at": "2026-08-13T08:00:00Z",
  "actor_type": "string",
  "actor_id": "string",
  "actor_snapshot": {},
  "action": "string",
  "resource_type": "string",
  "resource_id": "string",
  "tool_id": "string",
  "environment_id": "string",
  "outcome": "string",
  "error_code": "string",
  "request_id": "string",
  "ip_address": "string",
  "before_json": {},
  "after_json": {},
  "metadata_json": {},
  "detail": "string"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 导出审计事件

`POST /api/v1/audit/exports`

导出审计事件（最多 100,000 行）。

**鉴权**: 会话 + CSRF + `platform.audit.export` 权限

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `start_at` | string | 是 | - |
| `end_at` | string | 是 | - |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/audit/exports?start_at=2026-08-01T00:00:00Z&end_at=2026-08-13T00:00:00Z' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt
```

**响应**

成功返回 HTTP 200，响应体为 **CSV 文件下载**（`Content-Type: text/csv`，文件名 `audit-events.csv`），列为：id、occurred_at、actor_type、actor_id、action、resource_type、resource_id、tool_id、environment_id、outcome、error_code、request_id。

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）；特征错误：`AUDIT_EXPORT_TOO_LARGE`、`PERMISSION_DENIED`。完整说明见文末错误码总表。


### 模块：用户与权限管理

### 用户列表

`GET /api/v1/admin/users`

列出平台用户。

**鉴权**: 会话 + `platform.user.manage` 权限

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/admin/users' \
  -b cookies.txt
```

**响应**

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
[
  {
    "id": "string",
    "username": "string",
    "display_name": "string",
    "status": "string",
    "must_change_password": true,
    "role_ids": [
      "string"
    ],
    "last_login_at": "2026-08-13T08:00:00Z",
    "created_at": "2026-08-13T08:00:00Z"
  }
]
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 创建用户

`POST /api/v1/admin/users`

创建用户并绑定角色。

**鉴权**: 会话 + CSRF + `platform.user.manage` 权限

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `username` | string (≤128) | 是 | 长度≥3、长度≤128 |
| `display_name` | string (≤128) | 是 | 长度≥1、长度≤128 |
| `password` | string (≤256) | 是 | 长度≥12、长度≤256 |
| `role_ids` | array<string> | 否 | - |
| `must_change_password` | boolean | 否 | - |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/admin/users' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt \
  -d '{
  "username": "string",
  "display_name": "string",
  "password": "string",
  "role_ids": [
    "string"
  ],
  "must_change_password": true
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `username` | string | 是 | - |
| `display_name` | string | 是 | - |
| `status` | string | 是 | - |
| `must_change_password` | boolean | 是 | - |
| `role_ids` | array<string> | 是 | - |
| `last_login_at` | string (ISO8601 时间) | 是 | - |
| `created_at` | string (ISO8601 时间) | 是 | - |

成功返回 HTTP 201，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "username": "string",
  "display_name": "string",
  "status": "string",
  "must_change_password": true,
  "role_ids": [
    "string"
  ],
  "last_login_at": "2026-08-13T08:00:00Z",
  "created_at": "2026-08-13T08:00:00Z"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）；特征错误：`USERNAME_EXISTS`、`VALIDATION_ERROR（角色不存在）`。完整说明见文末错误码总表。


### 用户详情

`GET /api/v1/admin/users/{user_id}`

查看用户及其角色。

**鉴权**: 会话 + `platform.user.manage` 权限

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `user_id` | string | 用户 id（usr_ 前缀） |

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/admin/users/{user_id}' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `username` | string | 是 | - |
| `display_name` | string | 是 | - |
| `status` | string | 是 | - |
| `must_change_password` | boolean | 是 | - |
| `role_ids` | array<string> | 是 | - |
| `last_login_at` | string (ISO8601 时间) | 是 | - |
| `created_at` | string (ISO8601 时间) | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "username": "string",
  "display_name": "string",
  "status": "string",
  "must_change_password": true,
  "role_ids": [
    "string"
  ],
  "last_login_at": "2026-08-13T08:00:00Z",
  "created_at": "2026-08-13T08:00:00Z"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 更新用户

`PATCH /api/v1/admin/users/{user_id}`

修改显示名、状态、角色；禁用用户会同时注销其所有会话。

**鉴权**: 会话 + CSRF + `platform.user.manage` 权限

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `user_id` | string | 用户 id（usr_ 前缀） |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `display_name` | string (≤128) | 否 | 长度≥1、长度≤128 |
| `status` | string | 否 | 正则约束 |
| `role_ids` | array<string> | 否 | - |

**请求示例**

```bash
curl -X PATCH 'http://170.106.159.147:41873/api/v1/admin/users/{user_id}' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt \
  -d '{
  "display_name": "string",
  "status": "string",
  "role_ids": [
    "string"
  ]
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `username` | string | 是 | - |
| `display_name` | string | 是 | - |
| `status` | string | 是 | - |
| `must_change_password` | boolean | 是 | - |
| `role_ids` | array<string> | 是 | - |
| `last_login_at` | string (ISO8601 时间) | 是 | - |
| `created_at` | string (ISO8601 时间) | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "username": "string",
  "display_name": "string",
  "status": "string",
  "must_change_password": true,
  "role_ids": [
    "string"
  ],
  "last_login_at": "2026-08-13T08:00:00Z",
  "created_at": "2026-08-13T08:00:00Z"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）。完整说明见文末错误码总表。


### 重置用户密码

`POST /api/v1/admin/users/{user_id}/reset-password`

重置密码并强制下次登录修改，同时注销该用户所有会话。

**鉴权**: 会话 + CSRF + `platform.user.manage` 权限

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `user_id` | string | 用户 id（usr_ 前缀） |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `new_password` | string (≤256) | 是 | 长度≥12、长度≤256 |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/admin/users/{user_id}/reset-password' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt \
  -d '{
  "new_password": "string"
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `message` | string | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "message": "string"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）。完整说明见文末错误码总表。


### 注销用户全部会话

`DELETE /api/v1/admin/users/{user_id}/sessions`

强制某用户全部会话失效。

**鉴权**: 会话 + CSRF + `platform.user.manage` 权限

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `user_id` | string | 用户 id（usr_ 前缀） |

**请求示例**

```bash
curl -X DELETE 'http://170.106.159.147:41873/api/v1/admin/users/{user_id}/sessions' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `message` | string | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "message": "string"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）。完整说明见文末错误码总表。


### 角色列表

`GET /api/v1/admin/roles`

列出角色及其授权明细。

**鉴权**: 会话 + `platform.role.manage` 权限

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/admin/roles' \
  -b cookies.txt
```

**响应**

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
[
  {
    "id": "string",
    "name": "string",
    "description": "string",
    "is_builtin": true,
    "grants": [
      {
        "permission_code": "string",
        "resource_type": "string",
        "resource_id": "string"
      }
    ]
  }
]
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 创建角色

`POST /api/v1/admin/roles`

创建自定义角色并配置授权。

**鉴权**: 会话 + CSRF + `platform.role.manage` 权限

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `name` | string (≤128) | 是 | 长度≥1、长度≤128 |
| `description` | string (≤1000) | 否 | 长度≤1000 |
| `grants` | array<object> | 否 | - |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/admin/roles' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt \
  -d '{
  "name": "string",
  "description": "string",
  "grants": [
    {
      "permission_code": "string",
      "resource_type": "string",
      "resource_id": "string"
    }
  ]
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `name` | string | 是 | - |
| `description` | string | 是 | - |
| `is_builtin` | boolean | 是 | - |
| `grants` | array<object> | 是 | - |

成功返回 HTTP 201，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "name": "string",
  "description": "string",
  "is_builtin": true,
  "grants": [
    {
      "permission_code": "string",
      "resource_type": "string",
      "resource_id": "string"
    }
  ]
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）；特征错误：`ROLE_EXISTS`、`VALIDATION_ERROR`。完整说明见文末错误码总表。


### 角色详情

`GET /api/v1/admin/roles/{role_id}`

查看角色详情。

**鉴权**: 会话 + `platform.role.manage` 权限

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `role_id` | string | 角色 id（role_ 前缀） |

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/admin/roles/{role_id}' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `name` | string | 是 | - |
| `description` | string | 是 | - |
| `is_builtin` | boolean | 是 | - |
| `grants` | array<object> | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "name": "string",
  "description": "string",
  "is_builtin": true,
  "grants": [
    {
      "permission_code": "string",
      "resource_type": "string",
      "resource_id": "string"
    }
  ]
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 更新角色

`PATCH /api/v1/admin/roles/{role_id}`

修改角色描述与授权（内置角色不可改名）。

**鉴权**: 会话 + CSRF + `platform.role.manage` 权限

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `role_id` | string | 角色 id（role_ 前缀） |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `name` | string (≤128) | 否 | 长度≥1、长度≤128 |
| `description` | string (≤1000) | 否 | 长度≤1000 |
| `grants` | array<object> | 否 | - |

**请求示例**

```bash
curl -X PATCH 'http://170.106.159.147:41873/api/v1/admin/roles/{role_id}' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt \
  -d '{
  "name": "string",
  "description": "string",
  "grants": [
    {
      "permission_code": "string",
      "resource_type": "string",
      "resource_id": "string"
    }
  ]
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `id` | string | 是 | - |
| `name` | string | 是 | - |
| `description` | string | 是 | - |
| `is_builtin` | boolean | 是 | - |
| `grants` | array<object> | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "id": "string",
  "name": "string",
  "description": "string",
  "is_builtin": true,
  "grants": [
    {
      "permission_code": "string",
      "resource_type": "string",
      "resource_id": "string"
    }
  ]
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）。完整说明见文末错误码总表。


### 删除角色

`DELETE /api/v1/admin/roles/{role_id}`

删除自定义角色（内置角色与仍被使用的角色不可删除）。

**鉴权**: 会话 + CSRF + `platform.role.manage` 权限

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `role_id` | string | 角色 id（role_ 前缀） |

**请求示例**

```bash
curl -X DELETE 'http://170.106.159.147:41873/api/v1/admin/roles/{role_id}' \
  -H 'Content-Type: application/json' \
  -H 'X-CSRF-Token: <tp_csrf Cookie 值>' \
  -b cookies.txt
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `message` | string | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "message": "string"
}
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）、`403 CSRF_INVALID`（CSRF 校验失败）；特征错误：`ROLE_IN_USE`、`VALIDATION_ERROR（内置角色）`。完整说明见文末错误码总表。


### 权限定义列表

`GET /api/v1/admin/permissions`

返回平台全部权限定义（权限码、名称、资源类型）。

**鉴权**: 会话 + `platform.user.manage` 权限

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/admin/permissions' \
  -b cookies.txt
```

**响应**

真实响应示例（HTTP 200，已脱敏，线上当前共 17 条，此处截取前几条）：
```json
[
  {
    "code": "platform.audit.export",
    "name": "导出审计",
    "description": "导出审计",
    "resource_type": "platform"
  },
  {
    "code": "platform.audit.view",
    "name": "查看审计",
    "description": "查看审计",
    "resource_type": "platform"
  },
  {
    "code": "platform.config.manage",
    "name": "平台配置管理",
    "description": "平台配置管理",
    "resource_type": "platform"
  }
]
```

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 模块：健康检查

### 存活探针

`GET /api/v1/health/live`

进程存活检查，供容器/网关探活。

**鉴权**: 无需鉴权

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/health/live'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `service` | string | 否 | - |
| `status` | `ok` / `ready` | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "service": "string",
  "status": "ok"
}
```

**可能的业务错误**
无会话/CSRF 类通用错误。完整说明见文末错误码总表。


### 就绪探针

`GET /api/v1/health/ready`

就绪检查（含数据库连通性）。

**鉴权**: 无需鉴权

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/health/ready'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `service` | string | 否 | - |
| `status` | `ok` / `ready` | 是 | - |

真实响应示例（HTTP 200，已脱敏）：
```json
{
  "service": "platform-api",
  "status": "ready"
}
```

**可能的业务错误**
无会话/CSRF 类通用错误。完整说明见文末错误码总表。


### 模块：内部接口（工具调用，外网不可达）

### 网关鉴权回调

`GET /api/v1/internal/authorize`

供 Nginx auth_request 校验会话与工具路径权限，成功时 204 并回传用户身份 Header。**仅供网关调用，外网不可达。**

**鉴权**: 会话（网关内部）

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/internal/authorize' \
  -b cookies.txt
```

**响应**

成功返回 HTTP 204，无响应体。

**可能的业务错误**
通用：`401 AUTH_REQUIRED`（未登录/会话失效）。完整说明见文末错误码总表。


### 工具运行时配置

`GET /api/v1/internal/tools/{tool_id}/runtime-config`

工具拉取当前激活配置：normal 项直接给值，secret 项给冻结版本引用或解密值（按能力），并附带凭证项。**外网不可达，仅容器网络内访问。**

**鉴权**: 工具 Token（Bearer）

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `tool_id` | string | 工具 id |

**查询参数**

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `include_secrets` | boolean | 否 | - |

**请求示例**

```bash
curl 'http://170.106.159.147:41873/api/v1/internal/tools/{tool_id}/runtime-config' \
  -H 'Authorization: Bearer <工具 Client Token>'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `tool_id` | string | 是 | - |
| `environment` | string | 是 | - |
| `release_id` | string | 是 | - |
| `release_version` | integer | 是 | - |
| `normal` | object | 是 | - |
| `secrets` | object | 是 | - |
| `credential_metadata` | object | 是 | - |
| `configured_secret_keys` | array<string> | 否 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "tool_id": "string",
  "environment": "string",
  "release_id": "string",
  "release_version": 1,
  "normal": {},
  "secrets": {},
  "credential_metadata": {},
  "configured_secret_keys": [
    "string"
  ]
}
```

**可能的业务错误**
通用：`401 TOOL_CLIENT_UNAUTHORIZED`（工具身份无效）；特征错误：`TOOL_CLIENT_FORBIDDEN`。完整说明见文末错误码总表。


### 配置确认

`POST /api/v1/internal/tools/{tool_id}/config-ack`

工具确认已应用某版本配置。

**鉴权**: 工具 Token（Bearer）+ config 能力

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `tool_id` | string | 工具 id |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `release_id` | string | 是 | - |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/internal/tools/{tool_id}/config-ack' \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <工具 Client Token>' \
  -d '{
  "release_id": "string"
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `message` | string | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "message": "string"
}
```

**可能的业务错误**
通用：`401 TOOL_CLIENT_UNAUTHORIZED`（工具身份无效）。完整说明见文末错误码总表。


### 凭证状态上报

`POST /api/v1/internal/tools/{tool_id}/credential-status`

工具上报凭证验证/刷新结果。

**鉴权**: 工具 Token（Bearer）+ credential 能力

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `tool_id` | string | 工具 id |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `provider_type` | string | 是 | - |
| `status` | string | 是 | - |
| `expires_at` | string | 否 | - |
| `error_code` | string | 否 | - |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/internal/tools/{tool_id}/credential-status' \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <工具 Client Token>' \
  -d '{
  "provider_type": "string",
  "status": "string",
  "expires_at": "string",
  "error_code": "string"
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `message` | string | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "message": "string"
}
```

**可能的业务错误**
通用：`401 TOOL_CLIENT_UNAUTHORIZED`（工具身份无效）。完整说明见文末错误码总表。


### 写回凭证会话

`PUT /api/v1/internal/tools/{tool_id}/credentials/{credential_id}/session`

工具写回新的会话凭证内容（含过期时间，支持毫秒时间戳或 ISO 时间）。

**鉴权**: 工具 Token（Bearer）+ credential 能力

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `tool_id` | string | 工具 id |
| `credential_id` | string | 凭证 id（cred_ 前缀） |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `expected_version` | integer | 是 | ≥0.0 |
| `values` | object | 是 | - |

**请求示例**

```bash
curl -X PUT 'http://170.106.159.147:41873/api/v1/internal/tools/{tool_id}/credentials/{credential_id}/session' \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <工具 Client Token>' \
  -d '{
  "expected_version": 1,
  "values": {}
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `message` | string | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "message": "string"
}
```

**可能的业务错误**
通用：`401 TOOL_CLIENT_UNAUTHORIZED`（工具身份无效）；特征错误：`VALIDATION_ERROR（过期时间格式）`、`VERSION_CONFLICT`。完整说明见文末错误码总表。


### 上报审计事件

`POST /api/v1/internal/tools/{tool_id}/audit-events`

工具上报自身审计事件到平台统一审计流。

**鉴权**: 工具 Token（Bearer）+ audit 能力

**路径参数**

| 参数 | 类型 | 说明 |
| --- | --- | --- |
| `tool_id` | string | 工具 id |

**请求体**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `event_id` | string (≤64) | 是 | 长度≥8、长度≤64 |
| `action` | string (≤128) | 是 | 长度≥3、长度≤128 |
| `resource_type` | string (≤64) | 是 | 长度≥1、长度≤64 |
| `resource_id` | string (≤128) | 否 | 长度≤128 |
| `outcome` | string | 是 | 正则约束 |
| `error_code` | string (≤128) | 否 | 长度≤128 |
| `actor_user_id` | string (≤64) | 否 | 长度≤64 |
| `actor_username` | string (≤128) | 否 | 长度≤128 |
| `metadata` | object | 否 | - |

**请求示例**

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/internal/tools/{tool_id}/audit-events' \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer <工具 Client Token>' \
  -d '{
  "event_id": "string",
  "action": "string",
  "resource_type": "string",
  "resource_id": "string",
  "outcome": "string",
  "error_code": "string",
  "actor_user_id": "string",
  "actor_username": "string",
  "metadata": {}
}'
```

**响应**

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `message` | string | 是 | - |

成功返回 HTTP 200，响应示例（依据数据结构生成）：
```json
{
  "message": "string"
}
```

**可能的业务错误**
通用：`401 TOOL_CLIENT_UNAUTHORIZED`（工具身份无效）。完整说明见文末错误码总表。


---

## 4. 典型流程示例

以下为线上实际验证过的流程（敏感值已脱敏）。

### 4.1 登录并获取当前用户

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/auth/login' \
  -H 'Content-Type: application/json' \
  -c cookies.txt \
  -d '{"username": "admin", "password": "********"}'
```

响应（HTTP 200，Set-Cookie 中同时下发 `tp_session` 与 `tp_csrf`）：
```json
{
  "user": {
    "id": "usr_631a700e12734ae1a613a337e9124e8f",
    "username": "admin",
    "display_name": "Administrator",
    "status": "active",
    "must_change_password": false
  },
  "roles": [
    "role_platform_admin"
  ],
  "platform_permissions": [
    "platform.audit.export",
    "platform.audit.view",
    "platform.config.manage",
    "platform.role.manage",
    "platform.secret.manage",
    "platform.user.manage"
  ],
  "tool_permissions": {
    "trackevents": [
      "api-test-agent.case.review",
      "api-test-agent.contract.review",
      "api-test-agent.defect.create",
      "api-test-agent.execute",
      "task.cancel",
      "task.view.all",
      "tool.config.manage",
      "tool.execute",
      "tool.result.view",
      "tool.secret.manage",
      "tool.view"
    ],
    "...": "其余 5 个工具的授权结构相同，已省略"
  },
  "session_expires_at": "2026-08-14T08:20:58.566478Z"
}
```

### 4.2 写入 Secret（以 api-autotest 的 AUTH_TOKEN 为例）

secret_id 生成规则：`sec_` + 环境 + `_` + 定义 id（`.` 替换为 `_`），即 `sec_dev_api-autotest_AUTH_TOKEN`。

```bash
curl -X PUT 'http://170.106.159.147:41873/api/v1/secrets/sec_dev_api-autotest_AUTH_TOKEN' \
  -H 'Content-Type: application/json' \
  -H "X-CSRF-Token: $CSRF" \
  -b cookies.txt \
  -d '{
    "environment_id": "dev",
    "owner_type": "tool",
    "owner_id": "api-autotest",
    "definition_id": "api-autotest.AUTH_TOKEN",
    "value": "<真实密钥值>"
  }'
```

成功返回 HTTP 200，Secret 新版本立即激活；值只保存密文，列表接口不返回明文。

### 4.3 配置发布流程（创建草稿 → 改配置 → 校验 → 发布）

```bash
# 1) 创建草稿（复制当前激活版本）
curl -X POST 'http://170.106.159.147:41873/api/v1/config/releases' \
  -H 'Content-Type: application/json' -H "X-CSRF-Token: $CSRF" -b cookies.txt \
  -d '{"environment_id": "dev", "owner_type": "tool", "owner_id": "api-autotest"}'
# => 响应含 release_id 与 revision

# 2) 更新草稿配置项（revision 必须与服务端一致，否则 409 VERSION_CONFLICT）
curl -X PUT 'http://170.106.159.147:41873/api/v1/config/releases/<release_id>/items' \
  -H 'Content-Type: application/json' -H "X-CSRF-Token: $CSRF" -b cookies.txt \
  -d '{"revision": 1, "items": [{"definition_id": "api-autotest.GATEWAY_API_URL", "value": "https://gateway.example.com"}]}'

# 3) 校验（required 项齐全、Secret 已登记）
curl -X POST 'http://170.106.159.147:41873/api/v1/config/releases/<release_id>/validate' \
  -H "X-CSRF-Token: $CSRF" -b cookies.txt

# 4) 发布（成为激活版本，同时冻结引用的 Secret 版本）
curl -X POST 'http://170.106.159.147:41873/api/v1/config/releases/<release_id>/publish' \
  -H "X-CSRF-Token: $CSRF" -b cookies.txt
```

### 4.4 创建凭证（由后台代理自动验证/续期）

```bash
curl -X POST 'http://170.106.159.147:41873/api/v1/credentials' \
  -H 'Content-Type: application/json' -H "X-CSRF-Token: $CSRF" -b cookies.txt \
  -d '{"tool_id": "api-autotest", "environment_id": "dev", "provider_type": "gateway_session"}'
```

线上当前凭证状态示例（截取）：
```json
[
  {
    "id": "cred_52d6dcf25da54dffbf2742962110ce1a",
    "tool_id": "api-autotest",
    "environment_id": "dev",
    "provider_type": "admin_login",
    "status": "healthy",
    "current_version": 1,
    "expires_at": "2026-08-13T19:26:31.212000Z",
    "refresh_expires_at": null,
    "last_error_code": null,
    "last_checked_at": "2026-08-13T07:26:31.283955Z"
  },
  {
    "id": "cred_c1357ded60564d5db538ec023c5bbcce",
    "tool_id": "api-autotest",
    "environment_id": "dev",
    "provider_type": "gateway_session",
    "status": "healthy",
    "current_version": 1,
    "expires_at": "2026-08-20T07:26:31.308000Z",
    "refresh_expires_at": "2027-02-09T07:26:31.308000Z",
    "last_error_code": null,
    "last_checked_at": "2026-08-13T07:26:31.336678Z"
  }
]
```

### 4.5 工具拉取运行时配置（容器网络内）

```bash
curl 'http://170.106.159.147:41873/api/v1/internal/tools/api-autotest/runtime-config' \
  -H 'Authorization: Bearer <工具 Client Token>'
```

响应结构（normal 项直接给值；secret 项给版本引用，由工具在能力允许时解密；含凭证项）：

```json
{
  "tool_id": "string",
  "environment": "string",
  "release_id": "string",
  "release_version": 1,
  "normal": {},
  "secrets": {},
  "credential_metadata": {},
  "configured_secret_keys": [
    "string"
  ]
}
```

---

## 5. 错误码总表

| HTTP 状态 | code | 提示信息 | 触发场景与处理建议 |
| --- | --- | --- | --- |
| 400 | `CURRENT_PASSWORD_INVALID` | 当前密码不正确 | 修改密码时当前密码校验失败 |
| 401 | `AUTH_REQUIRED` | 请先登录 | 访问需登录接口时会话缺失/失效 |
| 401 | `INVALID_CREDENTIALS` | 用户名或密码错误 | 登录失败 |
| 401 | `TOOL_CLIENT_UNAUTHORIZED` | 工具身份无效 | 内部接口 Bearer Token 缺失或无效 |
| 403 | `BOOTSTRAP_DENIED` | 初始化凭据无效 | setup 的 bootstrap_token 不正确 |
| 403 | `CSRF_INVALID` | 请求安全校验失败 | 写操作缺少 `X-CSRF-Token` 请求头或其与 `tp_csrf` Cookie 不一致 |
| 403 | `PERMISSION_DENIED` | 无权执行此操作 | 当前用户缺少对应权限（工具访问、审计、用户/角色管理等） |
| 403 | `TOOL_CLIENT_FORBIDDEN` | 工具身份无权执行此操作 | 工具 Token 有效但缺少所需能力（capabilities）或作用域不匹配 |
| 404 | `NOT_FOUND` | 资源不存在 | 用户/角色/工具/会话/凭证/配置版本/审计事件不存在；也包含路径不存在 |
| 409 | `USERNAME_EXISTS` | 用户名已存在 | 创建用户或 setup 时用户名重复 |
| 409 | `ROLE_EXISTS` | 角色名称已存在 | 创建角色时名称重复 |
| 409 | `ROLE_IN_USE` | 角色仍被用户使用 | 删除角色前需先解除用户绑定 |
| 409 | `VERSION_CONFLICT` | 版本冲突 | 草稿 revision 过期、凭证并发更新、Secret 作用域冲突、重复创建凭证等 |
| 422 | `VALIDATION_ERROR` | 请求参数不正确 | 参数缺失/格式错误（统一提示，具体见字段说明） |
| 422 | `CONFIG_VALIDATION_FAILED` | 配置校验失败 | Secret 定义无效、作用域不匹配、凭证字段未登记等 |
| 422 | `AUDIT_EXPORT_TOO_LARGE` | 审计导出最多支持 100,000 行 | 缩小导出时间范围 |
| 423 | `ACCOUNT_LOCKED` | 登录尝试过多，请稍后再试 | 15 分钟内连续失败 5 次，锁定 15 分钟 |
| 503 | `SECRET_UNAVAILABLE` | Secret 服务暂时不可用 | Secret 主密钥文件缺失或损坏 |
| 503 | `SERVICE_UNAVAILABLE` | 服务暂时不可用 | 依赖服务不可用 |
| 503 | `DATABASE_UNAVAILABLE` | 平台数据库暂时不可用 | 数据库异常 |
| 500 | `INTERNAL_ERROR` | 平台服务内部错误 | 未预期异常，请联系管理员并提供 request_id |

补充说明：

- 请求参数缺失/格式错误统一返回 `422 VALIDATION_ERROR`（请求参数不正确），请对照接口字段表检查必填项与格式；业务校验失败则返回 `CONFIG_VALIDATION_FAILED`、`AUDIT_EXPORT_TOO_LARGE` 等具体错误码；
- 同一接口的具体错误以上文「可能的业务错误」为准；
- 排障时请携带响应中的 `request_id`（或响应头 `X-Request-ID`）联系平台管理员查日志。
