# 测试开发平台 · 环境信息手册

> **保密提示**：本文档包含服务器、数据库及第三方服务的真实凭证，仅限项目组成员内部使用，请勿外传或提交到代码仓库。
> **更新日期**：2026-08-13

平台共两套运行环境：**本机开发环境**（Mac）与**公司服务器环境**。两套环境跑同一份代码（`docker-compose.yml`），通过 `.env` 与密钥目录区分配置。

---

## 一、总览

| 项目 | 本机开发环境 | 公司服务器环境 |
|------|-------------|---------------|
| 访问地址 | http://127.0.0.1:8080/ | http://170.106.159.147:41873/ |
| 代码路径 | `/Users/admin/Testproject` | `~/Testproject`（ubuntu 用户家目录） |
| Compose 目录 | `test-platform/` | `test-platform/` |
| 运行环境标识 | `PLATFORM_RUNTIME_ENV=dev` | `dev`（域名/证书到位后切 prod） |
| 平台数据库 | PostgreSQL 容器（见下文） | PostgreSQL 容器（见下文） |

代码同步方式（**唯一推荐流程**）：本机 `git push origin dev` → 服务器 `git pull` → `docker compose up -d --build`。
Git 仓库：`https://github.com/Lnyy-CZJ/Testproject.git`，分支 `dev`。

---

## 二、公司服务器环境（170.106.159.147）

### 2.1 登录与访问

| 用途 | 地址 / 账号 | 密码 / 说明 |
|------|------------|------------|
| SSH 登录 | `ubuntu@170.106.159.147`（端口 22） | `s*Fx8EEeykUkr%HL`（已另配 SSH 密钥免密） |
| 平台 Web | http://170.106.159.147:41873/ | 管理员账号 `admin`，密码 `05db8f3084c9c03140c19653fbbb8864` |

**防火墙白名单（重要）**：服务器只对白名单 IP 开放 TCP 22 与 41873。当前已放行的我方出口 IP 为 `43.173.72.75`。若换网络后连不上 SSH 或打不开网页，找**振峰**把新出口 IP 加白名单。

### 2.2 部署与更新

```bash
ssh ubuntu@170.106.159.147
cd ~/Testproject && git pull origin dev
cd test-platform && docker compose up -d --build
```

服务器 `test-platform/.env` 关键项：

| 变量 | 值 | 说明 |
|------|-----|------|
| `PLATFORM_PORT` | `41873` | 对外 Web 端口 |
| `POSTGRES_DB` / `POSTGRES_USER` / `POSTGRES_PASSWORD` | `test_platform` / `platform` / `74521fb8d344aeb53dce64947e797add` | 平台数据库 |
| `PLATFORM_RUNTIME_ENV` | `dev` | 环境标识 |
| `APP_PUBLIC_URL` | `http://170.106.159.147:41873` | 对外地址 |
| `SEARCH_CONFIG_SOURCE` / `API_AUTOTEST_CONFIG_SOURCE` / `API_AUTOTEST_SESSION_PROVIDER` | `platform` | 工具凭证走平台统一管理 |
| `LOG_EXPORT_HOST_DIR` | `/home/ubuntu/Testproject/log-exports` | 日志导出目录 |

### 2.3 平台数据库（PostgreSQL）

- 容器：`platform-db`（postgres:17-alpine），**仅容器内网可达，未对宿主机/外网暴露端口**。
- 连接方式（服务器上）：`docker compose exec platform-db psql -U platform -d test_platform`
- 数据卷：`test-platform-dev-db-data`（当前在用的库）。
- 旧卷 `platform-db-data`：早期 tar 包部署时期的旧数据，仅作留存，未挂载。

### 2.4 密钥目录 `test-platform/.runtime-secrets/`

| 文件 | 用途 |
|------|------|
| `initial-admin-password` | 平台 admin 密码（与 2.1 一致） |
| `dev-kek.json` / `prod-kek.json` | 平台 Secret 加密主密钥，**泄露即所有凭证泄露，严禁外传** |
| `*-client-token`（平铺） | 各工具容器连接平台的身份 token |
| `dev/bootstrap-token` | 空库初始化 `/setup` 用引导 token |
| `dev/*-client-token` | dev 环境工具身份 token |

业务凭证（truthy-search / api-autotest 的 Access Token、Admin 账密等）**加密存在平台数据库**，在平台页面 `/settings/secrets`、`/settings/credentials` 管理，不以明文文件存放。

---

## 三、本机开发环境（Mac）

| 用途 | 地址 / 账号 | 密码 / 说明 |
|------|------------|------------|
| 平台 Web | http://127.0.0.1:8080/ | 管理员账号 `admin`，密码 `CnvfqGj5iuuwu8tLRCugGwD6wa/KM/v5FvmEsE3h/+Z44yVl` |

- 启动/重建：`cd /Users/admin/Testproject/test-platform && docker compose up -d --build`
- 平台数据库：容器 `platform-db`；库 `test_platform`、用户 `platform`、密码为 compose 默认值 `platform_dev_password`（本机 `.env` 未覆盖）；连接：`docker compose exec platform-db psql -U platform -d test_platform`
- 数据卷：当前在用 `test-platform-dev-db-data`；旧数据在 `platform-db-data`。
- 密钥目录：`test-platform/.runtime-secrets/`，结构与服务器相同。
- 注意：若用新 compose 重建本机库，会得到全新空库，需用 `POST /api/v1/setup` 重新初始化管理员（bootstrap token 在 `.runtime-secrets/dev/bootstrap-token`）。

---

## 四、数据库相关说明（避免混淆）

1. **平台自身数据库 = PostgreSQL**，两套环境都已随 compose 自带，**无需另行部署**。
2. 平台 Secret 页面里 api-test-agent 名下的 `DB_HOST / DB_PORT / DB_USER / DB_PASSWORD / DB_NAME` 是**遗留 MySQL 配置**，仅在开启"数据库持久化"（`DATABASE_PERSIST_ENABLED`）时生效，**默认关闭**。本机与服务器均未配置、也未运行 MySQL，页面显示 missing 属正常状态。

---

## 五、第三方服务凭证摘要

| 服务 | 地址 | 凭证 |
|------|------|------|
| 阿里云百炼（**主用 LLM**） | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `DASHSCOPE_API_KEY=sk-b2cb178250c0453bbb3644332a3610bb`（平台内以 `LLM_API_KEY` 存储） |
| 硅基流动 | `https://api.siliconflow.cn/v1` | `sk-airrhnpcnvbdgkqfrstfxvfswyutrdmbnbcdacwzvvpmpvws` |
| DeepSeek | `https://api.deepseek.com` | `sk-24113d3b3792436095ec0604ef7d87e5` |
| Xiaomi MIMO | `https://api.xiaomimimo.com/v1` | `sk-cob1neiuzjlu7r9uuisfibvqoiio9ih98l6usdehuo7auaqv` |
| 检索业务 Gateway | `http://gateway.spark-jam.top/gateway/invoke` | 见 `两个项目的env具体数据.rtf` / `Truthy_Search/.env` |
| 检索 Admin 采集 | `http://admin-staging.spark-jam.top/admin/invoke` | 账号 `zhaojian` / `123123` |

完整业务 env（含 Access/Refresh Token、设备标识等）见：
- `两个项目的env具体数据.rtf`（项目根目录）
- `Truthy_Search/.env`、`Truthy_ApiAutoTest2/.env`

---

## 六、待办提醒

- 域名与 TLS 证书到位后，服务器需从 dev 配置切换为 prod（改 `PLATFORM_RUNTIME_ENV`、nginx 加证书、端口改 80/443）。
- 平台 admin 密码建议首次使用后在 `/settings` 修改；修改后同步更新本文档。
