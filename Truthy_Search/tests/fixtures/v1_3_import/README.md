# v1.3 阶段2导入夹具

`import_sources.xlsx` 是使用工作区表格运行时生成的脱敏规范工作簿，用于验证：

- `Queries` Dataset 导入；
- `候选结果`、`Query对比` 和 `失败记录` 的统一 Run 导入；
- `Raw数据` 分块重组；
- `基准数据` 导入。

夹具只包含虚构人物和 `example.test` 链接，不包含真实接口凭证或个人数据。
