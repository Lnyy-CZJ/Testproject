# 测试开发平台工具接口文档

> 生成日期: 2026-08-13
> 适用对象: 开发、测试同学
> 配套文档: 《测试开发平台接口文档.md》（平台自身 API）

本文档覆盖通过测试开发平台网关对外提供的 **6 个工具** 的全部 HTTP 接口：

| 工具 | 网关路径前缀 | 用途 |
| --- | --- | --- |
| trackevents（埋点测试工具） | `/trackevents` | 解析 TrackEvents 埋点日志，校验事件字段/公参/响应一致性 |
| log-filter（Log 过滤工具） | `/log-filter` | 按接口名过滤客户端日志并统计接口成功率，支持导出 |
| truthy-search（知名人物检索） | `/truthy-search` | 检索系统评测平台：数据集导入、Run 执行、字段配置、复核、评估报告 |
| api-autotest（接口自动化测试） | `/api-autotest` | 提交/跟踪 pytest 接口自动化任务，查看结果与报告 |
| functional-test-agent（功能测试用例智能体） | `/functional-test-agent` | AI 需求拆解 → 测试点 → 测试用例生成，在线 Review 与确认 |
| api-test-agent（接口测试用例智能体） | `/api-test-agent` | AI 解析 API 文档 → 契约 Review → 基础/可执行用例 → 执行与缺陷草稿 |

---

## 1. 使用说明

### 1.1 Base URL

所有工具统一通过平台网关访问：

```
http://170.106.159.147:41873/<工具路径前缀>/...
```

例：`http://170.106.159.147:41873/api-autotest/health`。

工具容器端口（5001~5005 等）只在容器网络内监听，**不对外直接暴露**，外部一律走网关前缀。

### 1.2 鉴权（所有工具通用）

1. **必须先登录平台**：所有工具路径由网关统一鉴权（携带平台会话 Cookie `tp_session`）。未登录时：
   - 页面请求 → 302 跳转 `/login`；
   - API 请求 → 401 JSON：`{"code":"AUTH_REQUIRED","message":"请先登录","request_id":"nginx_..."}`。
2. **工具权限**：当前用户需拥有对应工具的访问权限，否则：
   - 页面请求 → 302 跳转 `/403`；
   - API 请求 → 403 JSON：`{"code":"PERMISSION_DENIED","message":"无权执行此操作","request_id":"nginx_..."}`。
3. **写操作 CSRF**：所有工具的写操作（POST/PUT/PATCH/DELETE）必须携带请求头 `X-CSRF-Token`，值等于 `tp_csrf` Cookie（登录时下发）。表单类接口也可用隐藏字段 `_csrf` 代替请求头。校验失败统一返回 403（各工具错误体格式见 1.4）。
4. **身份传递**：网关校验通过后向工具注入可信身份头 `X-Platform-User-ID`、`X-Platform-Username`、`X-Platform-Display-Name`、`X-Platform-Permissions`（浏览器伪造值会被覆盖）。工具侧仅用于审计上报与任务归属，不做二次登录。
5. **工具不可用**：工具容器宕机时网关返回 502/503/504 的静态提示页 `tool-unavailable.html`。

curl 典型姿势（与平台接口一致）：

```bash
# 登录（Cookie 存入 cookies.txt，含 tp_session 与 tp_csrf）
curl -X POST 'http://170.106.159.147:41873/api/v1/auth/login' \
  -H 'Content-Type: application/json' -c cookies.txt \
  -d '{"username": "你的用户名", "password": "你的密码"}'

CSRF=$(awk '$6=="tp_csrf" {print $7}' cookies.txt)

# 调用工具接口（GET 带 Cookie；写操作再加 X-CSRF-Token 头）
curl 'http://170.106.159.147:41873/api-autotest/api/tasks' -b cookies.txt
```

### 1.3 各工具健康检查

| 工具 | 健康检查地址 | 成功响应 |
| --- | --- | --- |
| trackevents | `GET /trackevents/health` | `{"service":"trackevents","status":"ok"}` |
| log-filter | `GET /log-filter/health` | `{"service":"log-filter","status":"ok"}` |
| truthy-search | `GET /truthy-search/health` | `{"service":"truthy-search","status":"ok"}`（SQLite 异常时 503 `{"status":"unavailable"}`） |
| api-autotest | `GET /api-autotest/health` | `{"status":"ok","service":"api-autotest"}` |
| functional-test-agent | `GET /functional-test-agent/health` | `{"status":"ok","service":"functional-test-agent"}` |
| api-test-agent | `GET /api-test-agent/health` | `{"status":"ok","service":"api-test-agent"}` |

健康检查接口不需要登录。

### 1.4 各工具错误响应格式（不统一，按工具区分）

| 工具 | 错误体格式 |
| --- | --- |
| 网关层（鉴权失败） | `{"code","message","request_id"}` |
| trackevents | `{"error": "错误信息"}` |
| log-filter | `{"message": "错误信息"}` |
| api-autotest | `{"error": "错误信息", "error_code": "稳定错误码"}`（部分 Flask 默认错误为 HTML 页） |
| truthy-search | JSON API 为 `{"error": "错误信息"}`；页面路由渲染统一错误页 `error.html` |
| functional/api-test-agent | `{"error": {"code","message","request_id","details"}}`，响应头附 `X-Request-ID` |

排障时请携带响应中的 `request_id`（或响应头 `X-Request-ID`）。

---

## 2. trackevents 埋点测试工具

前缀 `/trackevents`，内部端口 8000。解析 `method=TrackEvents` 的日志块，校验事件字段、公共参数、响应一致性，并对照预期触发次数。

### 2.1 GET `/trackevents/` — 工具页面

返回埋点测试单页应用 HTML（页面内自动附加 CSRF 头调用分析接口）。无参数，200 HTML。

### 2.2 POST `/trackevents/api/analyze` — 解析埋点日志

**请求**：`Content-Type: application/json`，需平台会话 + `X-CSRF-Token` 头。

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `log_text` | string | 否 | 日志全文；为空时回退读取服务端 `default.log`，也不存在则报错 |
| `expected_counts` | object | 否 | 预期触发次数，key 为事件 Action 名，value 为整数次数；非对象报 400 |

```bash
curl -X POST 'http://170.106.159.147:41873/trackevents/api/analyze' \
  -H 'Content-Type: application/json' -H "X-CSRF-Token: $CSRF" -b cookies.txt \
  -d '{"log_text": "<日志全文>", "expected_counts": {"app_foreground": 1}}'
```

**成功响应**（200 JSON，节选核心结构）：

```jsonc
{
  "summary": {
    "request_count": 5,          // TrackEvents 请求块数
    "response_count": 5,         // 响应块数
    "event_count": 7,            // 事件总数
    "passed_event_count": 6,     // 字段校验通过事件数
    "failed_event_count": 1,
    "failed_response_count": 0,  // 响应校验失败数
    "failed_count_check_count": 0 // 预期次数校验失败数
  },
  "event_counts": {"app_foreground": 1},   // 事件名 → 实际次数
  "event_stats": [
    {"module": "app", "event_name": "app_foreground", "count": 1}
  ],
  "count_checks": [            // 仅针对 expected_counts 中配置的事件
    {"event_name": "app_foreground", "expected": 1, "actual": 1, "status": "pass"}
    // status: "pass" | "fail"
  ],
  "events": [                  // 事件明细（含校验结果）
    {
      "request_index": 1, "event_index": 1,
      "event_id": "evt_xxx", "event_name": "app_foreground",
      "event_time_ms": 1700000000000,
      "module": "app",
      "required_params": ["page_from"],
      "business_params": {"page_from": "home"},
      "extra_params": {"foo": 1},        // 疑似多传字段
      "status": "pass",                   // "pass" | "fail"
      "errors": [],                       // 失败原因列表
      "warnings": ["疑似多传字段: foo"]
    }
  ],
  "response_checks": [
    {
      "request_index": 1, "request_event_count": 2, "accepted_count": 2,
      "success": true, "status": "pass", "errors": []
      // 常见 errors: "存在请求但没有对应响应"、"响应顶层 code 非 0: x"、
      // "响应 success 不是 true: x"、"accepted_count=N 与请求事件数 M 不一致"
    }
  ],
  "common_param_summary": [    // 公参汇总（42 条规则）
    {"key": "slogtime", "required": true, "allow_empty": false,
     "event_count": 7, "present_count": 7, "missing_count": 0,
     "empty_count": 0, "status": "pass"}
  ],
  "common_param_events": [     // 逐事件公参明细
    {"request_index": 1, "event_index": 1, "module": "app",
     "event_name": "app_foreground", "event_id": "evt_xxx",
     "values": {"slogtime": 1700000000000, "uid": null},
     "missing_required": [], "empty_not_allowed": [], "status": "pass"}
  ],
  "markdown_report": "# 埋点测试报告\n..."   // 完整 Markdown 报告（前端本地下载）
}
```

**错误响应**：

| 状态码 | 响应体 | 触发条件 |
| --- | --- | --- |
| 403 | `{"error": "请求安全校验失败"}` | CSRF 双提交校验失败 |
| 400 | `{"error": "<异常消息>"}` | body 非合法 JSON；`expected_counts` 非对象；`log_text` 为空且 `default.log` 不存在；解析内部异常 |

其他：`GET /trackevents`（无尾斜杠）308 重定向到 `/trackevents/`；`GET /trackevents/favicon.svg` 返回图标。

---

## 3. log-filter Log 过滤工具

前缀 `/log-filter`，内部端口 5001。按 `┌/└` 边界切块解析日志，按 method 过滤并统计接口请求/响应/成功率。请求体限制：表单 20MB、总大小 25MB（超限 413）。

### 3.1 GET `/log-filter/` — 工具页面

渲染日志过滤页面（粘贴框、method 多选、过滤结果区、接口分析统计区）。无参数，200 HTML。

### 3.2 POST `/log-filter/` — 解析并过滤日志

**请求**：`application/x-www-form-urlencoded`，需 CSRF（请求头或表单 `_csrf` 字段）。

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `log_text` | string | 实际必填 | 日志全文；空白时页面提示"请先粘贴日志内容"（仍 200） |
| `method` | string（可多选） | 否 | 要筛选的 method；特殊值 `__ALL__` 表示全部；未命中回退全部 |
| `_csrf` | string | 平台模式必填（与请求头二选一） | `tp_csrf` Cookie 值 |

```bash
curl -X POST 'http://170.106.159.147:41873/log-filter/' \
  -b cookies.txt -H "X-CSRF-Token: $CSRF" \
  --data-urlencode 'log_text=<日志全文>' \
  --data-urlencode 'method=__ALL__'
```

**成功响应**：200 HTML（整页回显过滤结果与接口分析统计：请求/响应/成功/失败次数、成功率、HTTP 状态码分布）。业务提示以内嵌消息呈现（仍 200）：

- 日志为空 → "请先粘贴日志内容"
- 未提取到 method → "未识别到 method，请检查日志格式"
- 指定 method 无匹配 → "没有匹配到相关日志"

**错误响应**：403 `{"message": "请求安全校验失败"}`（CSRF 失败）；413（超大小限制）。

### 3.3 GET `/log-filter/sample` — 示例日志

返回服务端示例文件 `log_default.log` 的原始文本（文件不存在时为空字符串）。200，无参数。

### 3.4 POST `/log-filter/export` — 导出日志到服务器目录

把页面中的日志保存为服务器固定导出目录下的 `.log` 文件（文件名带类型前缀和时间戳，客户端不能控制路径）。

**请求**：`Content-Type: application/json`，需 CSRF。

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `export_type` | string | 是 | 仅允许 `log_content`（原始日志）/ `filtered_result`（过滤结果） |
| `content` | string | 是 | 待保存文本；空/仅空白报 400 |

```bash
curl -X POST 'http://170.106.159.147:41873/log-filter/export' \
  -H 'Content-Type: application/json' -H "X-CSRF-Token: $CSRF" -b cookies.txt \
  -d '{"export_type": "filtered_result", "content": "<过滤结果文本>"}'
```

**成功响应**（200 JSON）：

```json
{
  "message": "导出成功",
  "filename": "filtered_result_20260813_120000.log",
  "path": "/home/ubuntu/Testproject/log-exports/filtered_result_20260813_120000.log"
}
```

**错误响应**：

| 状态码 | 响应体 | 触发条件 |
| --- | --- | --- |
| 400 | `{"message": "不支持的导出类型"}` | `export_type` 非法 |
| 400 | `{"message": "当前内容为空,无法导出"}` | `content` 缺失/空白 |
| 500 | `{"message": "导出失败:<详情>"}` | 目录创建或写盘失败；同名文件后缀冲突达上限 |
| 403 | `{"message": "请求安全校验失败"}` | CSRF 失败 |

---

## 4. api-autotest 接口自动化测试

前缀 `/api-autotest`，内部端口 5003。提交 pytest 接口自动化任务并跟踪生命周期。

### 4.0 任务模型

- **单槽位执行**：同一时刻至多一个 pending/running 任务，重复提交 409 `SLOT_BUSY`。
- **状态机**：`pending → running →` 终态 `succeeded` / `failed` / `cancelled`（终态不可再迁移）。
- **任务 ID 格式**：`YYYYMMDD-HHMMSS-<4位十六进制>`，如 `20260807-163012-a1b2`，按时间倒序。
- **超时**：单任务默认 1800 秒（`API_AUTOTEST_TASK_TIMEOUT_SECONDS`），超时置 `failed`、`timeout=true`、`error_code=TASK_TIMEOUT`。
- **保留数量**：默认保留最近 50 条任务记录（`API_AUTOTEST_TASKS_RETAIN`），超出自动删除最旧记录。
- **服务重启**：遗留 pending/running 任务置为 `failed`，`error_message="服务重启，任务中断"`。
- 线上配置来源为平台（`API_AUTOTEST_CONFIG_SOURCE=platform`）：提交任务前先向平台拉取运行时配置快照注入子进程；拉取失败提交被拒绝（503 `PLATFORM_CONFIG_UNAVAILABLE`），任务记录会多出 `config_release_id` / `config_release_version` / `credential_version` 字段。

### 4.1 POST `/api-autotest/api/tasks` — 提交测试任务

**请求**：`Content-Type: application/json`，需 CSRF。

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `env` | string | 是 | 环境名，必须存在 `config/env/<env>.yaml`（线上为 `test`） |
| `run_type` | string | 是 | `all` / `single` / `flow` |
| `flow` | string | 条件 | `run_type=flow` 时必填且必须存在 `data/flows/<flow>.yaml`；`single` 时禁止 |
| `tag` | string | 否 | 仅允许 `^[A-Za-z0-9_()\s]+$` 且 ≤200 字符，原样作为 pytest `-m` 表达式 |

```bash
curl -X POST 'http://170.106.159.147:41873/api-autotest/api/tasks' \
  -H 'Content-Type: application/json' -H "X-CSRF-Token: $CSRF" -b cookies.txt \
  -d '{"env": "test", "run_type": "flow", "flow": "NameWithConditionsSearch", "tag": "smoke"}'
```

**成功响应**（201 JSON）：

```json
{
  "id": "20260807-163012-a1b2",
  "status": "pending",
  "created_at": "2026-08-07T16:30:12+08:00"
}
```

**错误响应**（格式 `{"error","error_code"}`）：

| 状态码 | error_code | 触发条件 |
| --- | --- | --- |
| 400 | `INVALID_PARAMS` | 请求体非 JSON 对象；env/run_type/flow/tag 不合法或不存在；run_type=flow 缺 flow；single 指定 flow |
| 400 | `CREDENTIAL_FILE_INVALID` | `.env` 位置被目录占据 |
| 400 | `CREDENTIALS_MISSING` | 配置合并级预检失败（缺 DEVICE_ID、环境缺 gateway_base_url 等） |
| 400 | `ADMIN_CREDENTIALS_MISSING` | 目标含 Admin 审计步骤但缺凭证（只列字段名，如 `ADMIN_SESSION_TOKEN`） |
| 409 | `SLOT_BUSY` | 已有任务在执行（消息含占用任务的 id） |
| 503 | `PLATFORM_CONFIG_UNAVAILABLE` | 平台运行时配置拉取失败（线上配置来源为平台时） |
| 403 | `CSRF_INVALID` | CSRF 校验失败 |

Admin 凭证预检规则：`single` 恒不需要；`flow` 看场景文件是否含 `{{admin_` 占位符；`all` 逐个检查（带 tag 时只检查命中的 Flow）。

### 4.2 GET `/api-autotest/api/tasks` — 任务列表（分页）

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `page` | int | 1 | 小于 1 夹取为 1 |
| `page_size` | int | 20 | 夹取到 1~100 |

```bash
curl 'http://170.106.159.147:41873/api-autotest/api/tasks?page=1&page_size=10' -b cookies.txt
```

**成功响应**（200 JSON）：

```json
{
  "items": [
    {
      "id": "20260807-163012-a1b2",
      "status": "succeeded",
      "input": {"env": "test", "run_type": "single", "flow": null, "tag": null},
      "pid": 1234,
      "created_at": "2026-08-07T16:30:12+08:00",
      "started_at": "2026-08-07T16:30:12+08:00",
      "finished_at": "2026-08-07T16:31:05+08:00",
      "cancel_requested_at": null,
      "exit_code": 0,
      "timeout": false,
      "error_code": null,
      "error_message": null,
      "result_available": true,
      "junit_file": "reports/junit-task-20260807-163012-a1b2.xml",
      "log_file": "logs/2026-08-07/20260807-163012_test_1234.log",
      "summary": {"total": 2, "passed": 2, "failed": 0, "errors": 0, "skipped": 0}
    }
  ],
  "page": 1, "page_size": 10, "total": 1
}
```

### 4.3 GET `/api-autotest/api/tasks/<task_id>` — 任务详情

返回 4.2 中单条任务记录的全量字段（轮询此接口跟踪状态直至终态）。`task_id` 必须匹配 `^\d{8}-\d{6}-[0-9a-f]{4}$`，否则 404；记录不存在也 404。

### 4.4 POST `/api-autotest/api/tasks/<task_id>/cancel` — 取消任务

登记取消请求并向子进程组发送 SIGTERM（宽限 10 秒后 SIGKILL），接口立即返回，最终 `cancelled` 终态需再查详情确认。需 CSRF，无请求体。

**成功响应**（200 JSON）：`{"id": "20260807-163012-a1b2", "status": "running"}`（此时 `cancel_requested_at` 已写入）。

**错误响应**：

| 状态码 | error_code | 触发条件 |
| --- | --- | --- |
| 404 | `TASK_NOT_FOUND` | 任务不存在 |
| 409 | `TASK_TERMINATED` | 任务已处于终态 |

### 4.5 GET `/api-autotest/api/tasks/<task_id>/result` — 任务结果摘要

解析任务的 JUnit 文件，返回统计与失败用例清单（失败消息已脱敏并截断 500 字符）。

**成功响应 — 有结果**（200 JSON）：

```json
{
  "status": "failed",
  "result_available": true,
  "summary": {"total": 2, "passed": 1, "failed": 1, "errors": 0, "skipped": 0},
  "failed_cases": [{"name": "case_b", "message": "断言失败"}]
}
```

**成功响应 — 无 JUnit**（取消/强杀未生成结果，合法边界）：

```json
{
  "status": "succeeded",
  "result_available": false,
  "summary": null,
  "failed_cases": [],
  "reason_code": "JUNIT_NOT_GENERATED"
}
```

任务不存在 → 404。

### 4.6 GET `/api-autotest/api/tasks/<task_id>/logs` — 任务日志

三级来源：① 框架脱敏日志；② console 日志尾部（二次脱敏）；③ 空。

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `tail` | int | 500 | 返回行数，夹取到 1~2000；非整数 400 |

**成功响应**（200 JSON，`source` ∈ `framework_log` / `console_redacted` / `none`）：

```json
{
  "log_file": "logs/2026-08-07/20260807-163012_test_1234.log",
  "lines": ["...日志行..."],
  "source": "framework_log"
}
```

### 4.7 GET `/api-autotest/api/catalog` — 用例库清单

只读解析 `data/` 目录，产出 API / Case / Flow 三类清单；单个坏文件进入 `errors` 数组不影响整体。无参数。

**成功响应**（200 JSON，顶层固定四个键）：

```json
{
  "apis": [{"id": "GetMe", "name": "...", "service_name": "...", "method_name": "..."}],
  "cases": [{"api": "GetMe", "id": "...", "name": "...", "tags": ["smoke"]}],
  "flows": [
    {"name": "NameWithConditionsSearch", "tags": [], "step_count": 3,
     "apis": ["CreateAnonymousSession", "GetMe"]}
  ],
  "errors": [{"file": "data/flows/Broken.yaml", "message": "...解析错误原因..."}]
}
```

### 4.8 GET `/api-autotest/api/credentials/status` — 凭证就绪状态预检

任务提交前本地预检的只读版本。**只返回状态与缺失字段名，绝不返回凭证值。**

| 参数 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `env` | string | `test` | 环境名 |
| `run_type` | string | `all` | `all`/`single`/`flow` |
| `flow` | string | 无 | `run_type=flow` 时定位场景 |
| `tag` | string | 无 | `run_type=all` 时只检查命中的 Flow |

```bash
curl 'http://170.106.159.147:41873/api-autotest/api/credentials/status?env=test&run_type=flow&flow=NameWithConditionsSearch' -b cookies.txt
```

**成功响应**（200 JSON）：

```json
{
  "base_config": {"ready": true, "message": ""},
  "admin": {"required": true, "ready": false, "missing_fields": ["ADMIN_SESSION_TOKEN"]}
}
```

- `base_config.ready`：配置合并级预检是否通过，失败时 `message` 为可读原因；
- `admin.missing_fields` 可能值：`ADMIN_SESSION_TOKEN`、`ADMIN_OPERATOR_ID`、`ADMIN_OPERATOR_NAME`。

### 4.9 GET `/api-autotest/api/report/meta` — Allure 报告元信息

**成功响应**（200 JSON，三种形态）：

```json
// 报告不存在
{"exists": false, "report_url": null}

// 报告存在且有元信息
{"exists": true, "report_url": "/api-autotest/reports/index.html", "synced_at": "2026-08-10T10:00:00+08:00", "source": "jenkins"}

// 报告存在但无元信息（source 为 unknown）
{"exists": true, "report_url": "/api-autotest/reports/index.html", "synced_at": "2026-08-10T10:00:00+08:00", "source": "unknown"}
```

### 4.10 GET `/api-autotest/reports/<filename>` — Allure 静态报告资源

托管 Allure 静态文件（如 `/api-autotest/reports/index.html`、`data/suites.csv`）。报告未发布或文件不存在 → 404。

### 4.11 页面路由（HTML）

| 方法 + 路径 | 说明 |
| --- | --- |
| GET `/api-autotest/` | 首页：执行表单、凭证状态、报告入口、最近任务 |
| GET `/api-autotest/tasks/<task_id>` | 任务详情页（轮询任务状态/结果/日志，可取消）；任务不存在 404 |
| GET `/api-autotest/catalog` | 用例库页：API/Case/Flow 清单与解析错误 |

### 4.12 错误码速查

| 错误码 | HTTP | 出现接口 | 含义 |
| --- | --- | --- | --- |
| `INVALID_PARAMS` | 400 | 提交任务 | 请求体/参数校验失败 |
| `CREDENTIAL_FILE_INVALID` | 400 | 提交任务 | `.env` 位置被目录占据 |
| `CREDENTIALS_MISSING` | 400 | 提交任务 | 配置合并级凭证缺失 |
| `ADMIN_CREDENTIALS_MISSING` | 400 | 提交任务 | Admin 凭证字段缺失 |
| `SLOT_BUSY` | 409 | 提交任务 | 单槽位被占用 |
| `TASK_NOT_FOUND` | 404 | 取消任务 | 任务不存在 |
| `TASK_TERMINATED` | 409 | 取消任务 | 任务已终态 |
| `CSRF_INVALID` | 403 | 所有写接口 | CSRF 校验失败 |
| `PLATFORM_CONFIG_UNAVAILABLE` | 503 | 提交任务 | 平台运行时配置不可用 |
| `TASK_TIMEOUT` | —（任务记录内） | — | 任务超时被终止 |
| `ALL_TESTS_SKIPPED` | —（任务记录内） | — | 全部用例被跳过，判定失败 |
| `JUNIT_NOT_GENERATED` | —（result 的 reason_code） | 结果接口 | JUnit 未生成 |

注意：分页参数非法（page/page_size/tail 非整数）返回 Flask 默认 HTML 400 错误页，非 JSON。

---

## 5. truthy-search 知名人物检索评测工具

前缀 `/truthy-search`，内部端口 5002，Flask + SQLite。用于检索系统的常态化评测：导入数据集与基准 → 创建 Run 执行检索 → 字段处理 → 人工复核 → 生成评估报告。

要点：

- **Run 全局串行**：同一时间只允许一个 `PENDING`/`RUNNING` 的执行 Run（单线程后台执行器），重复创建返回 409。
- **不可变快照**：参考线方案、字段配置、Process 处理结果一旦创建不可修改，只能复制发布新版本；修改人物关联/复核会把关联的 READY 报告标记为 STALE。
- **平台配置模式**：线上 `SEARCH_CONFIG_SOURCE=platform`，创建 Run 前先从平台拉取运行时配置快照，平台配置不可用时返回 503。

### 5.0 全局约定与枚举速查

**交互形态**

| 项目 | 约定 |
| --- | --- |
| 页面路由 | 返回 HTML；写操作为 `application/x-www-form-urlencoded` 表单，需 CSRF（`X-CSRF-Token` 头或表单字段 `_csrf`） |
| 写操作成功 | 多数为 `302` 重定向 + 页面 flash 提示；失败渲染 `error.html`（带状态码）或回到表单页 |
| 上传限制 | 单请求体 **25 MB**（线上 `SEARCH_WEB_MAX_UPLOAD_BYTES=26214400`），超限返回 413 错误页；上传扩展名仅 `.jsonl`、`.json`、`.xlsx` |
| 分页 | 列表页固定每页 50 条，页码参数 `page`（最小 1） |
| 展示时区 | 页面时间统一按 Asia/Shanghai 渲染 |

**枚举速查**

| 枚举 | 取值 |
| --- | --- |
| Run 状态 | `PENDING`、`RUNNING`、`COMPLETED`、`PARTIAL_FAILED`、`FAILED`、`INTERRUPTED`（服务重启后遗留的运行中 Run 被标记为 INTERRUPTED） |
| Query 状态 | `PENDING`、`RUNNING`、`SUCCESS`、`NO_CANDIDATE`、`PARTIAL_DETAIL_FAILED`、`FAILED` |
| Query Stage（检索线） | `FULL_NAME`、`FULL_NAME_SOCIAL`、`FULL_NAME_PHOTO`、`FULL_NAME_SOCIAL_PHOTO` |
| 评估阶段 evaluation_phase | `PHASE_1_BASELINE`、`PHASE_2_POST_OPTIMIZATION`、`PHASE_3_TARGETED_ITERATION`、`UNSPECIFIED`（新建执行 Run 不允许 UNSPECIFIED；历史导入可用） |
| Query 结果状态 | `HAS_CANDIDATES`、`NO_CANDIDATES`、`EXECUTION_FAILED` |
| 参考线方案状态 | `ACTIVE`、`ARCHIVED` |
| 报告类型 / 报告状态 | `SINGLE` / `COMPARE`；`READY`、`STALE`（数据变更后过期）、`FAILED` |
| 候选人判定 judgement | `HIT`、`NOT_HIT`、`SUSPECTED`（复核前为 `PENDING_REVIEW`） |
| 参考线字段（每个 Stage 各一份） | `min_retrieval_success`、`min_matched_completeness`、`min_matched_accuracy`（比率下限）、`max_average_total_cost`、`max_average_search_duration_ms`（数值上限） |

### 5.1 GET `/truthy-search/health` — 健康检查

无需登录。检查 Flask 进程与 SQLite 可查询性。

- 200：`{"service": "truthy-search", "status": "ok"}`
- 503：`{"service": "truthy-search", "status": "unavailable"}`（数据库不可查询）

### 5.2 页面路由速查（GET，返回 HTML，需登录）

**总览与报告**

| 路由 | 说明 |
| --- | --- |
| `GET /truthy-search/` | 首页：Evaluation 汇总 + 最近 10 份报告入口 |
| `GET /truthy-search/reports` | 报告列表，每页 50 条；筛选参数 `evaluation_id`、`system_version`（模糊）、`report_type`（SINGLE/COMPARE）、`status`（READY/STALE/FAILED） |
| `GET /truthy-search/reports/<report_id>` | 报告详情（只读 metrics_json 快照，不重新计算）；404 = 报告不存在 |
| `GET /truthy-search/downloads/<file_type>/<run_id>` | 文件下载，见 5.5 |

**参考线方案（Threshold Profile）**

| 路由 | 说明 |
| --- | --- |
| `GET /truthy-search/threshold-profiles` | 方案列表（含已归档历史版本与引用它的 Evaluation 数） |
| `GET /truthy-search/threshold-profiles/new` | 新建方案表单 |
| `GET /truthy-search/threshold-profiles/<profile_id>` | 方案详情（不可变内容、来源版本、引用列表）；404 = 不存在 |
| `GET /truthy-search/threshold-profiles/<profile_id>/copy` | 基于历史版本预填的新建表单；404 = 来源不存在 |

**评测（Evaluation）与执行（Run）**

| 路由 | 说明 |
| --- | --- |
| `GET /truthy-search/evaluations/new` | 新建评测表单（可选 ACTIVE 参考线方案） |
| `GET /truthy-search/evaluations/<evaluation_id>` | 评测详情：Run 列表、Dataset 选择、参考线编辑、报告列表；404 = 不存在 |
| `GET /truthy-search/runs/<run_id>` | Run 详情：Query 分页（每页 50）、失败记录、进度；404 = 不存在 |
| `GET /truthy-search/runs/<run_id>/person-links` | 人物关联维护页（见 5.3.8），过滤参数 `q`、`status`（linked/unlinked/invalid/unique/multiple）、`baseline_version` |
| `GET /truthy-search/runs/<run_id>/queries/<query_id>` | Query 详情：输入、Task 字段、候选人（每页 50）、Raw 索引、失败记录；404 = 不存在 |
| `GET /truthy-search/candidates/<candidate_pk>` | 候选人详情（五个业务模块 + 按需加载 Detail Raw）；404 = 不存在 |

**导入与基准**

| 路由 | 说明 |
| --- | --- |
| `GET /truthy-search/imports` | 导入页（Dataset / 历史 JSONL / 历史 Excel） |
| `GET /truthy-search/baselines` | Person 基准列表与人物记录浏览；参数 `baseline_version`、`page` |

**字段配置（Field Schema）**

| 路由 | 说明 |
| --- | --- |
| `GET /truthy-search/field-schemas` | 字段配置版本列表（不可变、活跃标记、Process 引用数）；过滤 `q`、`module`、`role` |
| `GET /truthy-search/field-schemas/new` | 发布新版本表单（可带 `base=<schema_version>` 预填；base 不存在 404） |
| `GET /truthy-search/field-schemas/<schema_version>/comparison-matrix` | 字段 × 基准对比矩阵页；参数 `baseline_version`、`process_id`、`person_id`、`module`、`status`、`q`；404 = 版本不存在 |

**处理与复核**

| 路由 | 说明 |
| --- | --- |
| `GET /truthy-search/processes/<process_id>` | Process 详情：不可变处理结果、字段级错误、指标；404 = 不存在 |
| `GET /truthy-search/processes/<process_id>/queries/<query_id>/classification` | 单 Query 候选人身份归类页；404 = 上下文不存在 |

### 5.3 写操作接口详情

以下接口均为 POST（需平台会话 + CSRF）。成功时除特别说明外均为 **302 重定向**；失败渲染 `error.html` 或回到表单页并携带对应状态码。

#### 5.3.1 POST `/truthy-search/threshold-profiles` — 创建参考线方案

创建一个不可变的参考线方案版本。

| 表单字段 | 必填 | 说明 |
| --- | --- | --- |
| `profile_id` | 是 | 方案唯一标识 |
| `name` | 是 | 方案名称 |
| `version` | 是 | 整数版本号，同名方案下唯一 |
| `description` | 否 | 描述 |
| `based_on_profile_id` | 否 | 来源方案（复制创建时填写） |
| `threshold__<stage>__<field>` | 否 | 每个 Query Stage × 参考线字段的阈值，如 `threshold__FULL_NAME__min_retrieval_success=0.95`；留空表示不配置 |

```bash
curl -X POST 'http://170.106.159.147:41873/truthy-search/threshold-profiles' \
  -H "X-CSRF-Token: $CSRF" -b cookies.txt \
  --data-urlencode 'profile_id=tp_default' \
  --data-urlencode 'name=默认参考线' \
  --data-urlencode 'version=1' \
  --data-urlencode 'threshold__FULL_NAME__min_retrieval_success=0.95'
```

- 成功：302 → 方案详情页。
- 失败：400（ID/版本重复、version 非整数、字段校验失败等），回到表单页并显示「创建参考线方案失败: …」。

#### 5.3.2 POST `/truthy-search/threshold-profiles/<profile_id>/archive` — 归档方案

归档后方案不再出现在新建表单的可选项中，历史 Evaluation 引用不受影响。404 = 方案不存在或已不可归档。成功 302 → 方案详情。

#### 5.3.3 POST `/truthy-search/evaluations/new` — 创建评测

| 表单字段 | 必填 | 说明 |
| --- | --- | --- |
| `evaluation_id` | 是 | 评测唯一标识 |
| `name` | 是 | 评测名称 |
| `notes` | 否 | 备注 |
| `threshold_profile_id` | 否 | 初始参考线方案（仅 ACTIVE 可选），留空则后续手工配置 |

成功 302 → 评测详情；失败 400（标识重复/非法），回到表单页。

#### 5.3.4 POST `/truthy-search/evaluations/<evaluation_id>/threshold-profile` — 更换参考线方案

表单字段 `threshold_profile_id`（可为空表示解除绑定）。只影响之后新生成的报告，既有报告保留原快照。失败 400（方案不存在/已归档等），渲染 error.html。成功 302 → 评测详情。

#### 5.3.5 POST `/truthy-search/evaluations/<evaluation_id>/thresholds` — 保存评测参考线

表单字段与 5.3.1 相同（`threshold__<stage>__<field>`）。非法值不会覆盖旧参考线，返回 400 error.html。成功 302 → 评测详情。

#### 5.3.6 POST `/truthy-search/evaluations/<evaluation_id>/runs` — 创建执行 Run

创建 Run 并立即提交后台单线程队列执行。**全局同一时间只允许一个活动执行 Run**。

| 表单字段 | 必填 | 说明 |
| --- | --- | --- |
| `dataset_id` | 是 | 已导入且 Query 数 > 0 的数据集 |
| `run_label` | 是 | 本次运行标签 |
| `system_version` | 是 | 被测系统版本 |
| `evaluation_phase` | 是 | `PHASE_1_BASELINE` / `PHASE_2_POST_OPTIMIZATION` / `PHASE_3_TARGETED_ITERATION`（不允许 UNSPECIFIED） |

```bash
curl -X POST 'http://170.106.159.147:41873/truthy-search/evaluations/eval_demo/runs' \
  -H "X-CSRF-Token: $CSRF" -b cookies.txt \
  --data-urlencode 'dataset_id=ds_famous_100' \
  --data-urlencode 'run_label=基线首轮' \
  --data-urlencode 'system_version=v1.3.0' \
  --data-urlencode 'evaluation_phase=PHASE_1_BASELINE'
```

**响应**

| 状态码 | 条件 |
| --- | --- |
| 302 | 创建成功 → Run 详情页（Run 进入 PENDING/RUNNING） |
| 400 | 参数错误：run_label/system_version 为空、evaluation_phase 非法、Evaluation 或 Dataset 不存在、Dataset 无可执行 Query |
| 409 | 已有活动执行任务（「已有执行任务 \<run_id\> 正在等待或运行」） |
| 503 | 平台运行时配置不可用（平台模式拉取配置失败，稍后重试） |

#### 5.3.7 POST `/truthy-search/runs/<run_id>/evaluation-phase` — 补录评估阶段

表单字段 `evaluation_phase`（枚举见 5.0）。用于历史导入或遗漏阶段的 Run 人工补录。失败 400（非法阶段值）。成功 302 → Run 详情。

#### 5.3.8 POST `/truthy-search/runs/<run_id>/person-links` — 保存人物关联

把 Run 内 Query 与基准 Person 建立关联，支持乐观锁防并发覆盖。GET 版本用于打开维护页（只生成精确姓名建议，不自动写入）。

| 表单字段 | 必填 | 说明 |
| --- | --- | --- |
| `baseline_version` | 否 | 目标基准版本；缺省取最新基准 |
| `changes_json` | 二选一 | JSON 数组批量提交，元素形如 `{"query_id":"q1","expected_person_id":"p_old","person_id":"p_new"}`；`person_id` 为空字符串表示解除关联 |
| `person_id__<query_id>` | 二选一 | 无 JS 的逐行表单提交方式；配套 `expected_person_id__<query_id>` |
| `sync_dataset` | 否 | `true` 时把关联同步回 Dataset |
| `note` | 否 | 备注 |

其中 `expected_person_id` 是页面加载时的旧值，服务端用它做乐观锁比较。

**响应**

| 状态码 | 条件 |
| --- | --- |
| 302 | 保存成功（flash 展示 Run 更新条数 / Dataset 同步条数 / 被标记过期的报告数） |
| 400 | `changes_json` 非法 JSON；人物不存在等校验错误 |
| 409 | 乐观锁冲突：记录已被其他页面修改，请刷新后重试 |

#### 5.3.9 POST `/truthy-search/runs/<run_id>/queries/<query_id>/retry` — 单条 Query 重跑

在原 Run 中排队重跑一条失败或中断遗留的 Query（会产生新的检索成本）。限制：仅 `source_type=EXECUTION` 的 Run；Run 必须已结束（非 PENDING/RUNNING）；Query 状态必须为 `FAILED` 或 `PENDING`。

| 状态码 | 条件 |
| --- | --- |
| 302 | 已进入重跑队列 → Query 详情 |
| 400 | Run/Query 不存在、非执行 Run、Query 状态不允许重跑 |
| 409 | 当前 Run 尚未结束，或该 Query 已在重跑队列中 |

#### 5.3.10 POST `/truthy-search/imports` — 导入 Dataset / 历史结果

multipart/form-data 上传，单请求体上限 25 MB。

| 表单字段 | 必填 | 说明 |
| --- | --- | --- |
| `import_type` | 是 | `dataset`（数据集）/ `results_jsonl`（历史 JSONL 结果）/ `results_excel`（历史 Excel 结果） |
| `source_file` | 是 | 主文件；dataset 支持 `.jsonl`/`.xlsx`，results_jsonl 必须 `.jsonl`，results_excel 必须 `.xlsx` |
| `name` | dataset 必填 | 数据集名称 |
| `dataset_id` | 否 | 指定数据集 ID（默认自动生成） |
| `evaluation_id` / `run_label` / `system_version` | 结果导入必填 | 归属评测、运行标签、系统版本 |
| `evaluation_phase` | 否 | 默认 `UNSPECIFIED` |
| `failures_file` / `metadata_file` | 否 | 仅 `results_jsonl`：失败记录与 Query 元数据辅助文件 |

**响应**

| 状态码 | 条件 |
| --- | --- |
| 302 | 导入成功（dataset → 导入页；历史结果 → 对应 Run 详情） |
| 400 | 未选文件、类型/后缀不匹配、必填字段为空、文件内容校验失败、**重复导入**（同内容已导入过） |
| 413 | 请求体超过 25 MB，渲染「上传文件过大」错误页 |

#### 5.3.11 POST `/truthy-search/baselines` — 导入 Person 基准

multipart/form-data。

| 表单字段 | 必填 | 说明 |
| --- | --- | --- |
| `source_file` | 是 | `.jsonl` 或 `.xlsx` 基准文件 |
| `name` | 是 | 基准名称 |
| `baseline_version` | 是 | 基准版本号（唯一） |

成功 302 → `/baselines?baseline_version=<version>`；失败 400（必填为空、格式错误、版本重复）；超限 413。

#### 5.3.12 POST `/truthy-search/baselines/<baseline_version>/people/<person_id>/available-fields` — 更新基准可用字段

表单字段 `available_fields`（可多个，复选框列表）。维护单个人物的「可评估字段」，来源标记为人工维护。失败 400（基准/人物不存在或字段非法）。成功 302 → 基准页。

#### 5.3.13 POST `/truthy-search/field-schemas/new` — 发布字段配置版本

| 表单字段 | 必填 | 说明 |
| --- | --- | --- |
| `name` | 是 | 版本名称 |
| `definitions_json` | 是 | 完整字段定义 JSON 数组（field_key、display_name、module、data_type、source_path、scoring_role 等） |
| `created_by` | 否 | 创建人 |
| `base_version` | 否 | 复制来源版本（页面展示用） |

发布后新版本成为活跃版本。失败 400：JSON 解析失败（「字段配置 JSON 格式错误: …」）或字段定义校验失败（逐条错误信息）。成功 302 → 字段配置列表。

#### 5.3.14 POST `/truthy-search/field-schemas/<schema_version>/comparison-matrix` — 从矩阵发布新版本

基于某个不可变版本的字段矩阵批量调整并发布新版本（已有 Process 不受影响）。

主要表单字段：`name`、`created_by`、`baseline_version`，以及逐字段复选框列表 `enabled_fields`、`completeness_fields`、`accuracy_fields`、`identity_fields`、`visible_fields`、`display_fields`、`baseline_compare_fields`、`run_compare_fields`，逐字段参数 `compare_mode__<field>`、`normalizer__<field>`、`baseline_field_key__<field>`、`similarity_threshold__<field>`，新发现字段 `discovered_field_keys` + `discovered_definitions_json`。

失败 400：相似度阈值非数值、待配置字段 JSON 失效、字段 key 冲突、字段定义校验失败等。成功 302 → 新版本的矩阵页。

#### 5.3.15 POST `/truthy-search/runs/<run_id>/process` — 启动字段处理

**同步执行**（请求会阻塞到处理完成），生成一个不可变 Process。

| 表单字段 | 必填 | 说明 |
| --- | --- | --- |
| `schema_version` | 是 | 字段配置版本 |
| `baseline_version` | 否 | 基准版本（参与字段对比时需要） |
| `processing_mode` | 否 | `PROCESS_EXISTING`（默认）/ `REPROCESS_EXISTING`（仅重处理已入库数据，不再请求检索接口） |
| `confirm_existing_data` | 重处理必填 | 必须为 `true`，确认只重处理已入库数据 |
| `acknowledge_alignment_errors` | 视预检结果 | 字段对齐预检存在阻断（ERROR）问题时，必须显式 `true` 确认风险 |

成功 302 → Process 详情（flash 展示候选人数与字段错误数）。失败 400（未确认重处理、对齐预检阻断未确认、schema/baseline 不存在等）。

#### 5.3.16 POST `/truthy-search/processes/<process_id>/queries/<query_id>/classification` — 保存身份归类

保存一个 Query 下候选人的身份归类（HIT/NOT_HIT/SUSPECTED）。保存后关联的 READY 报告被标记 STALE。

| 表单字段 | 必填 | 说明 |
| --- | --- | --- |
| `judgement__<candidate_pk>` | 否 | `HIT`/`NOT_HIT`/`SUSPECTED`；缺省 `PENDING_REVIEW` 表示不提交该候选人 |
| `reason__<candidate_pk>` | 否 | 判定理由类别，默认 `MANUAL` |
| `evidence__<candidate_pk>` | 否 | 证据说明 |
| `expected_reviewed_at__<candidate_pk>` | 否 | 页面加载时的复核时间，用于乐观锁 |
| `primary_hit_candidate_pk` | 否 | 主命中候选人（judgement=HIT 时指定） |
| `confirm_no_hit` | 否 | `true` 确认本 Query 无命中 |
| `bulk_remaining_not_hit` | 否 | `true` 时把其余 detail_status=SUCCESS 且未判定的候选人批量置为 NOT_HIT |
| `reviewer` / `review_note` | 否 | 复核人与备注 |

| 状态码 | 条件 |
| --- | --- |
| 302 | 保存成功 |
| 400 | 判定值非法、主命中与判定不一致等校验错误 |
| 409 | 乐观锁冲突：其他页面已修改同一记录 |

GET 同一URL打开归类页；Process/Query 不存在时 404。

#### 5.3.17 POST `/truthy-search/processes/<process_id>/candidates/<candidate_pk>/review` — 保存候选人复核

保存候选人最终判定与字段级得分。

| 表单字段 | 必填 | 说明 |
| --- | --- | --- |
| `judgement` | 是 | `HIT` / `NOT_HIT` / `SUSPECTED` |
| `reason` / `evidence` | 否 | 理由与证据 |
| `reviewer` / `review_note` | 否 | 复核人与备注 |
| `field_scores_json` | 二选一 | 字段得分 JSON（含 completeness_score/accuracy_score/review_note） |
| `completeness__<field>` / `accuracy__<field>` / `field_note__<field>` | 二选一 | 无 JS 的逐字段表单提交方式 |
| `expected_reviewed_at` | 否 | 乐观锁：页面加载时的复核时间 |

| 状态码 | 条件 |
| --- | --- |
| 302 | 保存成功（关联 READY 报告标记 STALE） |
| 400 | `field_scores_json` 非法 JSON、判定值非法等 |
| 409 | 乐观锁冲突：其他页面已更新该复核记录 |

#### 5.3.18 POST `/truthy-search/reports` — 创建报告

生成报告模型快照 + 静态 HTML，并按开关生成 Excel（线上 `SEARCH_REPORT_EXCEL_ENABLED=false`，不生成 Excel）。

| 表单字段 | 必填 | 说明 |
| --- | --- | --- |
| `candidate_process_id` | 是 | 候选 Process（报告主体） |
| `baseline_process_id` | 否 | 基线 Process；填写则生成 COMPARE 对比报告，否则 SINGLE |
| `data_marker` | 否 | 数据标记，默认 `REAL_TEST_DATA` |

| 状态码 | 条件 |
| --- | --- |
| 302 | 生成成功 → 报告详情（Excel 不可用时以 flash 警告提示，不阻塞） |
| 400 | Process 不存在、复核未完成、配置缺失等业务校验失败（报告记录置 FAILED） |
| 500 | 报告文件写出等意外错误（报告记录置 FAILED） |

#### 5.3.19 POST `/truthy-search/reports/<report_id>/name` — 重命名报告

表单字段 `report_name`。保存后同步重新生成静态 HTML（静态文件写失败仅提示警告，不撤销命名）。404 = 报告不存在；400 = 名称校验失败。成功 302 → 报告详情。

### 5.4 JSON API（返回 JSON，需登录）

#### 5.4.1 GET `/truthy-search/api/runs/<run_id>/status` — Run 进度

供页面轮询的进度接口。

```bash
curl 'http://170.106.159.147:41873/truthy-search/api/runs/run_3f2a…/status' -b cookies.txt
```

```jsonc
// 200
{
  "run_id": "run_3f2a9c1e",
  "status": "RUNNING",              // Run 状态枚举
  "total_queries": 100,
  "completed_queries": 37,          // success + failed
  "success_queries": 35,
  "failed_queries": 2,
  "current_query_id": "q_038",      // 当前正在执行的 Query，无则空串
  "current_stage": "FULL_NAME_SOCIAL",
  "message": "执行中"
}
```

404：Run 不存在（error.html）。

#### 5.4.2 GET `/truthy-search/api/field-schemas/<schema_version>` — 字段配置快照

```jsonc
// 200
{
  "schema_version": "fs_v4_20260801",
  "name": "v4 字段配置",
  "created_by": "tester01",
  "created_at": "2026-08-01T02:11:09+00:00",
  "is_active": true,
  "definitions": [ /* 字段定义数组 */ ]
}
```

404：版本不存在。

#### 5.4.3 GET `/truthy-search/api/field-schemas/<schema_version>/comparison-matrix` — 字段矩阵 JSON

查询参数：`baseline_version`（**必填**）、`process_id`、`person_id`（可选过滤）。

错误体（JSON）：

| 状态码 | 场景 |
| --- | --- |
| 400 | `{"error": "baseline_version 不能为空"}` 或字段矩阵构建失败 `{"error": "..."}` |
| 404 | schema 版本不存在（error.html） |

#### 5.4.4 GET `/truthy-search/api/processes/<process_id>/status` — Process 状态

```jsonc
// 200
{
  "process_id": "proc_7d21",
  "run_id": "run_3f2a9c1e",
  "schema_version": "fs_v4_20260801",
  "status": "COMPLETED",
  "error_count": 3,
  "created_at": "2026-08-03T08:00:12+00:00",
  "finished_at": "2026-08-03T08:00:41+00:00"
}
```

404：Process 不存在。

#### 5.4.5 GET `/truthy-search/api/processes/<process_id>/metrics` — Process 指标

返回与 Process 页面一致的可追溯指标模型 JSON（200）。404：Process 不存在或指标不可计算。

#### 5.4.6 GET `/truthy-search/api/raw/<raw_id>` — 单条 Raw 记录

按需加载一条脱敏后的原始业务 JSON。

```jsonc
// 200
{
  "raw_id": "raw_000123",
  "run_id": "run_3f2a9c1e",
  "query_id": "q_038",
  "candidate_pk": "cand_8842",
  "stage": "FULL_NAME_SOCIAL",
  "sequence_no": 2,
  "collected_at": "2026-08-03T07:31:22+00:00",
  "payload": { /* 原始响应脱敏后的业务字段 */ }
}
```

404：Raw 不存在。

### 5.5 GET `/truthy-search/downloads/<file_type>/<run_id>` — 文件下载

| `file_type` | `<run_id>` 语义 | 下载内容 |
| --- | --- | --- |
| `results` | Run ID | Run 结果 JSONL |
| `failures` | Run ID | Run 失败记录 JSONL |
| `report-html` | **Report ID** | 报告静态 HTML（FAILED 报告不可下载） |
| `report-excel` | **Report ID** | 报告 processed Excel（线上未启用 Excel 生成，通常为 404） |

以下情况一律 404（不暴露具体原因）：file_type 非法、记录不存在、产物文件不存在、FAILED 报告、路径越界。

### 5.6 全局错误速查

| 状态码 | 场景 |
| --- | --- |
| 400 | 表单/JSON 校验失败（参数缺失、枚举非法、JSON 解析失败、重复导入、业务规则不满足） |
| 403 | CSRF 校验失败：`{"code":"CSRF_INVALID","message":"请求安全校验失败"}`（before_request 拦截，JSON 格式） |
| 404 | 对象不存在；下载防护；路径不在 `/truthy-search` 前缀内 |
| 409 | 乐观锁冲突（person-links / classification / review）；已有活动 Run；重复提交重跑 |
| 413 | 上传超过 25 MB |
| 500 | 未预期错误（error.html，不暴露堆栈） |
| 503 | 平台运行时配置不可用（创建 Run 时）；健康检查数据库异常 |

页面路由的错误统一渲染 `error.html`（含 title/message/状态码）；仅 `/api/` 前缀与 CSRF 拦截返回 JSON。

---

## 6. functional-test-agent 功能测试用例智能体

前缀 `/functional-test-agent`，内部端口 5004。AI 驱动的「需求拆解 → 测试点 → 测试用例」生成流水线，支持测试点/用例在线 Review、AI 辅助修改与确认发布。

要点：

- **异步任务模型**：所有生成操作均为异步任务（提交返回 202），通过任务详情 + 日志轮询接口跟踪进度，**无 SSE/WebSocket**。
- **等待 Review 状态**：`generate_test_points` / `full_pipeline` 生成测试点后任务停在 `waiting_review`，等待人工确认（resume）才继续生成用例；开启在线用例 Review 时用例阶段停在 `waiting_case_review`。
- **依赖 LLM 配置**：`require_llm_config=True`，平台未配置 `LLM_API_KEY` 时任务创建仍返回 202，但任务随后异步失败（`error_code=CONFIG_NOT_READY`）。可用 readiness 接口的 `llm_secret_configured` 字段提前检查。

### 6.0 两个 Agent 的共用约定（本节同样适用于 §7 api-test-agent）

**错误体格式**（所有 JSON 接口统一）：

```jsonc
// HTTP 状态码 4xx/5xx
{
  "error": {
    "code": "REVIEW_REVISION_CONFLICT",      // 稳定错误码
    "message": "草稿已被其他人更新",
    "request_id": "req_8f3a2b1c9d0e4f5a6b7c", // 排障请携带
    "details": { }                            // 可选，仅非空时出现
  }
}
```

每个响应都带 `X-Request-ID` 响应头（与 `error.request_id` 一致）。

**身份与权限**

- 身份完全依赖网关注入的 `X-Platform-User-ID/Username/Display-Name/Permissions` 头，工具侧不做二次登录。
- `X-Platform-User-ID` 缺失 → **401 `AUTH_REQUIRED`「请先登录」**（失败关闭，直连容器端口绕过网关也会被拒绝）。
- 权限不足 → 403 `PERMISSION_DENIED`；写操作 CSRF 失败 → 403 `CSRF_INVALID`。
- 任务默认可见范围 = 本人创建；持 `task.view.all` 可见全部任务；访问他人任务返回 404 `TASK_NOT_FOUND`。

**权限点一览**

| 权限点 | 作用 |
| --- | --- |
| `tool.view` | 打开工具首页、readiness |
| `tool.execute` | 创建任务、保存/导入草稿、触发 AI、resume、用例确认 |
| `tool.result.view` | 任务列表/详情、日志、产物、Review 数据与下载 |
| `task.cancel` | 取消任务、取消 AI 子任务 |
| `task.view.all` | 查看他人任务 |
| `api-test-agent.contract.review` / `case.review` / `execute` / `defect.create` | api-test-agent V2 专属，见 §7 |

**大小与队列限制**（线上取平台 Release 配置，下表为默认值）

| 项 | 限制 | 超限行为 |
| --- | --- | --- |
| 单请求体（Flask） | 11 MiB | 413 `INVALID_INPUT` |
| 文档上传 | 5 MiB / 50 万字符 | 413 `INVALID_INPUT` |
| 测试点条数 | 5000 | 上传 422 `REVIEW_FILE_INVALID`；草稿校验 error `POINTS_LIMIT_EXCEEDED`（可保存、阻止确认） |
| 用例 Review | 2000 条 / 10 MiB / 100 万字符 | 校验 error `CASES_LIMIT_EXCEEDED` / `CASE_REVIEW_SIZE_EXCEEDED`（可保存、阻止确认） |
| 等待队列 | 5 个排队任务 | 创建 409 `TASK_QUEUE_FULL`；入队 409 `QUEUE_FULL` |
| 产物保留 | 摘要 180 天、产物 90 天 | 过期后访问返回 410 `ARTIFACT_EXPIRED` |

**CAS 乐观锁协议**（functional 的 Review 草稿）：客户端每次提交携带 `{revision, sha256}`，服务端与当前草稿比较；不一致返回 409 冲突，`details` 携带 `current_revision`/`current_sha256`（前端应刷新后重试）。`sha256` 为规范化 JSON（键排序、紧凑分隔符）的 SHA-256。保存成功 `revision+1`；内容未变化时幂等返回当前草稿。

### 6.1 任务模型

**operation 取值（创建任务时必选其一）**

| operation | 含义 | 终态路径 |
| --- | --- | --- |
| `decompose_requirement` | 需求拆解（结构化拆解需求文档） | 直接 `succeeded` |
| `generate_test_points` | 生成测试点 | 停在 `waiting_review` 等确认 |
| `generate_test_cases` | 由测试点直接生成用例（**必须上传 `test_points_file`**） | `waiting_case_review` 或 `succeeded` |
| `full_pipeline` | 测试点 + 用例全流程 | 先停 `waiting_review`，确认后继续 |

**任务状态机**

```
pending(queued) → running → succeeded / failed / cancelled
running → waiting_review（等待测试点确认）
running → waiting_case_review（等待用例确认）
waiting_review → resume → pending → running（继续生成用例）
waiting_review / waiting_case_review → AI 子任务（pending → running）→ 回到原等待状态
waiting_case_review → case-review/confirm → succeeded(case_review_published)
pending / waiting_* → cancel → cancelled
```

终态集合：`succeeded`、`failed`、`cancelled`（另有 `partial_success` 属 api-test-agent 语义）。服务重启会把运行中主任务恢复为 `failed`（`WORKER_INTERRUPTED`），AI 子任务恢复为对应等待状态。

**public_task 载荷**（任务列表/详情/创建响应统一结构，白名单序列化，内部字段不外泄）：

```jsonc
{
  "schema_version": 1,
  "id": "task_20260812_a1b2c3",
  "agent_type": "functional",
  "operation": "full_pipeline",
  "status": "waiting_review",          // 状态机取值
  "stage": "waiting_for_review",       // 细粒度阶段
  "created_by_user_id": "u_1001",
  "created_by_username": "tester01",
  "project_name": "个人中心",
  "module_name": "历史记录",
  "environment": "prod",
  "created_at": "2026-08-12T02:11:09+00:00",
  "started_at": "...", "finished_at": "...",
  "config_release_id": "rel_...", "config_release_version": 3,
  "model_name": "...", "prompt_bundle_sha256": "...",
  "result_summary": {},
  "error_code": null, "error_message": null,   // failed 时填充
  "artifacts_expire_at": "...", "artifacts_expired": false,
  "review": {"version": 1, "sha256": "...", "confirmed_at": "...", "test_point_count": 128},
  "review_draft": {"revision": 3, "content_sha256": "...", "saved_by_username": "tester01", "saved_at": "..."},
  "review_ai": {"status": "ready", "operation": "supplement", "suggestion_count": 5, "...": "..."},
  "case_review": {"version": 1, "sha256": "...", "confirmed_at": "...", "test_case_count": 210},
  "case_review_draft": {"revision": 2, "...": "..."},
  "case_review_ai": {}, "completed_stages": []
  // 其余可选字段：queued_at、resume_requested_at、cancel_requested_at、
  // review_source、case_review_source、config_history、current_versions 等
}
```

### 6.2 基础接口（与 api-test-agent 完全共用，路径前缀替换即可）

#### 6.2.1 GET `/functional-test-agent/health` — 健康检查

无需登录。200：`{"status": "ok", "service": "functional-test-agent"}`。

#### 6.2.2 GET `/functional-test-agent/api/v1/readiness` — 就绪检查

权限 `tool.view`。

```jsonc
// 200
{
  "status": "ready",                    // ready / not_ready
  "storage_writable": true,
  "release_id": "rel_xxx",
  "llm_secret_configured": true,        // LLM_API_KEY 是否已配置（false 时任务会失败）
  "environment": "prod",
  "online_review_enabled": true,        // 以下四项为 functional 的 Release 开关
  "review_ai_enabled": true,
  "online_case_review_enabled": true,
  "case_review_ai_enabled": true
}
```

配置读取失败 → 503 `{"status":"not_ready","storage_writable":...,"configuration_available":false}`。

#### 6.2.3 页面路由

| 路由 | 权限 | 说明 |
| --- | --- | --- |
| `GET /functional-test-agent/` | `tool.view` | 首页：任务列表（前 20 条）与创建入口 |
| `GET /functional-test-agent/tasks/<task_id>` | `tool.result.view` | 任务详情页；测试点/用例 Review 工作区内嵌于此页（非独立路由）。404 `TASK_NOT_FOUND` |

#### 6.2.4 POST `/functional-test-agent/api/v1/tasks` — 创建任务

权限 `tool.execute` + CSRF。`multipart/form-data`，成功返回 **202** + `public_task`（此时 `status=pending`、`stage=queued`）。

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `operation` | 是 | 见 6.1；非法值 422 `INVALID_INPUT` |
| `project_name` / `module_name` | 是 | 各 1～128 字符 |
| `environment` | 否 | 缺省为部署环境；与部署环境不一致 422 `INVALID_INPUT` |
| `additional_context` | 否 | 补充说明，≤4000 字符 |
| `feature_name` | 否 | 截取前 128 字符 |
| `document_file` | 二选一 | 需求文档，允许 `.md`/`.txt`；与 `document_text` 二选一 |
| `document_text` | 二选一 | 直接粘贴文档文本 |
| `test_points_file` | 条件必填 | 仅 `operation=generate_test_cases` 允许且必填；`.json`，≤5000 条，每条含 `test_point` 字段，否则 422 `REVIEW_FILE_INVALID` |

```bash
curl -X POST 'http://170.106.159.147:41873/functional-test-agent/api/v1/tasks' \
  -H "X-CSRF-Token: $CSRF" -b cookies.txt \
  -F 'operation=full_pipeline' \
  -F 'project_name=个人中心' -F 'module_name=历史记录' \
  -F 'document_file=@./历史记录需求功能.md'
```

**主要错误**：422 `INVALID_INPUT`（参数/文件校验）、422 `UNSUPPORTED_DOCUMENT`（扩展名不允许）、422 `REVIEW_FILE_INVALID`（测试点 JSON 非法）、409 `TASK_QUEUE_FULL`（排队已满 5 个）、413 `INVALID_INPUT`（超大小限制）。

注意：创建接口**不支持** `Idempotency-Key`（该头仅用于 §6.3.5 / §6.4.5 / §6.4.6）。

#### 6.2.5 GET `/functional-test-agent/api/v1/tasks` — 任务列表

权限 `tool.result.view`。Query：`page`（默认 1）、`page_size`（默认 20，1～100）、`status`、`operation`（可选过滤）。

```jsonc
// 200
{"items": [ /* public_task */ ], "total": 37, "page": 1, "page_size": 20}
```

#### 6.2.6 GET `/functional-test-agent/api/v1/tasks/<task_id>` — 任务详情

权限 `tool.result.view` + 所有权。200：`public_task` 附加：

- `artifacts`：产物注册表数组，条目结构 `{id, type, name, size, sha256, stage, created_at, review_input, expired}`；
- `can_cancel`：非终态时为 true；
- `can_resume`：status 为 `waiting_review` 时为 true。

#### 6.2.7 POST `/functional-test-agent/api/v1/tasks/<task_id>/cancel` — 取消任务

权限 `task.cancel` + CSRF。

- 终态任务 → 409 `INVALID_TASK_STATE`；
- `pending`/`waiting_*` 直接置 `cancelled`（响应 200）；`running` 终止进程组（响应 202，记录可能短暂仍为 running）。

#### 6.2.8 GET `/functional-test-agent/api/v1/tasks/<task_id>/logs` — 日志轮询

权限 `tool.result.view` + 所有权。**无 SSE，用本接口按字节偏移增量轮询**。

Query：`cursor`（字节偏移，默认 0）、`limit`（默认 65536，钳制 1～262144）。

```jsonc
// 200
{
  "content": "<UTF-8 日志文本，已脱敏>",
  "next_cursor": 131072,    // 下次轮询直接传此值续读
  "truncated": true,        // true 表示还有剩余日志
  "complete": false         // status ∈ {succeeded, failed, cancelled, waiting_review} 时 true
}
```

410 `ARTIFACT_EXPIRED`（产物已过期）；404 `TASK_NOT_FOUND`。

#### 6.2.9 产物接口

| 接口 | 权限 | 说明 |
| --- | --- | --- |
| `GET /api/v1/tasks/<task_id>/artifacts` | `tool.result.view` | 产物列表 `{"items":[...]}`；410 `ARTIFACT_EXPIRED` |
| `GET /api/v1/tasks/<task_id>/artifacts/<artifact_id>` | `tool.result.view` | 附件下载（只接受注册表内 ID）；404 `ARTIFACT_NOT_READY`；410 `ARTIFACT_EXPIRED` |

### 6.3 测试点 Review（仅 functional，任务处于 `waiting_review` 时使用）

#### 6.3.1 GET `/functional-test-agent/api/v1/tasks/<task_id>/review` — 加载 Review 工作区数据

权限 `tool.result.view` + 所有权。前置 `ONLINE_REVIEW_ENABLED`（Release 开关），否则 403 `FEATURE_DISABLED`。

```jsonc
// 200
{
  "points": [ /* 当前草稿测试点；无草稿时为原稿 */ ],
  "original_points": [ /* 模型原稿 */ ],
  "revision": 3,                    // 无草稿为 0
  "sha256": "<规范化内容 SHA-256>",
  "source_artifact_id": "...",
  "saved_at": "...", "saved_by_username": "tester01",
  "validation": {"valid_for_resume": true, "errors": [], "warnings": []},
  "diff_summary": {"added": 2, "modified": 1, "deleted": 0, "unchanged": 125, "risk_changed": 1},
  "editable": true,                 // waiting_review 且有 tool.execute
  "task_status": "waiting_review", "stage": "waiting_for_review",
  "review_ai": { /* AI 元数据 */ }, "review_ai_enabled": true
}
```

错误：404 `REVIEW_DRAFT_REQUIRED`（原稿产物缺失）、410 `ARTIFACT_EXPIRED`。

#### 6.3.2 PUT `/functional-test-agent/api/v1/tasks/<task_id>/review-draft` — 保存测试点草稿

权限 `tool.execute` + CSRF。前置状态 `waiting_review`（否则 409 `INVALID_TASK_STATE`）。

请求体（JSON）：`points`（数组）、`revision`、`sha256`（CAS 基准，见 6.0）。

```bash
curl -X PUT 'http://170.106.159.147:41873/functional-test-agent/api/v1/tasks/task_xxx/review-draft' \
  -H 'Content-Type: application/json' -H "X-CSRF-Token: $CSRF" -b cookies.txt \
  -d '{"points": [ {"test_point": "...", "risk_level": "high"} ], "revision": 3, "sha256": "ab12..."}'
```

- 200：与 GET review 相同结构（新 revision/sha256/validation/diff_summary）。
- 409 `REVIEW_REVISION_CONFLICT`：CAS 冲突，`details` 含 `current_revision`/`current_sha256`/`saved_at`/`saved_by`。
- 422 `REVIEW_FILE_INVALID`：`points` 非数组。
- 校验存在 error 时**仍允许保存**，只阻止后续 resume（`valid_for_resume=false`）；超过 5MiB/50 万字符记为校验错误 `REVIEW_SIZE_EXCEEDED`。

#### 6.3.3 POST `/functional-test-agent/api/v1/tasks/<task_id>/review-draft/import` — 导入 JSON 为草稿

权限/前置同 6.3.2。multipart：`review_file`（必填，`.json`，顶层为测试点数组或含 `test_points`/`test_point`/`points` 包装），可选表单 `revision`、`sha256`（缺省用当前草稿值）。之后走相同 CAS 保存。错误：422 `REVIEW_FILE_INVALID`、409 `REVIEW_REVISION_CONFLICT`。

#### 6.3.4 GET `/functional-test-agent/api/v1/tasks/<task_id>/review/download` — 下载测试点

权限 `tool.result.view`。Query `kind`：

| kind | 文件 | 附加条件 |
| --- | --- | --- |
| `generated` | 模型原稿 `generated-test-points.json` | — |
| `draft`（默认） | 当前草稿 `review-draft.json` | — |
| `confirmed` | 确认版本 `review-test-points-v{version}.json` | 必须带 `version` 且等于任务已确认版本，否则 404 `ARTIFACT_NOT_READY` |

其他 kind → 422 `INVALID_INPUT`；过期 → 410 `ARTIFACT_EXPIRED`。

#### 6.3.5 POST `/functional-test-agent/api/v1/tasks/<task_id>/review-ai` — 触发 AI 辅助修改

权限 `tool.execute` + CSRF；前置 `waiting_review` 且 `ONLINE_REVIEW_ENABLED` + `REVIEW_AI_ENABLED`（否则 403 `FEATURE_DISABLED`）。

| 请求体字段 | 说明 |
| --- | --- |
| `revision` / `sha256` | 基准草稿（revision=0 → 422 `REVIEW_DRAFT_REQUIRED`；基准已变化 → 409 `REVIEW_AI_BASE_CHANGED`） |
| `operation` | `supplement`（补充）/ `rewrite_selected`（改写选中）/ `generate_from_instruction`（按指令生成） |
| `selected_ids` | `rewrite_selected` 必填，1～100 个测试点 ID |
| `scope` | `{modules:[...], features:[...]}` 作用域过滤 |
| `instruction` | `generate_from_instruction` 必填，≤2000 字符 |

头 `Idempotency-Key`：可选，8～128 字符。同 key 不同内容 → 409 `INVALID_INPUT`；同 key 同内容幂等返回（pending/running 时 202，否则 200）。

成功 **202** + `public_task`（任务转 `pending/review_ai_queued`，完成后回到 `waiting_review` 且 `review_ai.status=ready`）。队列满 → 409 `QUEUE_FULL`。

#### 6.3.6 GET `/functional-test-agent/api/v1/tasks/<task_id>/review-ai` — 查询 AI 结果

权限 `tool.result.view`。200：`review_ai` 元数据；`status=ready` 时附加 `summary`、`suggestions`、`rejected_suggestions`、`warnings`、`model_name`、`prompt_bundle_sha256`、`finished_at`。建议文件损坏 → 422 `REVIEW_AI_RESPONSE_INVALID`。

#### 6.3.7 POST `/functional-test-agent/api/v1/tasks/<task_id>/review-ai/cancel` — 取消 AI 子任务

权限 `task.cancel` + CSRF。非 AI 执行中状态 → 409 `INVALID_TASK_STATE`；成功后任务回到 `waiting_review`（running 时响应 202，否则 200）。

#### 6.3.8 POST `/functional-test-agent/api/v1/tasks/<task_id>/resume` — 确认测试点并继续生成用例

权限 `tool.execute` + CSRF；前置 `waiting_review`（否则 409 `INVALID_TASK_STATE`）。两种模式：

**JSON 模式**（确认在线草稿，`Content-Type: application/json`）：

```jsonc
{"revision": 3, "sha256": "ab12...", "accept_warnings": false}
```

**上传模式**（multipart）：`review_file`（`.json` 测试点文件，≤5MiB/50万字符/5000 条）。

确认逻辑：revision=0 → 422 `REVIEW_DRAFT_REQUIRED`；CAS 不匹配 → 409 `REVIEW_REVISION_CONFLICT`；存在阻塞 error → 422 `REVIEW_VALIDATION_FAILED`；有 warning 且 `accept_warnings=false` → 409 `REVIEW_WARNING_CONFIRMATION_REQUIRED`（details 携带 validation，前端提示后带 `accept_warnings=true` 重试）。

成功后生成不可变确认版本（相同内容复用已有版本），任务入队继续生成用例：**202** + `public_task`（回到 `pending/queued`）。队列满 → 409 `QUEUE_FULL`。

### 6.4 用例 Review（仅 functional，任务处于 `waiting_case_review` 时使用）

以下接口均需 `ONLINE_CASE_REVIEW_ENABLED`（Release 开关），否则 403 `FEATURE_DISABLED`。

#### 6.4.1 GET `/functional-test-agent/api/v1/tasks/<task_id>/case-review` — 加载用例 Review 数据

权限 `tool.result.view` + 所有权。200 结构：

```jsonc
{
  "cases": [ /* 当前草稿用例 */ ], "original_cases": [ /* 模型原稿 */ ],
  "revision": 2, "sha256": "...",
  "source_artifact_id": "...", "saved_at": "...", "saved_by_username": "...",
  "validation": {
    "valid_for_confirm": true, "errors": [], "warnings": [],
    "coverage": {"confirmed_test_points": 128, "covered_test_points": 120,
                 "uncovered_test_points": 8, "uncovered_ids": ["tp_101"]}
  },
  "diff_summary": {"added": 3, "modified": 2, "deleted": 1, "unchanged": 204, "priority_changed": 0},
  "confirmed_test_point_ids": ["tp_001"],
  "editable": true, "task_status": "waiting_case_review", "stage": "case_review_editing",
  "test_points": [ /* 确认测试点摘要：id/module/feature/scenario/test_point/risk_level */ ],
  "case_review_ai": {}, "case_review_ai_enabled": true
}
```

错误：404 `REVIEW_DRAFT_REQUIRED`、410 `ARTIFACT_EXPIRED`。

#### 6.4.2 PUT `/functional-test-agent/api/v1/tasks/<task_id>/case-review-draft` — 保存用例草稿

权限 `tool.execute` + CSRF；前置 `waiting_case_review`（否则 409 `INVALID_TASK_STATE`）。请求体：`cases`（数组，否则 422 `CASE_REVIEW_FILE_INVALID`）、`revision`、`sha256`。CAS 冲突 → 409 `CASE_REVIEW_REVISION_CONFLICT`（details 同测试点版）。限额：2000 条/10MiB/100 万字符（超限记校验错误，可保存、阻止确认）。200：load 全量结构 + 新 revision/sha256。

#### 6.4.3 POST `/functional-test-agent/api/v1/tasks/<task_id>/case-review-draft/import` — 导入用例 JSON

multipart：`review_file`（必填 `.json`，顶层为数组或含 `test_cases`/`cases` 包装），可选表单 `revision`、`sha256`。错误：422 `CASE_REVIEW_FILE_INVALID`、409 `CASE_REVIEW_REVISION_CONFLICT`。

#### 6.4.4 GET `/functional-test-agent/api/v1/tasks/<task_id>/case-review/download` — 下载用例

Query `kind`：`generated`（`generated-test-cases.json`）/ `draft`（`case-review-draft.json`）/ `confirmed`（须 `version` 匹配，`review-test-cases-v{version}.json`）。其余同 6.3.4。

#### 6.4.5 POST `/functional-test-agent/api/v1/tasks/<task_id>/case-review/confirm` — 确认并发布用例

权限 `tool.execute` + CSRF。头 `Idempotency-Key` **必填**（8～128 字符）。请求体：`revision`、`sha256`、`accept_warnings`。

幂等与冲突：

- 同 key 不同 `{revision, sha256}` → 409 `INVALID_INPUT`；
- 任务已 `succeeded` 且 key/内容完全匹配 → 200 幂等返回；
- 状态非 `waiting_case_review` → 409 `INVALID_TASK_STATE`；
- revision=0 → 422 `CASE_REVIEW_DRAFT_REQUIRED`；CAS 冲突 → 409 `CASE_REVIEW_REVISION_CONFLICT`；
- 有阻塞 error → 422 `CASE_REVIEW_VALIDATION_FAILED`；warning 未接受 → 409 `CASE_REVIEW_WARNING_CONFIRMATION_REQUIRED`。

成功 **200**：

```jsonc
{
  "task": { /* public_task，status=succeeded, stage=case_review_published */ },
  "case_review": {"version": 1, "sha256": "...", "confirmed_at": "...", "test_case_count": 210},
  "artifacts": [ /* test-cases.json 与 test-cases.xlsx 两个产物（stage=case_review_published） */ ]
}
```

#### 6.4.6 POST `/functional-test-agent/api/v1/tasks/<task_id>/case-review-ai` — 触发用例 AI 辅助

同 6.3.5，差异：基准错误码 `CASE_REVIEW_DRAFT_REQUIRED` / `CASE_REVIEW_AI_BASE_CHANGED`；`rewrite_selected` 上限 50 条；`Idempotency-Key` **必填**；需 `CASE_REVIEW_AI_ENABLED`。成功 202（`pending/case_review_ai_queued`）。

#### 6.4.7 GET `/functional-test-agent/api/v1/tasks/<task_id>/case-review-ai` — 查询用例 AI 结果

同 6.3.6；建议文件损坏 → 422 `CASE_REVIEW_AI_RESPONSE_INVALID`。

#### 6.4.8 POST `/functional-test-agent/api/v1/tasks/<task_id>/case-review-ai/cancel` — 取消用例 AI 子任务

同 6.3.7；回到 `waiting_case_review`。

### 6.5 异步任务错误码（记录在 public_task 的 `error_code` 字段）

| 错误码 | 含义 |
| --- | --- |
| `CONFIG_NOT_READY` | LLM API Key 未配置（任务 `failed/configuration`） |
| `TASK_TIMEOUT` | 主任务超时（默认 3600s） |
| `LLM_TIMEOUT` | AI 子任务超时（默认 600s） |
| `LLM_AUTH_FAILED` / `LLM_RATE_LIMITED` / `LLM_RESPONSE_INVALID` | 模型调用失败归类 |
| `QUALITY_GATE_FAILED` | 质量门禁未通过 |
| `WORKER_FAILED` / `WORKER_START_FAILED` | Runner 执行/启动失败 |
| `WORKER_INTERRUPTED` | 服务重启导致中断 |
| `WORKFLOW_FAILED` | 工作流失败兜底 |

### 6.6 functional HTTP 错误码速查

| 错误码 | HTTP | 场景 |
| --- | --- | --- |
| `AUTH_REQUIRED` | 401 | 缺 `X-Platform-User-ID` |
| `PERMISSION_DENIED` | 403 | 缺权限点 |
| `CSRF_INVALID` | 403 | CSRF 校验失败 |
| `FEATURE_DISABLED` | 403 | 在线 Review/AI 开关未启用 |
| `TASK_NOT_FOUND` | 404 | 任务不存在/非本人/functional 专属路由被 api agent 访问 |
| `REVIEW_DRAFT_REQUIRED` | 404/422 | 原稿缺失（404）；未保存过草稿就确认/触发 AI（422） |
| `ARTIFACT_NOT_READY` | 404 | 确认版本不匹配/产物未登记 |
| `ARTIFACT_EXPIRED` | 410 | 产物过期（90 天） |
| `INVALID_TASK_STATE` | 409 | 当前状态不允许该操作 |
| `TASK_QUEUE_FULL` / `QUEUE_FULL` | 409 | 创建/入队时队列已满（5） |
| `REVIEW_REVISION_CONFLICT` / `CASE_REVIEW_REVISION_CONFLICT` | 409 | 草稿 CAS 冲突 |
| `REVIEW_WARNING_CONFIRMATION_REQUIRED` / `CASE_REVIEW_WARNING_CONFIRMATION_REQUIRED` | 409 | warning 需显式接受 |
| `REVIEW_AI_BASE_CHANGED` / `CASE_REVIEW_AI_BASE_CHANGED` | 409 | AI 基准草稿已变化 |
| `INVALID_INPUT` | 422/413 | 参数、文件、Idempotency-Key 校验失败；超大小限制 |
| `UNSUPPORTED_DOCUMENT` | 422 | 文档扩展名不允许 |
| `REVIEW_FILE_INVALID` / `CASE_REVIEW_FILE_INVALID` | 422 | 测试点/用例 JSON 结构非法 |
| `REVIEW_VALIDATION_FAILED` / `CASE_REVIEW_VALIDATION_FAILED` | 422 | 确认时存在阻塞校验错误 |
| `REVIEW_AI_RESPONSE_INVALID` / `CASE_REVIEW_AI_RESPONSE_INVALID` | 422 | AI 建议文件损坏/超限 |
| `REVIEW_AI_SCOPE_REQUIRED` / `CASE_REVIEW_AI_SCOPE_REQUIRED` | 422 | AI 作用域过大（测试点>500 / 用例>300） |
| `CASE_REFERENCE_INVALID` | 422 | 用例引用了不在确认版本的测试点 |
| `STORAGE_WRITE_FAILED` | 500 | 存储完整性校验失败 |
| `CONFIG_NOT_READY` | 503 | readiness 场景下配置服务不可用（任务异步失败见 6.5） |

## 7. api-test-agent（API 测试智能体）

内部端口 5005，网关前缀 `/api-test-agent`。解析 API 文档（OpenAPI/Swagger/非结构化 HTTP 接口文本）为可追溯的接口契约，再经结构化覆盖生成基础测试用例与可执行用例，支持契约/用例人工 Review、阶段重试、受控真实执行与本地 Bug 草稿。

要点：

- **确定性解析优先**：`require_llm_config=False`，OpenAPI/Swagger 文档无需 LLM 配置即可完成全流程；非结构化文本在 Runner 内可能调用模型增强，失败时映射为 `LLM_*` 任务错误码。
- **V2 阶段流水线**：`document_preflight → 契约 Review → base_case_generation → 用例 Review → executable_generation → execution_ready`，中间有两次人工 Review 停顿；各阶段均为异步（202 + 轮询任务状态）。
- **版本化产物**：contracts / base-cases / coverage / executable-cases / defect-drafts 均按递增版本保存（`v1.json`、`v2.json`…），并自动登记为可下载产物。
- **真实执行默认关闭**：仅当 `API_EXECUTION_ENABLED=true` 且运行环境非 prod 时启用；执行目标须由管理员预先登记，预览与响应中目标 URL 一律脱敏，契约与可执行用例不保存真实 Host。
- 共用约定（错误体、身份、CSRF、限额、日志轮询协议、产物）见 §6.0；基础任务接口（health/readiness/创建/列表/详情/cancel/logs/artifacts）见 §6.2，本章只描述差异与 V2 专属接口。

### 7.1 基础接口与 functional-test-agent 的差异

**operation 取值**（创建任务表单字段 `operation`）

| operation | 含义 |
| --- | --- |
| `parse_api_document` | 解析 API 文档并生成文件化测试用例 |
| `generate_api_cases` | 生成 API 测试用例 |

> 两个操作当前进入同一条 V2 阶段流水线（从 `document_preflight` 开始）。提交 `execute_api_cases` 或 `full_pipeline` 会直接返回 **403 `FEATURE_DISABLED`**「API 真实执行在 MVP 中未启用」。

**上传格式**：`document_file` 支持 `.md`、`.txt`、`.json`、`.yaml`、`.yml`（或粘贴 `document_text`，二选一）；大小限制见 §6.0。`test_points_file` 仅 functional agent 可用，在本工具上传返回 422 `INVALID_INPUT`。

**创建任务示例**

```bash
curl -X POST "http://170.106.159.147:41873/api-test-agent/api/v1/tasks" \
  -H "X-CSRF-Token: $TP_CSRF" \
  --cookie "tp_csrf=$TP_CSRF" \
  -F "operation=parse_api_document" \
  -F "project_name=电商平台" \
  -F "module_name=订单模块" \
  -F "feature_name=创建订单接口" \
  -F "additional_context=重点关注库存扣减场景" \
  -F "document_file=@openapi.yaml"
```

202 响应为 `public_task` 对象（字段定义见 §6.1），后续用 `GET /api-test-agent/api/v1/tasks/<task_id>` 与 `logs?cursor=` 跟踪。

**其他差异**

| 项 | 行为 |
| --- | --- |
| 任务详情 `can_resume` | 恒为 `false`（`resume` 接口为 functional 专属，本工具返回 404） |
| functional 专属 Review 接口 | 在本工具一律失败关闭返回 404 `TASK_NOT_FOUND`：`/review`、`/review-draft`(PUT)、`/review-draft/import`、`/review/download`、`/review-ai`(POST/GET)、`/review-ai/cancel`、`/resume`、`/case-review`、`/case-review-draft`(PUT)、`/case-review-draft/import`、`/case-review/download`、`/case-review/confirm`、`/case-review-ai`(POST/GET)、`/case-review-ai/cancel`。本工具 Review 数据请改用 §7.3 / §7.4 |
| readiness 中 review 字段 | `online_review_enabled` / `review_ai_enabled` / `online_case_review_enabled` / `case_review_ai_enabled` 恒为 `false`；`llm_secret_configured` 仅供参考（不阻断创建任务） |

### 7.2 V2 任务状态机

```
pending → running → waiting_contract_review   （文档解析完成，等待契约 Review）
running → waiting_case_review                 （基础用例生成完成，等待用例 Review）
running → succeeded / partial_success         （可执行用例生成完成）
running → failed / cancelled
waiting_contract_review → pending             （提交 cases/generate 后） / cancelled
waiting_case_review → pending                 （提交 executable-cases/generate 后） / cancelled
failed → pending                              （提交阶段重试后）
succeeded / partial_success / cancelled → 终态
```

> `waiting_execution_confirmation` 已在状态机中定义，但当前版本没有任何代码路径进入该状态，可忽略。

**阶段与状态的对应关系**

| 阶段 stage | 触发方式 | 成功后的任务状态 | 阶段结果标识 |
| --- | --- | --- | --- |
| `document_preflight` | 创建任务后自动执行 | `waiting_contract_review` | `contract_review` |
| `base_case_generation` | `POST .../cases/generate` | `waiting_case_review` | `case_review` |
| `executable_generation` | `POST .../executable-cases/generate` | 全部 ready → `succeeded`；存在 disabled → `partial_success` | `execution_ready` / `static_validation_failed` |

阶段失败后任务停在 `failed`，可用 `POST .../retry`（§7.5）从失败阶段重新入队。

### 7.3 契约 Review 接口

#### 7.3.1 GET `/api/v1/tasks/<task_id>/contracts` — 获取当前契约版本

权限 `tool.result.view`。返回最新 contracts 版本信封 + 任务状态。

响应示例（节选）：

```jsonc
{
  "schema_version": 2,
  "kind": "contracts",
  "version": 3,                       // 当前契约版本号，即 Review 提交的 base_version
  "sha256": "9f2c...(items 内容 SHA-256)",
  "source_versions": { "contracts": 2 },
  "created_by": "u_1001",
  "created_at": "2026-08-13T06:30:00+00:00",
  "items": [
    {
      "contract_id": "ct_a1b2c3...",
      "version": 1,
      "name": "创建订单",
      "summary": "创建普通商品订单",
      "method": "POST",
      "path": "/api/v1/orders",            // 恒为相对路径，执行目标由 target_id 决定
      "sla_ms": 500,                        // 文档未声明时为 null
      "servers": [ { "url": "...", "description": "仅文档声明，不作为执行目标" } ],
      "parameters": [
        { "name": "Authorization", "location": "header", "required": true,
          "description": "Bearer token", "schema": { "type": "string" }, "example": null }
      ],
      "request_body": { "required": true, "content": { "application/json": { "type": "object" } } },
      "responses": [ { "status_code": "200", "description": "成功", "content": {} } ],
      "security": [ { "scheme": "bearerAuth", "scopes": [] } ],
      "dependencies": [ { "contract_id": "ct_x...", "variable": "sku_id", "source_path": "..." } ],
      "source_trace": { "source_id": "输入文档 SHA-256", "section_id": "openapi:/paths/~1api~1v1~1orders", "quote": "" },
      "field_evidence": [
        { "field_path": "method", "value": "POST", "source_type": "openapi_node",
          "source_pointer": "/paths/~1api~1v1~1orders/post", "quote": "",
          "evidence_type": "explicit", "confidence": 1.0 }
      ],
      "unresolved": [],              // ReviewIssue 数组：缺失信息
      "ambiguity_notes": [],         // 歧义
      "conflict_items": [],          // 冲突
      "test_design_suggestions": [],
      "quality_report": {
        "quality_score": 0.92, "hard_gate_passed": true, "evidence_rate": 0.95,
        "grounding_passed": true, "unsupported_facts": 0, "blockers": []
      },
      "status": "confirmed_candidate",     // draft/confirmed_candidate/confirmed/changed/deprecated
      "change_history": []
    }
  ],
  "task_status": "waiting_contract_review"
}
```

错误码：404 `TASK_NOT_FOUND`（任务不存在 / 非 V2 任务 / 无权访问）。

#### 7.3.2 PUT `/api/v1/tasks/<task_id>/contracts/review` — 提交契约 Review

权限 `api-test-agent.contract.review` + CSRF。前置条件：任务状态为 `waiting_contract_review`，否则 409 `INVALID_TASK_STATE`。

请求体：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `base_version` | int | 读取时的契约版本号（整数乐观锁）；与当前版本不一致返回 409 `REVIEW_VERSION_CONFLICT` |
| `changes` | array | 批量变更，每项 `{contract_id, action, fields?, reason?}`，按数组顺序逐条应用 |

**action 取值**

| action | 含义 | 约束 |
| --- | --- | --- |
| `edit` | 编辑契约 | `fields` 仅可含 `name`、`summary`、`method`、`path`、`parameters`、`request_body`、`responses`、`security`、`dependencies`、`test_design_suggestions`；编辑后契约回到 `draft` 并重跑质量门禁；被改字段的原证据与未决问题清除，追加 `human_override` 人工证据 |
| `confirm` | 确认契约 | 要求 `quality_report.hard_gate_passed=true` 且状态 ∈ {`confirmed_candidate`, `confirmed`}，否则 409 `CONTRACT_QUALITY_BLOCKED` |
| `deprecate` | 废弃契约 | 状态变为 `deprecated` |
| `return` | 退回契约 | 状态回到 `draft`，未填 reason 时默认「人工退回」 |

请求示例：

```bash
curl -X PUT "http://170.106.159.147:41873/api-test-agent/api/v1/tasks/task_xxx/contracts/review" \
  -H "X-CSRF-Token: $TP_CSRF" \
  -H "Content-Type: application/json" \
  --cookie "tp_csrf=$TP_CSRF" \
  -d '{
    "base_version": 3,
    "changes": [
      { "contract_id": "ct_a1b2c3", "action": "edit",
        "fields": { "summary": "创建普通商品订单（含库存校验）" }, "reason": "补充摘要" },
      { "contract_id": "ct_d4e5f6", "action": "confirm" },
      { "contract_id": "ct_g7h8i9", "action": "deprecate", "reason": "接口已下线" }
    ]
  }'
```

成功响应 200，返回新的契约版本信封（结构同 §7.3.1，`version` +1，`source_versions` 记录本次 base_version）。

错误码：409 `REVIEW_VERSION_CONFLICT` / `INVALID_TASK_STATE` / `CONTRACT_QUALITY_BLOCKED`；422 `CONTRACT_NOT_FOUND`（changes 含不存在的契约）/ `REVIEW_PATCH_INVALID`（编辑字段为空）/ `REVIEW_FIELD_FORBIDDEN`（字段不在白名单）/ `REVIEW_ACTION_UNSUPPORTED`（action 不受支持）。

#### 7.3.3 POST `/api/v1/tasks/<task_id>/cases/generate` — 生成基础用例

权限 `api-test-agent.case.review` + CSRF。前置条件：

- 任务状态 `waiting_contract_review`（否则 409 `INVALID_TASK_STATE`）；
- 当前契约版本至少有一个 `status=confirmed` 的契约（否则 409 `CONTRACT_NOT_CONFIRMED`）。

```bash
curl -X POST "http://170.106.159.147:41873/api-test-agent/api/v1/tasks/task_xxx/cases/generate" \
  -H "X-CSRF-Token: $TP_CSRF" --cookie "tp_csrf=$TP_CSRF"
```

202 响应为 `public_task`（任务回到 `pending`，stage=`base_case_generation_queued`）。队列已满时返回 409 `QUEUE_FULL`。

### 7.4 用例 Review 接口

#### 7.4.1 GET `/api/v1/tasks/<task_id>/cases` — 获取当前用例版本与覆盖矩阵

权限 `tool.result.view`。响应 = base-cases 版本信封 + `coverage`（覆盖矩阵版本信封）+ `task_status`。

响应示例（节选）：

```jsonc
{
  "kind": "base-cases",
  "version": 2,
  "sha256": "...",
  "source_versions": { "contracts": 3 },
  "items": [
    {
      "case_id": "case_9f8e7d6c5b...",
      "version": 1,
      "contract_id": "ct_a1b2c3",
      "name": "必填参数完整创建订单成功",
      "objective": "验证必填参数合法时订单创建成功并返回单号",
      "dimension": "正常",
      "risk_level": "low",              // low/medium/high
      "preconditions": [ "用户已登录" ],
      "steps": [ { "action": "调用 POST /api/v1/orders", "data": { "sku": "A-1" } } ],
      "expected_results": [ "响应 200 且返回订单号" ],
      "source": "deterministic",         // deterministic/document/llm/human
      "status": "confirmed_candidate",   // draft/confirmed_candidate/confirmed/disabled
      "disabled_reason": "",
      "change_history": []
    }
  ],
  "coverage": {
    "kind": "coverage",
    "version": 1,
    "items": {
      "version": 1, "contract_version": 3, "round_count": 1,
      "items": [
        { "coverage_id": "cov_001", "contract_id": "ct_a1b2c3", "dimension": "必填参数",
          "rule": "必填参数需存在正常用例", "required": true, "covered": true,
          "case_ids": [ "case_9f8e7d6c5b" ], "decision_source": "deterministic",
          "confidence": 1.0, "gap_reason": "" }
      ],
      "accepted_gap_ids": [], "partial_success": false
    }
  },
  "task_status": "waiting_case_review"
}
```

#### 7.4.2 PUT `/api/v1/tasks/<task_id>/cases/review` — 提交用例 Review

权限 `api-test-agent.case.review` + CSRF。前置条件：任务状态 `waiting_case_review`，否则 409 `INVALID_TASK_STATE`。乐观锁与契约相同（`base_version` 不一致返回 409 `REVIEW_VERSION_CONFLICT`）。

**action 取值**

| action | 含义 | 约束 |
| --- | --- | --- |
| `add` | 新增用例 | `fields` 必须包含 `contract_id` 且仅含可编辑字段（白名单见 edit）；`risk_level=high` 时新用例状态为 `draft`，否则 `confirmed_candidate`；`source` 记为 `human` |
| `edit` | 编辑用例 | `fields` 白名单：`name`、`objective`、`dimension`、`risk_level`、`preconditions`、`steps`、`expected_results`、`disabled_reason`；编辑后 `source` 变为 `human`，状态重置（high → `draft`，其余 → `confirmed_candidate`） |
| `confirm` | 确认用例 | 高风险用例（`risk_level=high`）确认时要求调用者同时持有 `api-test-agent.execute` 权限，否则 403 `HIGH_RISK_PERMISSION_REQUIRED`；用例状态须 ∈ {`draft`, `confirmed_candidate`, `confirmed`}，否则 409 `CASE_REVIEW_BLOCKED` |
| `disable` | 禁用用例 | 状态变为 `disabled`，`disabled_reason` 取 `reason`，未填默认「人工禁用」 |

请求示例：

```bash
curl -X PUT "http://170.106.159.147:41873/api-test-agent/api/v1/tasks/task_xxx/cases/review" \
  -H "X-CSRF-Token: $TP_CSRF" \
  -H "Content-Type: application/json" \
  --cookie "tp_csrf=$TP_CSRF" \
  -d '{
    "base_version": 2,
    "changes": [
      { "action": "add", "fields": {
          "contract_id": "ct_a1b2c3", "name": "非法 SKU 创建订单",
          "objective": "验证无效 SKU 被拒绝", "dimension": "异常",
          "risk_level": "medium", "preconditions": [], "steps": [],
          "expected_results": [ "响应 400 并返回错误码" ] } },
      { "action": "edit", "case_id": "case_9f8e7d6c5b",
        "fields": { "objective": "更新后的目标描述" }, "reason": "优化措辞" },
      { "action": "confirm", "case_id": "case_9f8e7d6c5b" },
      { "action": "disable", "case_id": "case_0001112222", "reason": "与既有用例重复" }
    ]
  }'
```

成功响应 200，返回新的 base-cases 版本信封（`version` +1）。

错误码：409 `REVIEW_VERSION_CONFLICT` / `INVALID_TASK_STATE` / `CASE_REVIEW_BLOCKED`；403 `HIGH_RISK_PERMISSION_REQUIRED`；422 `CASE_NOT_FOUND` / `REVIEW_PATCH_INVALID` / `REVIEW_FIELD_FORBIDDEN` / `REVIEW_ACTION_UNSUPPORTED`。

#### 7.4.3 POST `/api/v1/tasks/<task_id>/executable-cases/generate` — 生成可执行用例

权限 `api-test-agent.case.review` + CSRF。前置条件：任务状态 `waiting_case_review`（否则 409 `INVALID_TASK_STATE`）；当前用例版本至少有一个 `confirmed` 用例（否则 409 `CASE_NOT_CONFIRMED`）。

```bash
curl -X POST "http://170.106.159.147:41873/api-test-agent/api/v1/tasks/task_xxx/executable-cases/generate" \
  -H "X-CSRF-Token: $TP_CSRF" --cookie "tp_csrf=$TP_CSRF"
```

202 响应为 `public_task`（任务回到 `pending`，stage=`executable_generation_queued`）。执行完成后：全部用例通过静态校验 → 任务 `succeeded`（stage=`execution_ready`）；存在被禁用用例 → `partial_success`（stage=`static_validation_failed`）。可执行用例不含真实 Host，可通过产物接口下载（类型 `api_v2_executable-cases`）。

### 7.5 POST `/api/v1/tasks/<task_id>/retry` — 阶段重试

权限 `tool.execute` + CSRF。仅允许任务状态为 `failed` 时调用，否则 409 `INVALID_TASK_STATE`。

请求体：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `stage` | string | `document_preflight` / `base_case_generation` / `executable_generation` 三者之一，否则 422 `RETRY_STAGE_UNSUPPORTED` |
| `source_versions` | object | 可选，显式指定复用的上游版本，如 `{"contracts": 3, "base-cases": 2}` |

```bash
curl -X POST "http://170.106.159.147:41873/api-test-agent/api/v1/tasks/task_xxx/retry" \
  -H "X-CSRF-Token: $TP_CSRF" \
  -H "Content-Type: application/json" \
  --cookie "tp_csrf=$TP_CSRF" \
  -d '{ "stage": "base_case_generation", "source_versions": { "contracts": 3 } }'
```

202 响应为 `public_task`（任务回到 `pending`，stage=`<stage>_queued`）。队列已满时返回 409 `QUEUE_FULL`。

### 7.6 执行确认与执行 Run

> 真实执行需要平台显式开启 `API_EXECUTION_ENABLED=true`（且运行环境非 prod）并登记执行目标，否则执行相关请求一律 403 `EXECUTION_NOT_READY`。Web 侧不保存也不返回真实目标 Host，目标 URL 仅以脱敏形式展示。

#### 7.6.1 GET `/api/v1/tasks/<task_id>/execute/preview` — 执行预览（确认摘要）

权限 `tool.result.view`。返回稳定的确认摘要；客户端执行时必须回传本接口返回的 `confirmation_sha256`。

```jsonc
{
  "task_id": "task_xxx",
  "executable_version": 1,
  "executable_sha256": "...",
  "ready_case_ids": [ "xc_aaa", "xc_bbb" ],    // enabled 且静态校验通过的可执行用例
  "write_case_count": 1,                        // POST/PUT/PATCH/DELETE 用例数
  "high_risk_count": 0,
  "script_count": 0,                            // 含 setup/teardown 脚本的用例数
  "target_id": "staging-s1",
  "target": "https://staging-***.example.com",  // 已脱敏
  "execution_enabled": true,
  "blocking_reasons": [],                       // 非空表示不允许执行
  "confirmation_sha256": "ab12...(上述摘要规范化 JSON 的 SHA-256)"
}
```

`blocking_reasons` 可能取值：「API_EXECUTION_ENABLED=false 或当前环境为生产环境」「未登记执行目标」「没有通过静态校验的可执行用例」「目标未授权写操作」（目标不允许写方法但存在写操作用例时）。

#### 7.6.2 POST `/api/v1/tasks/<task_id>/execute` — 创建执行 Run

权限 `api-test-agent.execute` + CSRF。请求体：`{ "target_id": "可选，缺省用预览中的目标", "confirmation_sha256": "必填" }`。

**门禁检查顺序**（全部在创建 Run 之前拒绝）：

| 顺序 | 检查项 | 失败错误码 |
| --- | --- | --- |
| 1 | 执行运行时已启用（`API_EXECUTION_ENABLED=true` 且非 prod） | 403 `EXECUTION_NOT_READY` |
| 2 | 预览 `blocking_reasons` 为空 | 403 `EXECUTION_NOT_READY` |
| 3 | `target_id` 已登记 | 403 `EXECUTION_TARGET_DENIED` |
| 4 | `confirmation_sha256` 与最新预览一致 | 409 `EXECUTION_CONFIRMATION_STALE` |
| 5 | 存在 enabled 且 ready 的可执行用例 | 409 `EXECUTABLE_CASE_NOT_READY` |

```bash
curl -X POST "http://170.106.159.147:41873/api-test-agent/api/v1/tasks/task_xxx/execute" \
  -H "X-CSRF-Token: $TP_CSRF" \
  -H "Content-Type: application/json" \
  --cookie "tp_csrf=$TP_CSRF" \
  -d '{ "target_id": "staging-s1", "confirmation_sha256": "ab12..." }'
```

202 响应为 ExecutionRun 对象。执行通过独立执行 Controller 同步完成，返回时可能已是终态：

```jsonc
{
  "run_id": "run_3f9c2a8b1e...",
  "task_id": "task_xxx",
  "executable_case_version": 1,
  "environment": "test",
  "target_id": "staging-s1",
  "resolved_base_url_masked": "",
  "status": "succeeded",      // 状态机：created→validating→provisioning→running→reporting→succeeded
                              // validating/provisioning/running 可转 failed/cancelled/timed_out
  "created_by": "u_1001",
  "confirmed_by": "u_1001",
  "confirmation_sha256": "ab12...",
  "config_release_id": "rel_20260801",
  "created_at": "2026-08-13T07:00:00+00:00",
  "started_at": "2026-08-13T07:00:01+00:00",
  "finished_at": "2026-08-13T07:00:09+00:00",
  "summary": { "total": 2, "passed": 2, "failed": 0, "classifications": { "none": 2 } },
  "error_code": null,          // 失败时如 EXECUTION_TIMEOUT / CONTROLLER_UNAVAILABLE
  "error_message": null
}
```

#### 7.6.3 GET `/api/v1/tasks/<task_id>/runs/<run_id>` — Run 详情与报告

权限 `tool.result.view`。`run_id` 必须以 `run_` 开头且不含路径分隔符，否则 404 `RUN_NOT_FOUND`。

响应为 `{ "run": ExecutionRun, "report": report.json 内容或 null（报告未生成时） }`。`report.case_results[]` 为逐条脱敏的用例结果，每条含：`status`（passed/failed/error/skipped/cancelled）、`duration_ms`、`request_summary`、`response_summary`、`assertion_results`、`failure_classification`（product_defect_candidate / environment_blocked / test_data_issue / test_case_issue / performance_candidate / unknown / none）、`performance_evaluation`（慢响应判定：阈值优先级为文档 SLA > 项目配置 > 环境配置 > 默认 3000ms；同用例、同环境、同阈值语义末尾连续 3 次超限才标记 `performance_candidate`）。

#### 7.6.4 POST `/api/v1/tasks/<task_id>/runs/<run_id>/cancel` — 取消 Run

权限 `api-test-agent.execute` + CSRF。运行时未启用返回 403 `EXECUTION_NOT_READY`；终态 Run（succeeded/failed/cancelled/timed_out）不允许取消，返回 409 `INVALID_RUN_STATE`。成功返回 200 + 更新后的 run。

#### 7.6.5 POST `/api/v1/tasks/<task_id>/runs/<run_id>/retry` — 重试 Run

权限 `api-test-agent.execute` + CSRF。先校验旧 Run 存在（否则 404 `RUN_NOT_FOUND`），然后基于最新执行预览（确认摘要重新计算）创建**新 Run**，返回 202 + 新 run。

> 本工具不提供 Run 列表接口和 Run 日志接口；Run 的唯一查询入口是上述详情接口（`run_id` 来自 execute/retry 的响应）。

### 7.7 Bug 草稿（defect-drafts）

> Bug 草稿只在任务目录内保存、编辑与下载，**没有任何外部缺陷系统推送接口**；如需提单请下载后人工提交。

#### 7.7.1 POST `/api/v1/tasks/<task_id>/defect-drafts` — 从失败结果生成草稿

权限 `api-test-agent.defect.create` + CSRF。请求体：`{ "run_id": "run_xxx", "case_ids": ["xc_bbb"], "manual_reason": "可选" }`。

约束：

- 所选用例必须存在已保存的失败结果（`status != passed` 或失败分类为 `performance_candidate`），否则 422 `DEFECT_SOURCE_INVALID`；
- 若失败分类 ∈ {`environment_blocked`, `test_data_issue`, `test_case_issue`}（不建议作为接口 Bug），必须填写非空 `manual_reason`，否则 422 `DEFECT_REASON_REQUIRED`；
- `run_id` 不存在或结果缺失返回 404 `RUN_NOT_FOUND`。

响应 201，返回 DefectDraft：

```jsonc
{
  "draft_id": "draft_7c1d2e3f4a...",
  "version": 1,
  "task_id": "task_xxx",
  "run_id": "run_3f9c2a8b1e",
  "case_ids": [ "xc_bbb" ],
  "title": "[API] xc_bbb 执行结果不符合预期",
  "module": "API 测试",
  "interface": "xc_bbb",
  "severity_suggestion": "medium",      // low/medium/high/critical
  "environment": "test",
  "preconditions": [ "使用已确认契约和用例版本执行" ],
  "reproduction_steps": [ "选择对应执行环境", "执行已确认用例", "查看失败断言与脱敏响应" ],
  "masked_request": { "method": "POST", "path": "/api/v1/orders" },
  "expected_result": "接口响应满足已确认契约与断言",
  "actual_result": "{\"status_code\": 500}",
  "status_code": 500,
  "request_id": "",
  "error_summary": "...",
  "evidence_links": [ "runs/run_3f9c2a8b1e/case-results.json" ],
  "ai_analysis": "失败分类：product_defect_candidate",
  "confidence": 0.5,
  "open_questions": [],
  "manual_reason": "",
  "created_by": "u_1001",
  "updated_by": "u_1001",
  "created_at": "2026-08-13T07:10:00+00:00",
  "updated_at": "2026-08-13T07:10:00+00:00",
  "sha256": "..."
}
```

#### 7.7.2 GET `/api/v1/tasks/<task_id>/defect-drafts` — 草稿列表

权限 `tool.result.view`。响应 `{ "items": [DefectDraft, ...] }`，按更新时间倒序。

#### 7.7.3 GET `/api/v1/tasks/<task_id>/defect-drafts/<draft_id>` — 草稿详情

权限 `tool.result.view`。`draft_id` 必须以 `draft_` 开头；不存在返回 404 `DEFECT_DRAFT_NOT_FOUND`。返回最新版本。

#### 7.7.4 PUT `/api/v1/tasks/<task_id>/defect-drafts/<draft_id>` — 更新草稿

权限 `api-test-agent.defect.create` + CSRF。请求体 `{ "base_version": 1, "fields": { ... } }`：

- `base_version` 与当前版本不一致 → 409 `REVIEW_VERSION_CONFLICT`；
- `fields` 为空或超出白名单 → 422 `REVIEW_FIELD_FORBIDDEN`。

可编辑字段白名单：`title`、`module`、`severity_suggestion`、`preconditions`、`reproduction_steps`、`expected_result`、`actual_result`、`error_summary`、`ai_analysis`、`confidence`、`open_questions`、`manual_reason`。

更新以新版本追加（`version` +1），旧版本保留；写入前对字段值再次脱敏。响应 200 返回新草稿。

#### 7.7.5 GET `/api/v1/tasks/<task_id>/defect-drafts/<draft_id>/download?format=json|markdown` — 下载草稿

权限 `tool.result.view`。下载前再次脱敏。`format=json` 返回 `{draft_id}.json`（application/json）；`format=markdown` 返回 `{draft_id}.md`（确定性生成的 Markdown 缺陷描述）；其他取值 422 `DOWNLOAD_FORMAT_UNSUPPORTED`。

### 7.8 错误码速查

**Web 接口错误码**（响应体 `error.code`，错误体格式见 §6.0）

| code | HTTP | 说明 |
| --- | --- | --- |
| `AUTH_REQUIRED` | 401 | 缺少网关身份头（未登录） |
| `PERMISSION_DENIED` | 403 | 缺少对应权限点 |
| `CSRF_INVALID` | 403 | 写操作 CSRF 校验失败 |
| `FEATURE_DISABLED` | 403 | `operation=execute_api_cases/full_pipeline`（真实执行未启用） |
| `INVALID_INPUT` | 422 | 创建任务参数不合法（operation/environment/名称长度/补充说明/文档格式/文件与文本未二选一等） |
| `TASK_QUEUE_FULL` | 409 | 创建任务时等待队列已满（默认 5） |
| `QUEUE_FULL` | 409 | 阶段入队时队列已满（cases/generate、executable-cases/generate、retry） |
| `TASK_NOT_FOUND` | 404 | 任务不存在 / 无权访问 / 非 V2 任务；也是 functional 专属 Review 接口在本工具的失败关闭码 |
| `INVALID_TASK_STATE` | 409 | 任务状态与操作要求不符（Review、入队、重试） |
| `REVIEW_VERSION_CONFLICT` | 409 | `base_version` 乐观锁冲突（契约 / 用例 / Bug 草稿共用） |
| `CONTRACT_NOT_CONFIRMED` | 409 | 生成基础用例时没有已确认的契约 |
| `CASE_NOT_CONFIRMED` | 409 | 生成可执行用例时没有已确认的基础用例 |
| `RETRY_STAGE_UNSUPPORTED` | 422 | 重试阶段不是 document_preflight / base_case_generation / executable_generation |
| `CONTRACT_NOT_FOUND` | 422 | Review changes 中的 contract_id 不存在 |
| `CASE_NOT_FOUND` | 422 | Review changes 中的 case_id 不存在 |
| `REVIEW_PATCH_INVALID` | 422 | 编辑字段为空 / 新增用例字段不完整 |
| `REVIEW_FIELD_FORBIDDEN` | 422 | 字段超出可编辑白名单（契约 / 用例 / 草稿） |
| `REVIEW_ACTION_UNSUPPORTED` | 422 | action 不受支持 |
| `CONTRACT_QUALITY_BLOCKED` | 409 | 确认存在硬阻断或状态不可确认的契约 |
| `CASE_REVIEW_BLOCKED` | 409 | 用例当前状态不可确认 |
| `HIGH_RISK_PERMISSION_REQUIRED` | 403 | 确认高风险用例需要 `api-test-agent.execute` 权限 |
| `EXECUTION_NOT_READY` | 403 | 执行运行时未启用或预览存在阻断项 |
| `EXECUTION_TARGET_DENIED` | 403 | 执行目标未登记 |
| `EXECUTION_CONFIRMATION_STALE` | 409 | `confirmation_sha256` 与最新预览不一致 |
| `EXECUTABLE_CASE_NOT_READY` | 409 | 没有通过静态校验的可执行用例 |
| `RUN_NOT_FOUND` | 404 | Run 不存在或标识不合法 |
| `INVALID_RUN_STATE` | 409 | 终态 Run 不允许取消 |
| `DEFECT_SOURCE_INVALID` | 422 | 所选用例没有已保存的失败结果 |
| `DEFECT_REASON_REQUIRED` | 422 | 不建议提单的失败分类必须填写人工理由 |
| `DEFECT_DRAFT_NOT_FOUND` | 404 | Bug 草稿不存在 |
| `DOWNLOAD_FORMAT_UNSUPPORTED` | 422 | 下载格式不是 json / markdown |
| `EXECUTOR_RESULT_INVALID` | 502 | Executor 输出无效或与 Run 不匹配（真实执行时） |
| `ARTIFACT_EXPIRED` | 410 | 产物已过期（产物保留 90 天） |

**任务异步错误码**（任务 `failed` 时展示在任务详情 `error_code`）

| code | 说明 |
| --- | --- |
| `DOCUMENT_EMPTY` | 接口文档为空 |
| `DOCUMENT_SYNTAX_INVALID` | YAML/JSON 语法错误、alias 数量超限、结构过深或节点数超限 |
| `DOCUMENT_FORMAT_UNSUPPORTED` | 不支持 Postman Collection、AsyncAPI、GraphQL、gRPC/proto、WebSocket 文档 |
| `CONTRACT_PARSE_FAILED` | 非结构化接口文档解析失败 |
| `WORKFLOW_FAILED` | 阶段执行通用失败 |
| `LLM_RATE_LIMITED` / `LLM_AUTH_FAILED` / `LLM_TIMEOUT` | 模型限流或额度不足 / 鉴权失败 / 超时（仅非结构化文档增强场景可能出现） |
| `TASK_TIMEOUT` / `WORKER_INTERRUPTED` | 阶段执行超时 / 执行进程被中断 |

**Run 错误码**（Run 失败时展示在 `error_code`）

| code | 说明 |
| --- | --- |
| `EXECUTION_TIMEOUT` | 执行超时（Run 状态 `timed_out`） |
| `CONTROLLER_REJECTED` | 执行 Controller 返回错误 |
| `CONTROLLER_UNAVAILABLE` | 执行 Controller 不可达或响应无效 |

> 服务内部还维护了一张「建议动作」映射表，包含 `CONTRACT_QUALITY_FAILED`、`CASE_SCHEMA_INVALID`、`CASE_VALIDATION_FAILED`、`TARGET_NOT_ALLOWED`、`EXECUTION_RESULT_MISSING`、`DEFECT_DRAFT_INVALID` 等码；当前版本没有代码路径会产生这些错误码，联调时可忽略。

### 7.9 权限点速查

| 权限点 | 允许的操作 |
| --- | --- |
| `tool.view` | 首页、readiness |
| `tool.execute` | 创建任务、阶段重试（`POST .../retry`） |
| `tool.result.view` | 任务列表/详情/日志/产物，contracts/cases 查询，execute/preview，Run 详情，Bug 草稿查询与下载 |
| `task.cancel` | 取消任务 |
| `task.view.all` | 查看他人创建的任务 |
| `api-test-agent.contract.review` | `PUT contracts/review` |
| `api-test-agent.case.review` | `PUT cases/review`、`POST cases/generate`、`POST executable-cases/generate` |
| `api-test-agent.execute` | `POST execute`、Run cancel/retry；也是确认高风险用例的前提 |
| `api-test-agent.defect.create` | 创建 / 更新 Bug 草稿 |
