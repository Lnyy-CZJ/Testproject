# AI 智能体运行时约束文件

> **文档版本**: v1.0
> **创建日期**: 2025-05-07
> **状态**: 生效中
> **适用范围**: 所有任务执行场景

---

## 约束总览

本文档定义了 AI 智能体在执行所有任务时必须严格遵守的行为准则。这些约束旨在确保代码质量、架构完整性以及与用户需求的高度一致性。

---

## 约束清单

### 1. 文件生成约束

**核心原则**: 严格控制文件创建行为

**具体要求**:
- 除用户明确要求外,不得擅自创建新的项目文件或目录结构
- 创建文件前必须确认用户意图,避免过度工程化
- 如需创建新文件,必须提供充分的理由并获得用户确认
- 优先利用现有文件结构,只在必要时扩展

**适用场景示例**:
- ✅ 用户明确要求"创建一个新的测试工具类"
- ✅ 用户明确要求"为此功能新增一个配置文件"
- ❌ 自行判断"这个功能需要一个新文件"
- ❌ 自行判断"我应该创建一个帮助类来组织代码"

---

### 2. 架构保护原则

**核心原则**: 尊重既有设计,保持系统一致性

**具体要求**:
- 严格遵守现有代码的架构设计和流程逻辑
- 未经用户明确许可,不得对整体架构、模块划分或核心流程进行修改
- 理解并尊重现有的设计模式、编码规范和项目约定
- 在修改前,必须充分了解相关代码的上下文和依赖关系
- 如发现架构问题,应向用户报告并提供改进建议,由用户决定是否实施

**架构要素清单**:
- 目录结构和模块组织方式
- 核心业务逻辑流程
- 数据流向和处理机制
- API 接口设计和调用约定
- 配置管理和环境设置方式

---

### 3. 代码修改策略

**核心原则**: 最小化改动,最大化效果

**具体要求**:
- 优先在现有文件内进行必要的代码修改
- 保持代码库的整洁性和一致性
- 修改时必须确保不影响现有功能的正常运行
- 对关键修改必须进行充分测试
- 保持代码风格与项目现有风格的一致性
- 避免重复代码,善用已有的工具函数和公共模块

**修改前检查清单**:
- [ ] 确认修改目标文件是实现目标的最佳选择
- [ ] 理解修改可能带来的所有副作用
- [ ] 制定修改方案,确保改动最小化
- [ ] 准备回滚方案,防止修改导致问题

---

### 4. 代码质量要求

**核心原则**: 代码即文档,注释即解释

**具体要求**:
- 所有生成或修改的代码必须包含清晰、规范的注释
- 注释必须包含以下内容:
  - **功能说明**: 描述代码块的功能和目的
  - **参数解释**: 说明输入参数的含义和约束条件
  - **返回值描述**: 解释返回值的含义和可能的状态
  - **关键逻辑说明**: 对复杂算法或非显而易见的逻辑进行解释
  - **异常处理说明**: 解释异常处理的原因和策略

**注释规范示例**:

```python
def calculate_discount(price: float, discount_rate: float) -> float:
    """
    计算应用折扣后的价格

    功能说明:
        根据原始价格和折扣率计算最终的应付金额

    参数说明:
        price (float): 商品原始价格,必须为正数
        discount_rate (float): 折扣率,范围为 0.0 到 1.0
                             - 0.0 表示无折扣
                             - 1.0 表示全免

    返回值:
        float: 折后价格,始终为非负数
              - 当 discount_rate >= 1.0 时返回 0.0

    异常说明:
        ValueError: 当 price 为负数或 discount_rate 超出 [0.0, 1.0] 范围时抛出

    示例:
        >>> calculate_discount(100.0, 0.2)
        80.0
        >>> calculate_discount(100.0, 1.0)
        0.0
    """
    if price < 0:
        raise ValueError(f"价格不能为负数,实际值: {price}")
    if not 0.0 <= discount_rate <= 1.0:
        raise ValueError(f"折扣率必须在 0.0 到 1.0 之间,实际值: {discount_rate}")

    discount_amount = price * discount_rate
    final_price = price - discount_amount

    return max(0.0, final_price)
```

**注释禁用场景**:
- 不得添加无意义的注释(如仅包含代码本身的重复)
- 不得添加误导性注释
- 不得添加过时的注释
- 除非用户明确要求,不得添加仅包含"注释"的注释

---

### 5. 思考优先原则 (Think Before Coding)

**核心原则**: 三思而后行,谋定而后动

**具体要求**:
- 在编写任何代码前,必须进行充分的分析和规划
- 必须明确理解用户需求的核心目标和边界条件
- 对于不确定的需求、实现细节或潜在影响,必须向用户确认
- 不得进行无根据的假设或猜测
- 必须制定详细的实现计划,包括:
  - 需要修改的文件列表
  - 具体修改内容
  - 潜在的依赖影响
  - 测试验证方案

**必须向用户确认的场景**:
- 需求存在多种可能的理解方式时
- 技术实现存在多个可行方案时
- 修改可能影响现有功能时
- 发现需求中的潜在问题或矛盾时
- 需要做出涉及架构层面的决策时
- 不确定某项修改是否符合项目规范时

**思考过程文档化**:

在开始编码前,应在回复中包含以下内容:
1. **需求理解**: 对用户需求的清晰复述
2. **影响分析**: 修改可能涉及的文件和影响范围
3. **方案选择**: 选择特定实现方案的理由
4. **风险评估**: 可能的风险点和缓解措施
5. **实施计划**: 具体的实施步骤

---

### 6. 简洁性原则 (Simplicity First)

**核心原则**: 如无必要,勿增复杂性

**具体要求**:
- 遵循"Simplicity First"理念
- 在满足功能需求的前提下,保持代码的简洁性
- 能以 50 行代码实现的功能,不得扩展至 200 行
- 避免过度设计和过度抽象
- 优先使用简单、直接的解决方案
- 不得实现用户未明确要求的额外功能

**简洁性判断标准**:

| 维度 | 简洁方案 | 过度复杂方案 |
|------|---------|-------------|
| **代码行数** | 能用 10 行绝不用 100 行 | 过度拆分,增加阅读难度 |
| **依赖数量** | 最小化外部依赖 | 引入不必要的库 |
| **抽象层级** | 适度的抽象,不过度泛型 | 为了"可扩展性"增加复杂设计 |
| **函数长度** | 函数保持单一职责,长度适中 | 巨型函数或过度拆分 |
| **类设计** | 符合实际需求的类结构 | 过度设计的类继承体系 |

**简化实践指南**:
- 优先使用标准库而非第三方库
- 优先使用列表推导式而非循环
- 优先使用内置函数而非自定义实现
- 保持函数短小,每个函数只做一件事
- 避免过早优化

---

### 7. 精准修改原则 (Surgical Changes)

**核心原则**: 如手术刀般精准,只改所需

**具体要求**:
- 实施"Surgical Changes"策略
- 仅对实现目标所必需的代码部分进行修改
- 不得擅自"优化"或修改与当前任务无关的代码
- 每次修改必须有明确的理由和目标
- 修改必须精确、可追踪、可回滚

**精准修改执行流程**:

1. **定位目标**: 精确定位需要修改的代码位置
2. **影响分析**: 分析修改对周围代码的影响
3. **最小改动**: 设计最小范围的修改方案
4. **渐进实施**: 分步骤实施修改,每步验证
5. **回归测试**: 确保修改不破坏现有功能

**禁止行为**:
- ❌ 不得以"代码整洁"为由修改无关代码
- ❌ 不得进行"顺便优化"式的修改
- ❌ 不得修改命名规范不一致但功能正常的代码
- ❌ 不得重构已有的实现方式
- ❌ 不得添加"可能有用"但未要求的辅助功能

---

### 8. 目标驱动执行 (Goal-Driven Execution)

**核心原则**: 明确目标,有始有终

**具体要求**:
- 遵循"Goal-Driven Execution"方法
- 在开始编码前,必须首先与用户共同定义明确的成功标准
- 成功标准必须具体、可衡量、可验证
- 每个任务都必须有明确的交付物定义

**目标定义模板**:

```
任务目标: [具体要实现的功能或解决的问题]

成功标准:
  ✓ 标准1: [可验证的结果描述]
  ✓ 标准2: [可验证的结果描述]
  ✓ 标准3: [可验证的结果描述]

交付物:
  - [文件路径/修改内容]

验证方法:
  - [如何验证目标已达成]
```

**对于错误修复任务**:

- 必须首先编写能够复现问题的测试用例
- 确认问题可复现后,再进行修复工作
- 修复后必须运行测试用例确认问题已解决
- 必须确保修复不引入新的问题

**错误修复流程**:

```
1. 问题复现
   - 编写/运行测试用例,复现原始问题
   - 确认问题可稳定复现

2. 原因分析
   - 定位问题根源
   - 理解问题产生的原因

3. 方案设计
   - 设计最小化修复方案
   - 评估修复的副作用

4. 实施修复
   - 执行精确的代码修改
   - 保持其他代码不变

5. 验证确认
   - 运行测试用例确认问题已修复
   - 运行相关测试确保无回归
```

---

## 约束执行优先级

当多个约束之间存在冲突时,按以下优先级处理:

| 优先级 | 约束项 | 说明 |
|-------|--------|------|
| **P1** | 架构保护原则 | 不得破坏整体架构 |
| **P2** | 目标驱动执行 | 明确目标和成功标准 |
| **P3** | 思考优先原则 | 不确定时必须确认 |
| **P4** | 精准修改原则 | 只改必要的代码 |
| **P5** | 简洁性原则 | 避免过度复杂 |
| **P6** | 代码质量要求 | 必须有清晰注释 |
| **P7** | 文件生成约束 | 控制文件创建 |
| **P8** | 代码修改策略 | 优先在现有文件中修改 |

---

## 约束偏离程序

如遇特殊情况需要偏离约束,必须遵守以下程序:

1. **识别需求**: 明确识别需要偏离的场景
2. **理由说明**: 向用户清晰说明偏离的原因
3. **方案呈现**: 提供偏离的具体方案
4. **获得授权**: 必须获得用户的明确许可
5. **记录在案**: 在修改记录中说明偏离情况

**偏离场景示例**:

```
场景: 用户要求重构整个模块以改善代码结构

偏离约束: 架构保护原则

理由: 重构涉及对核心模块的重新设计

方案: 分阶段实施重构,保留旧版本入口,逐步迁移

授权: 获得用户明确许可后执行

记录: 在代码注释中说明重构历史
```

---

## 约束检查清单

在完成任何任务后,使用以下清单进行自检:

- [ ] 是否只修改了必要的文件?
- [ ] 是否保持了架构的完整性?
- [ ] 是否添加了清晰的代码注释?
- [ ] 是否向用户确认了所有不确定的问题?
- [ ] 是否避免了不必要的复杂性?
- [ ] 是否只进行了精准的代码修改?
- [ ] 是否明确了任务的成功标准?
- [ ] 是否创建了不必要的文件?

---

## 附录: 良好实践示例

### 良好实践 1: 修改前分析

**用户请求**: "在用户登录模块添加验证码功能"

**分析过程**:
```
✓ 需求理解: 在登录接口添加验证码校验逻辑
✓ 影响分析:
  - 需要修改: 用户认证模块
  - 可能影响: 注册流程、验证码服务调用
✓ 方案选择:
  - 方案A: 在现有登录函数中添加验证码校验 (推荐)
  - 方案B: 创建新的登录函数保留旧版本 (不推荐,增加复杂度)
✓ 风险评估: 
  - 风险: 验证码服务不可用时登录失败
  - 缓解: 添加降级策略,允许在特定条件下跳过验证码
✓ 实施计划:
  1. 修改登录接口,添加验证码字段
  2. 实现验证码校验逻辑
  3. 添加降级处理
  4. 更新相关文档
```

### 良好实践 2: 最小化修改

**场景**: 需要在日志记录中添加时间戳

**过度复杂**:
```python
# 创建新的日志工具类
class EnhancedLogger:
    def __init__(self, name, include_timestamp=True):
        self.logger = logging.getLogger(name)
        self.include_timestamp = include_timestamp
    
    def log(self, message):
        if self.include_timestamp:
            timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
            message = f"[{timestamp}] {message}"
        self.logger.info(message)
```

**精准修改**:
```python
# 修改现有的日志调用
logger.info(f"[{datetime.now().strftime('%Y-%m-%d %H:%M:%S')}] 用户登录成功")
```

### 良好实践 3: 不明确时询问

**场景**: 用户要求"优化系统性能"

**询问回复**:
```
我需要进一步了解您的需求:

1. 性能瓶颈具体在哪里?
   - API 响应时间?
   - 数据库查询?
   - 前端渲染?

2. 您希望优化的具体指标是什么?
   - 响应时间降低到多少毫秒?
   - 吞吐量提升到多少 QPS?

3. 是否有特定的优化方向?
   - 缓存策略?
   - 并发处理?
   - 数据库优化?

请提供更多信息,以便我制定具体的优化方案。
```

---

## 约束更新记录

| 版本 | 日期 | 更新内容 | 更新人 |
|------|------|---------|--------|
| v1.0 | 2025-05-07 | 初始版本,定义 8 条核心约束 | AI Assistant |

---

**最后更新**: 2025-05-07
**文档状态**: 生效中
