# 功能测试脑图工作台 V2 视觉与交互 QA

## 证据

- source visual truth path：`/var/folders/0y/bx0h56fj4z5bypwbfr5l7nzr0000gn/T/codex-clipboard-180d09fe-cdb1-42d2-8732-a4a881368672.png`
- implementation screenshot path：`/tmp/functional-workbench-v2-final-1440x900.png`
- 视口：1440 × 900 CSS px，device scale factor 1
- 源图像：1451 × 685 px；实现截图：1425 × 891 px（浏览器内容区扣除滚动条）
- 状态：源图为已完成用例脑图；实现为 `waiting_case_review` 可编辑用例脑图。状态差异属于产品流程差异，不用于判断状态文案一致性。
- 比较方式：同一工具结果中同时打开源图和实现截图，先比较全局信息架构，再聚焦任务头、标签栏、操作栏、脑图画布和右侧详情区。

## 必查表面

- 字体与层级：使用项目既有系统字体；任务标题、Review 标题、标签、工具按钮和节点文本层级清楚。未引入展示型字体。
- 间距与布局：任务头、阶段轨、摘要、标签、工具栏、画布与详情形成连续桌面工作台；1280、1440、1920 三档无横向页面溢出。
- 色彩与 token：保持平台既有蓝色强调、灰白表面和语义状态色。脑图采用单强调色而非复刻参考图多色分支，这是与现有设计系统一致的有意差异。
- 图像与资产：页面不依赖位图、占位插画或自制 SVG；脑图由固定版本的 Mind Elixir 5.14.0 本地渲染。
- 文案与内容：明确“脑图编辑、表格只读、AI 只产生建议、显式保存/确认”，没有把需求说明文字暴露为页面占位内容。
- 可访问性：工具栏为语义按钮；脑图节点补充 `treeitem`、`tabindex=0` 和 Enter/Space 选择；焦点轮廓、非纯颜色状态和 reduced-motion 规则存在。

## 比较历史

### 第一次比较

- [P1] 初次进入画布时脑图位于可视区域之外，首屏呈现空白。
  - 修复：在字体和连线完成测量后自动执行一次 `scaleFit`，大数据默认折叠到一个代表分组。
  - 复验：测试点和用例脑图均在首屏显示根、分组和叶子节点。
- [P1] Mind Elixir 5.14.0 的 `selectNodes` 返回 `NodeObj[]`，原适配器按 Topic 读取，导致详情不联动。
  - 修复：兼容 NodeObj 事件，并使用投影期 `data-nodeid → meta` 只读映射作为点击、键盘和拖动定位来源。
  - 复验：节点选择显示详情；编辑后只读表格同步；保存后 revision 从 0 变为 1。
- [P2] 任务状态胶囊与右上角操作按钮发生覆盖。
  - 修复：为状态和操作区预留固定布局空间，并限制长状态文本宽度。
  - 复验：`/tmp/functional-workbench-v2-case-1440x900-postfix.png` 中两者不再重叠。

### 最终比较

- 全视图：信息架构与参考方向一致，均以任务头、脑图/表格/版本标签、编辑工具、脑图主体为核心；实现增加了阶段轨、服务端校验摘要和右侧结构化详情，属于已批准产品要求。
- 聚焦区域：脑图和工具栏在 1440 × 900 清晰可用；表格无 input/textarea/select，点击行能返回脑图并定位选中节点。
- 控制台：检查 warning/error，未发现运行错误。
- 主要交互：节点点击与键盘选择、详情编辑、应用修改、撤销状态、表格双向定位、显式保存、版本标签、新建任务弹窗。

## 遗留 P3

- 参考图使用多色分支和状态 emoji，实现保持平台单蓝色强调与纯文本节点；这是设计系统一致性选择，不阻塞交付。
- 大数据首次只展开一个代表分组，避免 500 个节点同时显示；用户可继续逐层展开或搜索。

## 最终结果

final result: passed
