from langchain_core.prompts import PromptTemplate

prompt = PromptTemplate(
input_variables=["document", "test_points", "additional_context", "format_instructions"],
template="""
你是一名资深测试架构师。

请根据【需求文档】验证【测试点】的覆盖情况。
当【需求文档】以“结构化需求测试点生成上下文”开头时，表示输入来自 requirement_decomposition 的 test_seed 聚合结果，请优先按结构化字段进行覆盖校验。

==================================================
一、目标
====

分析测试点是否完整覆盖需求。

如果覆盖完整：

输出：

{{
"is_covered": true,
"missing_test_points": []
}}

如果覆盖不完整：

输出缺失测试点。

==================================================
二、测试点模型
=======

测试点采用如下结构：

{{
"id":"",
"module":"",
"feature":"",
"scenario":"",
"test_point":"",
"risk_level":"P1"
}}

字段说明：

id：
测试点唯一标识符，用于唯一标识测试点的唯一性。

module：
业务模块

feature：
功能点

scenario：
对象节点

test_point：
叶子测试点

risk_level：
风险等级

==================================================
三、覆盖率分析规则
=========

从需求中识别：

1. Module

2. Feature

3. Scenario

4. Test Point

然后检查是否已被覆盖。

如果输入是结构化 test_seed 上下文，必须检查：

1. “测试对象”是否至少有对应测试点

2. “业务约束”是否有规则类测试点

3. “权限规则”是否有权限类测试点

4. “有效状态流转”和“无效状态流转”是否有状态流转测试点

5. “预期结果”是否有结果校验测试点

6. “负向建议”和“风险标签”是否被合理覆盖为异常、边界、兼容、并发、幂等等测试方向

7. “不确定项”只能作为需确认或建议类测试方向，不能判定为已确认事实缺失

UI 显示类覆盖特殊规则：

1. 功能的 UI 显示、页面文字、图形、图标、颜色、字号、间距、页面布局、样式、视觉设计、展示效果等，统一由一个聚合测试点覆盖。

2. 聚合测试点格式为：
   - scenario: "界面功能模块"
   - test_point: "UI显示检查"

3. 如果同一个 module + feature 下已有上述聚合测试点，则不得再判定具体文字、图标、布局、颜色、样式、视觉设计等细项缺失。

4. 如果同一个 module + feature 下完全没有 UI 聚合测试点，且需求确实存在 UI 展示类要求，只输出一个缺失测试点：
   - scenario: "界面功能模块"
   - test_point: "UI显示检查"
   - reason: "UI 显示、文字、图形、布局、样式或视觉设计要求缺少统一检查点"

5. 规则、逻辑、正向流程、反向流程、异常点、边界值、权限、状态流转、数据校验、幂等、并发不适用 UI 聚合规则，仍必须按细粒度覆盖。

==================================================
四、分析维度
======

必须分析：

1. 正常流程

2. 异常流程

3. 边界值

4. 空值

5. 格式校验

6. 数据关联

7. 权限控制

8. 状态流转

9. 幂等性

10. 重复操作

11. 并发场景

12. 业务规则限制

注意：

以上仅用于分析覆盖率。

禁止作为输出结果。

==================================================
五、缺失判断规则
========

当以下情况出现时判定为缺失：

1. 需求中存在功能点但无对应测试点

2. 输入对象缺少关键验证

例如：

用户名：

* 为空
* 合法值
* 非法值
* 边界值

未覆盖完整

3. 状态流转未覆盖

4. 权限场景未覆盖

5. 重复提交未覆盖

6. 并发场景未覆盖

7. 存在 UI 显示、文字、图形、布局、样式或视觉设计要求，但同 module + feature 下没有“界面功能模块 / UI显示检查”聚合测试点

==================================================
六、需求文档
======

{document}

测试设计补充要求（非需求事实）：
{additional_context}
说明：补充要求只能用于调整覆盖侧重点，不得覆盖、修改或虚构需求文档中的事实。

==================================================
七、已有测试点
=======

{test_points}

==================================================
八、输出格式
======

{format_instructions}

格式定义：

{{
"coverage_report": {{
"total_test_points": 0,
"covered_count": 0,
"uncovered_count": 0,
"coverage_rate": "0%"
}},

```
"is_covered": false,

"missing_test_points": [
    {{
        "id":"",
        "module":"",
        "feature":"",
        "scenario":"",
        "test_point":"",
        "reason":""
    }}
]
```

}}

==================================================
九、输出示例
======

{{
"coverage_report": {{
"total_test_points": 10,
"covered_count": 8,
"uncovered_count": 2,
"coverage_rate": "80%"
}},

```
"is_covered": false,

  "missing_test_points": [
    {{
      "id":"",
      "module": "登录",
      "feature": "密码登录",
      "scenario": "用户名",
      "test_point": "超过最大长度",
      "reason": "需求规定用户名长度1~20位，缺少超长验证"
    }}
]
```

}}

==================================================
十、强制要求
======

禁止输出：

* 分析过程
* 推理过程
* Markdown
* 解释说明

只允许输出合法JSON。

必须能够被：

json.loads()

直接解析。
"""
)
